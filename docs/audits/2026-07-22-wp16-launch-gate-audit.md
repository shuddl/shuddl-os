# WP-16 launch-gate adversarial audit (REQ-119) — 2026-07-22

**Scope:** the full build at launch scale — ~20 targets (`packages/{ledger,rater,contracts,agents,edi,map,adapters,driver-core,design}`, `workers/{api,agents,mcp,translator,billing}`, `apps/{command,driver,portal}`, the CI gates, the migrations, the WP-15 overlay/authority seam, and the WP-16 changes themselves).
**Method:** 8 parallel adversarial lenses × the 12 deployed `.claude/skills/` × the Ten Laws (genesis/00) + invariants I1–I8 (genesis/10) + the hard budgets. Each lens ran **default-to-refute** — a finding survives only if reproducible against the real code with a `file:line` + an input→wrong-output. Seeded with the known-open 2026-07-15 Highs (now all fixed this branch) to confirm closure.

## Verdict: ZERO open Criticals. CLEAR-TO-CLOSE (CLAUDE.md rule 9).

The 2026-07-15 audit's Criticals (positions bypass C-1/REQ-190, duplicate-booking C-2/REQ-191) remain closed (WP-09). Its four code-unremediated Highs (identity-leak fail-open, idempotency-4xx, design-CI blind spots, map bearing) were fixed + reconciled this WP (REQ-167/206/207/208). The crown jewels — canonical hash + chain, append-only, tenant isolation, gate parity, money-as-projection, I6 redaction, the WP-15 source-aware overlay — all probed clean at launch scale.

## Findings register (most-severe first)

| # | Sev | Finding | File:line | Disposition |
|---|---|---|---|---|
| 1 | **HIGH** (latent) | An `invoice.corrected` **void** (empty `reissue_lines`) nets `money_lines` to 0 but left the `invoices` row `status='issued'`, so QB/GL export said 0 while AR-aging + DSO + the Collector overstated the voided invoice permanently (two read-models silently disagree). The exact defect `keep-readmodel-consistent-with-ledger` documents; tests blessed it. Latent today (no `invoice.corrected` producer exists — grep-confirmed) — becomes Critical the instant a void producer ships. | `packages/ledger/src/projection/money.ts:175` | **FIXED `1d3cf83`** — a `void` `InvoiceUpsert` mode flips the row to `status='void', total_cents=0` in the same batch; reconciliation test proves invoices-AR == money_lines-net after a void; bug-blessing tests flipped. No migration (no `invoices.status` CHECK). Corroborated by 2 lenses. → REQ-209 |
| 2 | **MED** | `check:coverage` (the flagship 100% register gate) + `test:acceptance` ran in no `.github/workflows/ci.yml` — enforced only by a manual `pnpm verify`, so a future PR with an unaccounted row stayed green on CI. | `.github/workflows/ci.yml` | **FIXED `c11eb25`** — wired `check:coverage` + `check:authority-coverage` into ci.yml; `REQUIRE_DENYLIST=1` on the identity step pins its fail-closed on any runner. → REQ-211 |
| 3 | **LOW** (forward-guard) | `redact.ts` `INTERNAL_NESTED` omitted `payment.received` / `settlement.executed` — both counterparty-default + loose-JsonObject payloads the money projection reads `division` from — so a party lens *would* leak the org/margin `division` if a real emitter added it. Not reachable today (`_platform`-only `payment.received` has no division; `settlement.executed` CONFIRM-gated). | `packages/ledger/src/redact.ts:12` | **FIXED `1d3cf83`** — added both kinds with `["division"]`; a division-carrying test + the strengthened REQ-192 general guard. → REQ-210 |
| 4 | **LOW** (local-env) | The product's stray-SQL / eslint fences scanned the whole filesystem, so a git-ignored untracked sibling project sharing the repo dir false-tripped `check:invariants` / `lint` (CI green on the tracked tree regardless). | `tools/checks/invariants.ts` | **FIXED `c11eb25`** — the stray-SQL fence is now git-aware (`git ls-files --cached --others --exclude-standard`, + `.claude/` governance excluded); eslint ignores the same siblings. → REQ-211 |
| 5 | **LOW** (defense-in-depth, deferred) | The append-only BEFORE INSERT guards (`0003_insert_guards.sql`) don't enumerate every UNIQUE surface (`events.hash`, `ux_events_device`, `ux_ml_corrects`) — so if a REPLACE/upsert ever evaded the lint OR a new UNIQUE were added to a guarded table without extending the guard, a Merkle-chained row could be silently rewritten. NOT exploitable today: every writer uses plain `INSERT` (unique-collision → ABORT, which is how I7 is enforced), and REPLACE/`ON CONFLICT DO UPDATE` are lint-banned on all three guarded tables in source AND migrations. A merge-ready complete guard is drafted at `.claude/skills/complete-append-only-insert-guards/reference-0006_events_guard_ins_complete.sql`. | `db/tenant/migrations/0003_insert_guards.sql:9` | **DEFERRED** → REQ-212 (vNEXT): ship the complete guard migration + a `WHEN`-clause completeness lint. Recorded in GO-LIVE-CHECKLIST §3. |
| 6 | **LOW** (defense-in-depth, deferred) | The `source:'legacy'`/`edi`/`email` lock is enforced at the ROUTE layer only (every client seam coerces/hardcodes `native`); the DO append trusts `parsed.source`. A future append route that forgets to coerce would silently reopen the source-forgery vector. NOT exploitable today (every current route covered + tested; MCP/EDI internal seams call the DO directly). | `workers/api/src/do/sequencer.ts` | **DEFERRED** → REQ-213 (vNEXT): a one-line DO-side assertion (reject a client-supplied non-`native` source unless an internal-caller flag, mirroring `platform:true`) makes the invariant structural. Recorded in GO-LIVE-CHECKLIST §3. Flagged by 2 lenses. |

## Informational (not defects — fail-safe / DARK / documented)
- **Comms parity counts internal ops notes** (`sla-sweep`/`biller` `message.received` notes inflate the native comms count) — fail-safe (only pushes drift UP, harder to flip to native, never a false green); comms parity is a documented coarse count. Future parity-accuracy refinement.
- **`usage_credits` row-identity/roster divergence at R4** — provisioning keys `uc-<slot_id>-<period>` (tenant_id=slot) while the metering sweep keys `<slug>:<period>` and iterates only static `TENANT_SLUGS`; a pool-provisioned tenant would carry two rows + not be swept. Entirely latent while PLG is DARK — reconcile before R4 (GO-LIVE-CHECKLIST). 
- **`internal-platform` authority-flip** — the secret-gated server-to-server seam could append `authority.flipped` on `_platform`'s t:root without `gatesGreenFor`; `_platform`-only, no customer impact, forces `source:native`. Same class as #6 (route-vs-DO enforcement).
- **`check:coverage` status-drift** — 26 register rows still tagged `*-DISCOVERED`/`vNEXT` while their code has shipped; advance at register review (surfaced by the coverage gate, non-failing).

## REFUTED-clean at launch scale (the crown jewels)
- **Ledger core** — I3 append-only (DO mutex load-bearing, plain-INSERT-ABORT enforces I7), canonical byte law (JCS/integer-only, NULL→omitted, `check:seed` meaningful), hash chain (server-assigned seq/prev_hash/hash, `EventInput` carries none), I2 (no invoice without POD).
- **Gate parity + tenant isolation** — every API-reachable gated fact re-enforces the server-side gate (positions bypass regression-locked via the shared `gate-context`); tenant from the JWT claim only (header/query rejected at auth); every R2/D1 key tenant-prefixed; DO structurally pinned; auth HS256-pinned.
- **Agents** — model never gates a price/identity/booking (identity keys off the authenticated envelope sender); every Queue consumer idempotent (deterministic ids, no Date/random); no LLM in `packages/ledger`; the interline executing-share floor (never gross) + the $222K anomaly hold.
- **Visibility/redaction (I6)** — every typed counterparty payload's internal field stripped at every depth (structural walk); visibility fails closed; projection-read leaks closed.
- **MCP / EDI / public** — OAuth PKCE + active-pairing re-resolve per call; caps DO-serialized fail-closed; no-bypass chokepoint; EDI HMAC + quarantine-not-drop + stops at quote.accepted; `/pub/*` host-resolved/MAC-verified/DARK; webhooks whitelist-payload + HMAC-signed.
- **WP-15 overlay/authority** — source:legacy internal-mirror-only + projection-skipped + read-model-excluded; flip guard fresh-evaluated + money blocked-by-construction + t:root-locked; Watchtower auto-fallback structurally to-legacy-only; echo converges (one stableStringify); resolveAuthority consulted on every module.
- **Design / map / budgets** — 5 tokens / 2 fonts / 0 shadows/gradients (the WP-16 token-count + JSON-scan checks bite); great-circle bearing + epsilon-hold (no due-north snap, no fabricated position); 21/22 tables, 35 kinds, 11/12 views.
- **The WP-16 gates themselves** — `coverage.ts` pure+total + fails on any unaccounted row; `identity-leak` genuinely fail-closed in CI; `cms.ts` no verify bypass; `idempotency` both directions; the acceptance runner can't green a broken tree.
- **PLG / driver / whole-build Laws** — `_platform` two-way isolation; atomic pool provisioning; exact OVERWRITE metering; idempotent signature-verified credits; server-side driver gate + loss-free offline + device auth; the do-not-build guards (no native GL / driver-pay / report-builder / 4th surface / 5th primitive; CONFIRM-gated features unbuilt).

## Fixes shipped this audit
`1d3cf83` (invoice-void read-model consistency + redaction forward-guard) · `c11eb25` (CI coverage wiring + git-aware fences). Deferred defense-in-depth items → REQ-212/213 (vNEXT), recorded in `docs/ops/GO-LIVE-CHECKLIST.md §3`.
