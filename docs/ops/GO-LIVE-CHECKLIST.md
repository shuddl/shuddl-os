# SHUDDL — Go-Live Checklist & Technical-Debt Ledger

**Path:** `docs/ops/GO-LIVE-CHECKLIST.md` · **Owner:** register owner · **Last amended:** 2026-08-05 at `cafb4d4` — six rows changed, and **four of them RELOCATED** since the last full re-audit, and this is a DIFFERENT claim from it (audit §330): amending rows is not re-auditing the ledger. (1) *Staging placeholder ids* re-measured — 1 `placeholder-resource-id`, not 5 (audit §291). (2) New row in *Repository-owned failures & debt*: REQ-030 authority-registry completeness (audit §313/§315). (3) That row moved **OPEN → TRIPWIRED** once the population gained a merge-profile pin (audit §326), leaving the repository-owned section with no OPEN DEFECTS. (4) A Low legibility row added for the hard-budget line's unlabelled ceilings (audit §336) — an owner decision about governing text, not a defect. (5) A second Low legibility row: the CONFIRM-GATED prohibition's 3-item parenthetical against the register's 14 rows (audit §338) — the same category, third member. (6) A Low row for rule 6's *routes ±10%* gate, which has no fixture, checker or register row (audit §344). **(7) Rows (2)–(6) were inserted into the WRONG TABLE** — a 3-column one — and rendered with five of eight cells dropped, so their severity, ownership, evidence, owner, status, grade and expiry trigger were invisible to a reader while the rows looked filed. Caught by `check:tables` in the phase's closing `verify:merge`, not by the fifty green runs before it, because the habitual verification subset never included that gate (audit §349). All four moved into the eight-column ledger and re-rendered. **Last synthesized:** 2026-07-19 (from the 5 WP-12-era audit sweeps) · **Last re-audited:** 2026-07-27 (V1 close-out Task 6 — every hold and failure row re-run against a live command *where one exists*; two holds have no gate at all and now say so. See §1.1 for the row schema and the bottom two ledgers for the schema applied) · **Closed out:** 2026-07-27 (V1 close-out Task 8 — the six objective questions, each answered with a citation or an honest no, and the close-out statement itself, are **§5 at the foot of this file**) · **Record-corrected:** 2026-07-27 (final cross-task review — defects visible only when two tasks' output is read together: the "nothing deployed" denial vs. a deployed, live-sending staging; the stale portal-is-synthetic claim in the REQ-136 launch-gate artifact; three miscounts; three unresolvable citations. Every correction is superseded in place per §1.1, and every corrected figure comes from a command re-run at `79ae54d`) · **Post-reboot reconciliation:** 2026-07-28 at HEAD `3fc592b` — the machine was rebooted, the `workerd` wedge cleared, and everything that was unmeasurable was measured: `packages/ledger` 598 tests, `workers/api` 719 tests (three consecutive runs), the full `pnpm test` 3,243 tests, `pnpm test:acceptance` green, and `pnpm verify:merge` run for the first time on this branch (16 gates PASS / 5 BLOCKED, aggregate **NOT PROMOTABLE** on absent private fixtures). The dagger comes off the failures ledger's first row, the `workerd` row is RESOLVED, and §5's "carries no evidence record at all" is superseded in place · **Anchor/anomaly debt cluster closed:** 2026-07-29 — the four adversarially-raised anchor rows in the failures ledger (stale marker outliving an anchored day · uncontained pre-loop scan · the substring test seam · the UPSERT that would not re-open a resolved alarm) are **FIXED** at `579a167`, `05f140a`, `cadb115`, `08e2156`, each superseded in place per §1.1. All four were closable by a commit and none needed a register amendment; every one now cites a run rather than a source read, and the two whose fix could have been vacuous carry a mutation proof · **Reconciled to the deployed world:** 2026-08-01 — the 2026-08-01 debt audit (19-agent, adversarially verified: `docs/audits/2026-08-01-technical-debt-audit.md`) found this file's External-holds ledger and three §2 cells still asserting the pre-provisioning world (BLOCKED / all-zero / "no backup exists" / "prod pending" / mcp "Not deployed") while its own reconciliation table and `RELEASE-EVIDENCE.md` recorded the 2026-07-30/31 provisioning, the preflight PASS-72, and the surfaces going live. Five hold rows and three cells superseded in place; one NEW repo-owned failures row appended (the agents-worker pool-tenant roster gap, hardened same-day); the EDI activation row gains the ISA/GS date latent

## 1. Purpose & upkeep

This is the single central ledger of everything that must be **done by a human** before SHUDDL can go live (secrets, Cloudflare/DNS provisioning, engagement-workspace fixtures, per-tenant/per-partner activation, milestone/CONFIRM gates, monitoring/DR) and everything that is **deferred, shortcut, or fails open** in the code. Its job: when every WP reads "done," nothing operational or debt-shaped is silently missing. It **points at** the existing ops docs (`secrets.md`, `DEPLOYMENT.md`, `dr-backups.md`, `slo.md`, `PROJECT-STATE.md`, `fixtures/manifest.json`) rather than re-stating them — see §4. **Keep it current:** every WP close-out and every adversarial-audit swarm (REQ-119) appends new operator/debt rows here as part of DoD; when an item lands, flip its Status/Severity in place (do not delete — struck-through history is traceability). Re-baseline against `genesis/09-REQUIREMENTS-REGISTER.csv` at each WP exit; a register status still reading `*-DISCOVERED`/`vNEXT` while the code shipped is itself a debt row (see §3).

### 1.1 The eight-field row schema (added by the V1 close-out Task 6, 2026-07-27)

Every row in the two ledgers at the **bottom** of this file — **External holds** and **Repository-owned failures & debt** — carries all eight fields below. The §2 operator tables and the §3 debt tables predate the schema and keep their original columns: they are narrative history, and re-columning every legacy row would rewrite the wording this file exists to preserve. Where a legacy row is now factually wrong it is **superseded in place** with a dated note — struck through, never deleted.

| Field | Vocabulary / rule |
|---|---|
| **Item** | what is wrong, in one clause |
| **Severity** | **Critical** = a launch-gate stopper or a live correctness/security defect · **High** = weakens or blocks a gate or a go-live path · **Med** = correctness/privacy residual, environment hazard, or a record that misleads a reader · **Low** = deferred refinement, fail-safe, or informational |
| **Ownership** | **Repo** = reproducible from this checkout and closable by a commit · **External** = needs an account, a credential, a licence, a vendored private artifact, or a named human; no commit can clear it |
| **Proof** | the exact command and the verdict it printed — never a summary. A row whose claim no command can produce says so plainly — **"no gate exists"**, or **"source read"** with the `file:line` a reader can open — and never dresses a reading up as a run. That absence is itself a finding. |
| **Owner** | infrastructure · backend · assurance · ops · counsel · founder · on-call · register owner |
| **Status** | `OPEN` · `FIXED` · `BLOCKED` (external; cannot be worked from here) · `NOT_APPLICABLE`. A **†** after the status means the claim is **not verified in this environment** — ~~see the workerd row in the failures table~~ **superseded 2026-07-28: no row in this file carries a † any more.** Exactly one ever did — the `anchors/run` containment fix in the failures ledger — and it was observed green at HEAD `3fc592b` once the reboot cleared the `workerd` wedge, so both that row and the wedge row below it are now resolved. The marker keeps its definition because the condition that minted it **recurs**: the next wedged run re-earns a dagger, and a claim that cannot be run here must wear one rather than read as a pass. |
| **Blocks grade** | the lowest V2 release grade (`docs/ops/V2-EXECUTION-FRAMEWORK.md` §9: R0 audited → R1 mergeable → R2 staging-certified → R3 pilot-ready → R4 production-ready → R5 authority cutover) this row denies |
| **Evidence expires** | when the verdict above stops being evidence — **at the earlier of** (a) the SHA changing in the files the proof reads, and (b) the external fact it depends on changing (credentials rotated, resources provisioned, fixtures vendored, a human named). Past either, the row is unproven: re-run the command. |

---

## 2. Operator / deploy requirements

Legend — **Blocks:** Yes = cannot go live for that surface/feature without it; No = safe default holds or non-blocking.

### Secrets & auth

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| `JWT_SECRET` (+ derived `STATUS_SECRET`/`DOC_SECRET`, domain-separated) | WP-01 · REQ-154/134 · `workers/api/wrangler.toml`; `secrets.md:14` | `wrangler secret put JWT_SECRET --env <env>` via OIDC; rotate 90d/on-suspicion | Yes (all authed `/v1` routes) | Set on staging; ~~**prod pending**~~ **DONE prod 2026-07-31** (one value across api+mcp; preflight PASS carried it) |
| `IDENTITY_DENYLIST` (REQ-167 leak lint) | WP-01/WP-12 · REQ-167 · `tools/checks/identity-leak.ts:35-61`; `secrets.md:15` | Set GitHub Actions secret (or gitignored `.identity-denylist.local`); rotate on tenant on/offboarding | No (CI-time) — **but the lint SKIPS locally without it, so a local run proves nothing about REQ-167** | ~~Open — fails open locally & CI~~ **Corrected 2026-07-27: fails CLOSED in CI, OPEN locally.** ~~CI binds the secret (`.github/workflows/ci.yml:49`)~~ **CORRECTED 2026-08-11 (audit §960/§962): `ci.yml:49` REFERENCES `${{ secrets.IDENTITY_DENYLIST }}`, and `gh api …/actions/secrets` returns `{"total_count":0}` — the secret does not exist, so the env var is the empty string. Referencing is not binding, and the gate is BLOCKED in CI exactly as it is locally** and `REQUIRE_DENYLIST`/`CI` force the fail-closed branch (`tools/checks/identity-leak.ts:54-59`). Verdicts: `pnpm -s check:identity` → `Lint SKIPPED` exit 0; `pnpm -s check:identity -- --mode merge` → `{"gate":"identity-leak","status":"BLOCKED","executed":false}`. Still **Open** — the secret is unset here |
| `RESEND_API_KEY` + `EVIDENCE_FROM` (evidence email) | WP-06 · REQ-092 · `workers/agents/wrangler.toml:81-84`; `packages/agents/src/biller/sender.ts:200@apiKey`; `secrets.md:17,20` | Set per env; `EVIDENCE_FROM` domain must be a verified Resend sender. Unbound ⇒ `NotConfiguredSender` rejects loudly (no send) | Yes (acceptance demo #1 POD→email) | Staging armed (`send.shuddl.tech`); **prod is a CONFIRM-gated flip** |
| Device signing root key | WP-01/WP-05 · REQ-134 · `secrets.md:16`; threat-model.md:8 | Hold in control plane; run key-rotation drill | Yes (evidence attribution) | Placeholder; drill executable only once WP-05 device signing lands |
| `TEST_SEND_TOKEN` + `ALLOW_TEST_SEND` | `secrets.md:18`; `workers/agents/wrangler.toml:17-22` | `wrangler secret` in dev/staging **only** | No — guardrail: **must stay OFF in prod** | Tracked |
| `ANTHROPIC_API_KEY` + `COPILOT_MODEL` (Command copilot) | REQ-038/024 · `workers/api/src/routes/copilot.ts:34-41` | Bind to enable `ClaudeCopilot`; unbound ⇒ `DeterministicCopilot` floor | No (deterministic floor) | CONFIRM-gated, optional |
| `ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL` (Concierge `ClaudeParser`) | REQ-024 · `workers/agents/src/index.ts:169`; `packages/agents/src/concierge/parse.ts:242` | Bind to flip from `DeterministicParser`; unbound ⇒ `NotConfiguredParser` rejects loudly; model only steers, never gates | No (deterministic parser is the floor) | CONFIRM-gated flip |
| Stripe billing keys/webhooks | WP-14 · REQ-123 · CLAUDE.md stack; threat-model.md:9 | Provision when PLG billing activates; credits metered as Money events on platform tenant | Gates PLG billing (CONFIRM-gated) | **GAP — no row in `secrets.md`** |
| Twilio (or equiv) SMS credentials | WP-06 · REQ-097 · `packages/agents/src/biller/sender.ts:266` | Provision when SMS evidence fallback activates | No (email-only floor) | **GAP — no secret row** |
| **Prod secret set never enumerated** | inferred from `secrets.md` (staging-focused) | Author a prod checklist: prod `JWT_SECRET`, `RESEND_API_KEY`, `EVIDENCE_FROM`, `IDENTITY_DENYLIST`, device root | Yes | **GAP** |

### Cloudflare / edge / DNS

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| Cloudflare **Workers Paid** plan | `DEPLOYMENT.md:33` | Activate (Queues + DO require it) | Yes | Assumed active staging |
| Provision D1×3 · KV · R2 · Queues×2 + paste ids | `DEPLOYMENT.md:34,16-21` | `wrangler d1/kv/r2/queues create` per env | Yes (per env) | Done staging; ~~**prod not provisioned**~~ **DONE prod 2026-07-30** — `provision:prod --apply`, 19 real ids across 5 configs, preflight PASS 72 |
| Remote D1 migrations (tenant 0001–0005, control 0001) | `DEPLOYMENT.md:35` | `wrangler d1 execute <db> --remote --file <mig>` | Yes (per env) | Done staging; ~~redo prod~~ **DONE prod** — 6 databases migrated (5 tenant planes @ 19 tables, control @ 5), verified by the restore drill's reads |
| Cloudflare **OIDC** creds (F1-A) | WP-01/WP-02 · REQ-117 · `secrets.md:6`; threat-model.md:20; dr-backups.md:7 | ~~Configure GitHub Actions OIDC ↔ Cloudflare~~ **CORRECTED 2026-08-11 (audit §960): the workflow does NOT use OIDC.** `nightly.yml:42-43` reads two REPOSITORY SECRETS — `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` — and no workflow requests an `id-token` permission anywhere. Measured: `gh api repos/shuddl/shuddl-os/actions/secrets` → **`{"total_count":0}`**. **Configuring OIDC alone would not unblock this**; set those two secrets (or convert the workflow to OIDC deliberately, which is a second decision). This is why the nightly `backup` job has exited 2 (`EVIDENCE_EXIT.PREREQ_BLOCKED`) every night since at least 2026-08-03; unblocks CI deploy + nightly snapshot | Yes (no DB backups / CI deploy without it) | **Deferred (F1-A)** |
| CF **edge rate-limit** on `/pub/*` (+ optional Turnstile on `/pub/quote`) | WP-09 · REQ-193 · spec 14§06 | Provision per-IP edge rule before public GA (explicitly **not** an in-Worker gate; KV counter is defense-in-depth only) | Yes (public GA) | **F0-DEPLOY-NOTE** |
| CORS allowlist real origins | WP-09 · REQ-025/189/167 · `workers/api/src/middleware/cors.ts:15-20` | Replace `portal.example`/`status.example` with real Portal + status deploy origins in `CORS_ALLOWED_ORIGINS` | Yes (Portal/status surfaces) | ~~Placeholder hosts only~~ **DONE** — all four real origins listed, and the live preflight now passes for them (`OPTIONS /v1/board` with `Origin: https://command.shuddl.tech` → 204 echoing that origin). The `.example` pair is deliberately kept: the screenshot/contract harnesses assert against it |
| `VITE_API_BASE` build var | `apps/command/src/lib/api.ts:15-19` | Set real per-env API host at build (unset ⇒ non-resolvable `.example`) | Yes (Command surface) | ~~Build config~~ **DONE + GATED (2026-07-31)** — `pnpm build:surfaces` bakes `https://api.shuddl.tech`, and `pnpm check:surfaces -- --built` refuses to deploy a bundle that lacks it. This was never a settings chore: a surface built without it renders its whole chrome against a host that cannot resolve, so every call fails as NETWORK rather than auth and the page shows empty panels with no warning — it looks deployed. Proven both ways before the deploy: unset ⇒ all three surfaces `FAIL api-base-not-baked`; set ⇒ PASS, and all three LIVE bundles were then re-checked over the wire |
| Self-hosted **Protomaps vectors** on R2 + offline SW cache | WP-03/WP-05 · REQ-075 · `packages/map/src/demo.ts:119@DEMO_TILE_URL`; `style.ts:9-34` | Host tiles on R2; swap `DEMO_TILE_URL`/style placeholders. ~~wire driver airplane-mode offline~~ **— that third sub-item is DONE and does not depend on the other two (audit §261).** The driver PWA's offline path is wired and tested end to end: an IndexedDB queue (`apps/driver/src/storage/idb-queue-store.ts`) behind `durable-seq.ts`, `sync/transport.ts`, and an app-shell service worker whose 6 tests include the SPA-fallback cache-poisoning cases (§253); proved through the REAL sequencer by `workers/api/test/airplane-soak.test.ts` — **55 signed offline events across two devices, `device_seq` monotonic from 0, synced under a seeded shuffle WITH duplicate re-sends, merging at zero loss and zero dupes with the chain verifying** (it is also demo 3's spine, §241). What remains here is the TILE work only, which is an ops act plus a one-line swap. **— and note WHY beyond deployment tidiness (audit §140): while the demo source is in use, every Command map viewport issues tile requests to a third party (`tiles.openfreemap.org`), so a dispatcher zooming to a delivery reveals that area of geographic interest to an outside service. No shipment data, PII or party identity leaves the system — the exposure is coarse viewport bounds correlated by IP and time — but it is a data-exposure property, not just an un-self-hosted dependency, and `CLAUDE.md`'s stack already mandates self-hosted Protomaps vectors. Self-hosting closes it automatically; recorded here so the reason survives if the deploy step is ever deferred on cost grounds.** cache | Yes (production-grade + driver offline; today renders off public demo host) | Deploy line item |
| Self-hosted **JetBrains-Mono glyph PBFs** on R2 | WP-03 · `style.ts` (`{PROVIDER_GLYPHS_URL}`) | Host on-map mono glyph PBFs; swap placeholder | No (cosmetic on-map; HTML chrome uses real fonts) | Deploy line item |
| Optional Mapbox path token | REQ-075 · `packages/map/src/style.ts:48`; `docs/security/threat-model.md:62@provenance` (content-anchored 2026-07-27 — `pnpm check:citations` now verifies the tile-provenance entry is still there, the one thing the line number alone could never say; corrected 2026-07-27: the old `threat-model.md:37` pointer was already stale and Task 5's insertions shifted it further — line 37 is now T14/T15 prose; the tile-provenance/no-third-party entry it means is the WP-03 change-log line 55) | Only if Mapbox chosen over self-host: `VITE_MAPBOX_TOKEN` + ToS confirm (conflicts REQ-075 no-third-party) | No (self-host is default) | **GAP — not in secrets inventory** |
| **`shuddl-backups-{env}` R2 bucket** | REQ-117 · `dr-backups.md:6` vs `DEPLOYMENT.md:34` | Provision backup bucket + add to provisioning/teardown lists | Yes (DR) | **GAP — inconsistent between the two docs** |
| Verified sending domain + **DKIM/SPF/DMARC** | WP-06 · REQ-092 · `secrets.md:24`; `DEPLOYMENT.md:48`; threat-model.md:19 | Publish 3 DNS records + `verify-domain`; wire bounce/suppression handling | Yes (any outbound / evidence email) | `send.shuddl.tech` verified staging; **prod sender NOT verified** |
| **Sending-domain 2-week warmup** (>98% deliverability) | WP-06 · REQ-157 · `secrets.md:24`; PROJECT-STATE.md:35 | Run seed-list ramp + bounce/complaint monitoring before tenant-0 consignee volume | Yes (prod email DoD) | **PILOT / deferred** |
| **Prod-sender domain divergence** (`send.shuddl.tech` vs `shuddl.tech`) | `DEPLOYMENT.md:48-49` vs `secrets.md:24`/PROJECT-STATE.md:35 | Resolve apex-vs-subdomain; pick + verify one prod sender | Yes (prod email) | **GAP — inconsistent across docs** |
| Per-shipment inbound address (`pro@tenant.shuddl.com`) + inbound webhook (svix verify + R2 body-resolver) | WP-06/WP-07 · REQ-091 · threat-model.md:19 | Provision addressing + inbox connectors; wire `email.received` → `message.received` (interim: bounded inline 2KB/32KB) | Yes (Concierge real email-in ingestion) | **Deferred** |
| Email suppression list | WP-06 · REQ-092/157 · threat-model.md:19 | Stand up + maintain outbound suppression list | Yes (prod email) | Listed mitigation; not evidenced |

### Engagement-workspace fixtures to vendor

All are **advisory-until-vendored**: harnesses loud-skip (exit 0), never false-green; a *partial* tariff vendor flips invoice+concierge harnesses to **hard-fail**. None block *merge*; each blocks its **WP DoD claim**. Verify with `pnpm check:fixtures` + `check:rater-parity`/`check:invoice-parity`/`check:concierge-parity`.

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **`zone-tariff-v1`** (keystone: zone tariff · 560-zip map · rate groups · accessorials · FSC · floors) | WP-04 · REQ-165 · `manifest.json:6`; `tools/rater/README.md:80` | Drop one JSON per rate_config kind into `fixtures/tariff/`; Zod-validate; flip manifest→vendored + sha256 | Yes — **gates WP-04, WP-06 & WP-07 DoDs simultaneously** | PENDING (dir absent) |
| `rater-48-tests` (audited engine's 48 cases) | WP-04 · REQ-027 · `manifest.json:4`; `parity.ts:202` | Load **exactly 48** ParityCase JSONs into `fixtures/rater/48-tests/`; pin sha256 | Yes (WP-04 REQ-027/165 DoD) | PENDING |
| `rater-504-sweep` (monotonic price sweep) | WP-04 · REQ-027 · `manifest.json:5`; `parity.ts:203` | Load **exactly 504** cases into `fixtures/rater/504-sweep/`; pin | Yes (WP-04 "no price on air") | PENDING |
| `invoice-500-replay` (penny-parity) | WP-06 · REQ-031 · `manifest.json:7`; `invoice-parity.ts:395` | Load **exactly 500** InvoiceReplayCase into `fixtures/invoice-replay/`; pin | Yes (WP-06 REQ-031 DoD) | PENDING |
| `concierge-parse-50` (50 real emails) | WP-07 · REQ-026/171 · `manifest.json:8`; `parse-parity.ts:408` | Load **exactly 50** ConciergeParityCase into `fixtures/concierge/parse-50/`; pin | Yes (WP-07 ≥90% parse + 100% floor-clean) | PENDING |
| `customer-roster` | WP-14/15 Migrator · `manifest.json:9` | Vendor roster into `fixtures/roster/`; pin (private manifest maps hash→origin, REQ-167) | Yes (Migrator DoD) | PENDING |
| `legacy-import-formats` | WP-15 · `manifest.json:10` | Vendor byte-exact formats into `fixtures/legacy-imports/`; pin (Migrator rule 10) | Yes (WP-15 overlay DoD) | PENDING |
| `legacy-export-replay` (9,314-bill / 4,405 re-rate, ±2%) | WP-02/04/15 · `manifest.json:11` | (a) **CONFIRM original export path at WP-01** (open); (b) vendor into `fixtures/legacy-export/`; pin | Yes (parity DoD) — **plus an open [CONFIRM] that blocks even vendoring** | PENDING + path unconfirmed |
| `synthetic-blitz-3100` | WP-15 shadow tooling · `manifest.json:12` | Vendor into `fixtures/blitz/`; pin | Yes (WP-15 shadow-run DoD) | PENDING |
| **Live legacy-TMS mirror feed** (not a byte fixture) | WP-15 · REQ-152 · `fixtures/README.md:17` (README-only) | Secure tenant standing data-access grant + wire nightly export/API mirror | Yes (WP-15 / Phase-0 clock) | PENDING — **not in `manifest.json`, easy to miss** |

### Per-tenant / per-partner activation

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **Every bound tenant needs a `tenants` control row before it can append** | 2026-08-02 §18/§27 · REQ-030/025/180 · `workers/api/src/do/sequencer.ts` | Create the control-plane `tenants` row for EVERY tenant a worker binds, in every environment. The sequencer REFUSES every append for a tenant whose row is MISSING or whose `policy` is unusable — deliberately: proceeding on `{}` drops `gates.dims_required`, widens `geofence_radius_m` to the 150m default, and drops every narrowing visibility override onto events that are stamped at append time and **immutable**, so that last one cannot be undone by fixing the row afterwards. No migration creates a static tenant's row (`0001` makes the table, `0002` inserts `_platform`, `0003` the pool sentinels) — it is hand-provisioned (`tools/deploy/staging-smoke.ts`), therefore hand-deletable. The guard found a real instance the day it shipped: `tenant-b` was a fully bound tenant with **no control row at all**, appending with silently-widened visibility for as long as the fixture had existed. **Policy authoring note (§27):** an empty YAML key serialises to `null`, and tenant-0's policy is generated from a config pack outside this repo — `null` is accepted as *unset*, but a visibility value must be exactly `internal`/`counterparty`/`public`; a typo is refused at the boundary rather than 500ing mid-append. | **Yes** (any tenant that appends) | **ENFORCED IN CODE** — refusal is server-side; the operational half is provisioning the rows |
| **Tenant-0 seed load** (roster · tariff · zips · rate groups · accessorials · FSC · pro ranges · transit) | WP-04 · REQ-165 · Doc13§02.4 | Load config pack; must reproduce audited engine quotes **exactly** | Yes (tenant-0) | F0.2-SPEC'D |
| **Live EDI transport adapter + secret store** | WP-12 · REQ-034/205 · `workers/translator/src/index.ts:20-41`; `wrangler.toml:20` | Set `EDI_TRANSPORT_URL`+`EDI_TRANSPORT_TOKEN` (secret) + a per-partner HMAC secret store; **build** live VAN/AS2/SFTP adapter. Today `NotConfiguredTransport`+`NotConfiguredSecretResolver` fail-closed (every live 204→401, nothing transmitted). ~~**Latent at the flip (2026-08-01 audit):** outbound ISA09/ISA10/GS04/GS05 are hard-coded to `000101`/`0000` … stamp real dates at send BEFORE activation~~ **CLOSED same day (commit `3899ae6`):** the 214 sweep + 990 ack stamp the real send instant through an injectable clock, at the same step that stamps control numbers; fixtures stay byte-stable. ~~**Second pre-flip line (2026-08-01 review):** the TRANSLATOR worker still enumerates only the static tenant roster and binds no pool D1s…~~ **CLOSED 2026-08-01, commit `805dbb8`** — the agents pattern is ported verbatim (static hot path → claimed-pool fallback over the parity-pinned allowlist, exclusivity fail-closed, enumeration fault degrading to the static roster with a loud log), the 214 sweep fans out over `allTenantSlugs`, the inbound-204 resolver is claimed-aware, pool D1s are bound in all three scopes with api-converged ids, and the preflight requires them. Nine tests pin it; ~~translator 88 PASS~~ **translator 10 files / 91 PASS (2026-08-02, after the §13 glob widening)**. ~~**No roster instance of this class remains anywhere in the repo**~~ **CORRECTED 2026-08-01 §12 — that claim was FALSE when written:** the BILLING worker's metering sweep still iterated the static roster (an unmetered claimed tenant is unbilled usage the day PLG flips — the row at §3 had ledgered it Low/DARK, and the absolute claim above wrongly overrode it). Billing is ported in the same session; the class is now closed in api, agents, translator AND billing, and the claim is stated per-worker rather than absolutely | Yes (all live EDI) | CONFIRM-gated, **adapter unbuilt** |
| **Per-partner LIVE EDI activation + cert** | WP-12 · REQ-203 · Doc13§01 · `workers/translator/src/tenants.ts:19` | Each partner = an `integrations` row; `cert_status→certified` only after that partner's replay round-trips clean; LIVE activation is a config-pack calendar object (genesis/13, outside repo) | Yes (per partner) | F0-SPEC'D (activation outside repo) |
| Pro-number ranges / legacy continuity config | WP-15 · REQ-058 | Per-tenant config preserving legacy pros verbatim | No (per-tenant config) | F0-SPEC'D |
| Hazmat workspace enablement | WP-14 · REQ-060 | Per-tenant workspace flag (excluded from Spark default) | No | F0-SPEC'D |
| Vanity multi-host per tenant (`tenants.host`) | WP-09 · genesis/13 | Onboarding provisions host; WP-09 uses static `HOST_TENANTS` allowlist | No | Deferred (onboarding) |
| PROOF-TO-CASH SKU provisioning | WP-14 · REQ-162 · Doc12§05 | Make standalone SKU provisionable via plan flag (excludes non-SKU agents) | No | F0.2-SPEC'D |

### Milestone / CONFIRM gates

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **M-H heartbeat unlocks GTM** | WP-06 · REQ-159 · Doc12§01 | Milestone decision at M-H exit; M-H demo video must exist before first outbound. **Never a code flag.** External auto-send of a firm quote also gated here | Yes (all GTM / demo #2) | Milestone gate |
| Tenant-0 legacy-TMS feed 3-day unattended mirror (Phase-0 exit) | WP-15 · REQ-152 · Doc13§02.6 | Nightly mirror must run unattended 3 days | Yes (tenant-0 onboarding) | F0.2-SPEC'D |
| Tenant **authority-flip** gate | WP-15 · REQ-153 · Doc13§04 | 30-day shadow ±2% · two consecutive clean closes · pilot week <0.5% exceptions · auto-fallback armed | Yes (authority cutover) | F0.2-SPEC'D |
| Per-module authority parity gates | WP-15 · REQ-023 | Flip blocked until ±2%/±10%/2-closes green | Yes (per module) | F0-SPEC'D |
| Overlay-before-authority fallback | WP-15 · REQ-008 · Doc00 L8 | Per-module flags + auto-fallback; forced-drift test triggers fallback | Yes (authority overlay) | F0-SPEC'D |
| **Pen-test clean** before launch | WP-16 · REQ-136 · 08§03 | Run pen-test; report clean | Yes (launch gate) | F0-SPEC'D |
| Pricing re-based on tenant-0 telemetry | WP-16 · REQ-130 · Doc04§5 | Re-base pricing + update doc 04 before public launch | Yes (public pricing) | **CONFIRM-GATED** |
| Naming/trademark clearance (SHUDDL) | F1 · REQ-141 · CONFIRM-1 | Clearance memo — **and note the BLAST RADIUS of the other answer (audit §166): the name is in 538 tracked files / 2,349 occurrences (420 code, 81 markdown, 18 `package.json`, 19 config), including the `@shuddl/*` npm scope every internal import resolves through. Outside the repo it is also the worker names (`shuddl-api-<env>`, `shuddl-agent-dlq-*`), the backups bucket (`shuddl-backups-{env}`) and the `shuddl.tech` zone. A clearance FAILURE is therefore not a memo but a repo-wide rename plus a scope migration, worker/bucket recreation and a domain change — and the cost grows every commit, which is an argument for sequencing this FIRST among the CONFIRM-1 items rather than at brand launch.** | Yes (public brand launch) | **CONFIRM-GATED** |
| ToS / Privacy / DPA for PLG signup | WP-14 · REQ-138 | Counsel sign-off | Yes (PLG signup GA) | **CONFIRM-GATED** |
| **Photo/PII retention policy + consignee notice** | WP-06 · REQ-140 · CONFIRM-2 · GA-11 | Counsel: what's kept, how long, notice wording (`lifecycle_class`/`visibility` knobs exist) **— and note WHICH ANSWERS THE EXISTING KNOBS CAN SATISFY (audit §165). Retention ("delete the photos after N years") and confidentiality ("counterparties never see PII") are both configurable today: `retention.ts` deletes the R2 bytes and tombstones the documents row, and `redact.ts` strips fields at read time. Neither touches the EVENT rows, which carry party refs and payload data and are immutable by I3 (no UPDATE/DELETE path exists, by design and by DB trigger). So if counsel's answer requires SUBJECT-LEVEL ERASURE from the ledger itself, that is not a config change — it is an architectural decision (payload encryption with per-subject keys / crypto-shredding, or a documented legal position that these records are retained on legitimate-interest or legal-obligation grounds). Recorded so the CONFIRM is scoped honestly: one class of answer is a policy publication, another is a design change, and the row previously read as though only the first were possible.** | Yes (delivery-evidence data handling) | **CONFIRM-2 / counsel** |
| eBOL/e-signature validity per mode + driver location-consent text | WP-05 · REQ-142/166 · CONFIRM-2 | Counsel notes; consent-capture mechanism already built | Yes (signature-as-evidence + consent) | **CONFIRM-GATED / counsel** |
| **Real TSA (RFC-3161) endpoint** | WP-02/WP-16 · REQ-014 · `workers/api/src/routes/anchors.ts:19`; `docs/security/threat-model.md:31` (corrected 2026-07-27 from `:29`, which now lands on the seq-race row) | Insert `integrations` row `kind='tsa'` w/ `config.url` per prod tenant + clear F1 CONFIRM (`HttpTsaClient` fake-tested only; missing ⇒ day left UNANCHORED, never faked in prod) | Yes (evidentiary anchor spine) | Prod unconfigured; F1 CONFIRM open |
| 2023 repos archived read-only | F1 · REQ-144 · Doc06 | Archive prior repos read-only with pointer | No | F0-SPEC'D (F1) |
| Real-driver pilot: install (QR+magic-link+card) + zero-instruction gated stop, on video | WP-05 · REQ-164/006 L6 | Real tenant-0 driver completes gated stop unassisted | Yes (acceptance demo #3) | PILOT |
| 10-min CSR acceptance with a non-freight tester | WP-10 · L6 | Observed human run (never claimed from a test run) | Yes (acceptance demo, L6) | PILOT |
| Stranger signup → quote <10 min | WP-07 DoD | ~~Depends on WP-14 signup + live parse~~ **Bind the R4 flag/secret set, then the observed human run** | Yes (acceptance demo #2) | ~~Depends WP-14 + pilot~~ **WP-14 SHIPPED but DARK — corrected 2026-08-11 (audit §946).** Its own close-out states the posture: *"the entire PLG surface ships DARK / fail-closed — nothing … opens signup until an operator binds its flag/secret/keys at R4."* The dependency is real but is no longer the BUILD: measured (audit §944/§945) `PROVISIONING_ENABLED` appears in no wrangler config and `ANTHROPIC_API_KEY` binds nowhere. Blocker = the R4 binding + pilot |
| MCP / command-bar booking places a gated append | WP-08→WP-13 · demo #4 | ~~Build MCP intake surface (WP-13)~~ **BUILT — film the real Claude-via-MCP booking against the deployed DO-backed api (the staging smoke)** | Yes (acceptance demo #4) | ~~Deferred WP-13~~ **WP-13 SHIPPED — corrected 2026-08-11 (audit §946).** `docs/wp/WP-13.md` reads *"complete, merged to main locally"*; demo 4's spine `@shuddl/mcp test/quote-book.test.ts` is registered in `tools/acceptance/demos.ts` and runs **green inside `pnpm test:acceptance`**, the merge-board gate. Remaining: the FILMED half only |
| Outdoor readability field test | WP-05 · REQ-067 | Legibility under direct sun on real hardware | No | PILOT |
| Battery/data budget field measure (<5%/day GPS @30s) | WP-05 · REQ-070 | Field measurement (emitter itself is a follow-up, see §3) | No | PILOT |
| **vNEXT CONFIRM-gated OUT features** (do-not-build until CONFIRM): settle/escrow instant-settle (REQ-033/143), voice recording + per-state consent (REQ-096/137), MCP Direct-merchant (REQ-104) + credit-line guest (REQ-103), broker authority+insurance (REQ-139) | genesis/09 rows 34/97/104/105/138/140/144 | Counsel/owner sign-off before *any* build; keep unbuilt while CONFIRM open | Blocks those features (not v1 go-live) | **CONFIRM-GATED** |
| Process rituals: threat-model reviewed each WP (REQ-131) · weekly register review (REQ-120) · identity-denylist upkeep (REQ-167) · ICP+first-25 CRM (REQ-161) | WP-01/ongoing/WP-16 | Standing calendars + logs | No (process / post-M-H GTM) | F0(.2)-SPEC'D |

### Monitoring / DR

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **Nightly ledger + control snapshots to R2** | WP-01 · REQ-117 · `dr-backups.md:6` | Activate `.github/workflows/nightly.yml` once F1-A OIDC + WP-02 DBs exist. ~~(workflow is a **stub**)~~ **corrected 2026-08-04 (audit §189): the workflow is complete, scheduled and tested — nothing needs writing. Supplying the credential is the whole remaining step, plus the separate operator decision to schedule PRODUCTION (the job is staging-only by design).** | Yes (no backups = data-loss risk) | Not active |
| DR multi-region backups (RPO 24h / RTO 4h) | WP-01 · REQ-135 · GA-19 | Provision multi-region backups | Yes (prod) | F0-SPEC'D |
| Quarterly restore drill | REQ-135 · `dr-backups.md:12` | Run restore→chain-verify→row-parity; log it | No to deploy, **Yes to trust DR** | ~~**Never run** (drill log empty)~~ **RUN 2026-07-31** on staging tenant-a (prod's ledger is empty — 0 events — so a prod drill would reconcile nothing): `restore-verify` ~~**PASS**, 11 checks~~ — **that first run had no `--rows`, so it counted two chain dimensions it never exercised; at HEAD the same command reports 9 and BLOCKED.** With `--rows`: **PASS, 11 checks, 0 problems**; 40 events / 5 invoices / 10 money lines / 17 anchor days identical. The `--rows` chain re-walk FAILED on the first run — a checker defect, not data loss (single-chain walk vs per-stream ledger) — and was **FIXED the same day**: `verifyChainOfRows` now walks per stream, the re-run is **PASS** with all 40 events re-derived, and the three negative controls (dropped row, flipped `hash`, flipped `prev_hash`) each fail naming their stream. Log + diagnosis + fix: `dr-backups.md:86` |
| Uptime/SLO monitors per surface | WP-01 · REQ-114 · `slo.md` | Stand up live monitors on first deploy (F1-A account) | No to deploy, Yes to operate safely | Targets defined; **not stood up** |
| Watchtower budget-drift alarms | WP-11 · REQ-113 · `slo.md:11` | Wire drift alarms | No | Delivered WP-11 (Watchtower) |
| R2 lifecycle policies per doc kind (7-yr POD) | WP-11 · REQ-116 · GA-16 | Apply lifecycle rules; add storage cost line | No (retention compliance) | F0-SPEC'D |
| DLQ drain / poison-message runbook | `DEPLOYMENT.md:21` (`shuddl-agent-dlq-staging` exists) | Define who watches DLQ + replay procedure | No to deploy, Yes to operate | **GAP — DLQ provisioned, no procedure** |
| Public status-page hosting | `slo.md:10` | Stand up public status pages (server-side geo boundary must hold first — see §3) | No | **GAP — SLO'd, no deploy steps** |
| Weekly Watchtower telemetry series (case study) | WP-11 · REQ-160 · Doc12§06 | Weekly snapshot series (unbilled/DSO/POD→invoice latency) from Phase-1 wk 1 | No (GTM proof) | F0.2-SPEC'D |
| GitHub Actions activation (F1-A) ~~+ 5 blessed Playwright screenshots + measured map perf~~ — **the two TECHNICAL halves are DONE; only the ops act remains (audit §260)** | WP-01/03/09/10 · REQ-158/079 | Activate workflows. ~~generate/bless 5 canonical screenshots + perf numbers on browser-capable CI~~ — **both delivered and re-verified 2026-08-05**: all five blessed PNGs are committed and `test:visual` passes 5/5 against them (§253/§256), and `perf:map` exits 0 with real numbers — **1000 entities, frames=401, p50 10.00ms, p95 11.30ms (~88fps) against an 18.18ms / 55fps budget**; cold-boot long tasks 1, worst 59.00ms (reported, not budgeted). `test:a11y` 4/4 and `test:e2e` 6/6 also run here. The bundling made three items read as one pending block; the browser-capable precondition is met on any machine with Chromium, and the design CI no longer self-skips — it is blocking (§258) | No (gates also run via `pnpm verify`) | **Ops act only: activate the workflows.** `.github/workflows/{ci,nightly}.yml` exist; `nightly.yml` carries `cron: "0 8 * * *"` and runs the REQ-118 orphan diff |

---

### WP-13 MCP v1 (appended 2026-07-20 per §1)

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **`workers/mcp` worker deploy** | WP-13 · REQ-101 · `workers/mcp/wrangler.toml` | Provision the 4th worker + its bindings per env: `API` service binding → `shuddl-api-{env}`, `CONTROL_DB`, `JWT_SECRET`, `GRANTS` KV, `CAPS_METER` Durable Object | Yes (the whole MCP surface) | ~~Not deployed~~ **DEPLOYED prod 2026-07-31** — `mcp.shuddl.tech` answers 200; staging scope still undeployed |
| **Live OAuth secret store + mcp pairing client secrets** | WP-13 · REQ-102/154 · `workers/mcp/src/{oauth,principal,secret-resolver}.ts` | Bind the pairing-secret store; `NotConfiguredSecretResolver` 401s every `/token` + `/register` until then (fail-closed) | Yes (all live MCP auth) | CONFIRM-gated flip |
| **Per-pairing caps provisioning** | WP-13 · REQ-105 · `pairings.caps` · `workers/mcp/src/caps.ts` | Set `caps={spend,velocity,lanes}` on every `kind='mcp'` pairing; the no-caps default is a fail-closed **refuse**, so an unprovisioned pairing cannot `book_shipment` (deliberate hard cutover) | Yes (per pairing) | Fail-closed until provisioned |
| **Webhook delivery activation** | WP-13 · REQ-109 · `workers/mcp/src/webhooks.ts` | Wire the live event-source (today `NotConfiguredEventSource` yields `[]` → inert); set the per-subscription webhook signing secret; receiver-side enforce a timestamp freshness window + restrict delivery URLs to `https://` | Only webhook delivery | Scaffolded / fail-closed |

### WP-14 PLG + metering (appended 2026-07-21 per §1) — all DARK until R4

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **REQ-138 legal (ToS / Privacy / DPA)** | WP-14 · REQ-138 · genesis/09 | Counsel-authored ToS/Privacy/DPA — the door-opener; **public signup legally cannot open** without sign-off. Do-not-build (CONFIRM-2) | Yes (all public self-serve) | CONFIRM-GATED |
| **Stripe keys + `STRIPE_WEBHOOK_SECRET`** | WP-14 · REQ-123/154 · `workers/billing/src/billing.ts` | Bind operator-injected (never toml); `NotConfiguredBilling` rejects until then | Yes (all billing) | DARK / fail-closed |
| **`PROVISIONING_ENABLED` flag** | WP-14 · REQ-121 · `workers/api/src/provision.ts` | Flip ON to open self-serve signup/provisioning; default OFF → `/pub/signup` 404s | Yes (self-serve) | DARK / OFF |
| **`PLATFORM_INTERNAL_SECRET`** | WP-14 · REQ-123/154 · ~~`workers/api/src/internal-platform.ts`~~ **corrected 2026-07-27: `workers/api/src/routes/internal-platform.ts`** (the old path does not exist — `ls` returns `No such file or directory`) | Bind the credit-append/settle route secret (never toml); unbound → 503 | Yes (credit emission) | DARK / 503 |
| **Tenant-D1 pool provisioning** | WP-14 · REQ-121 · `workers/api/wrangler.toml` (`TENANT_POOL_0N_DB`) | Ops pre-creates + migrates the pool D1s the provisioner claims; a pool-refill runbook | Yes (per-signup capacity) | Local pool only |
| **Spark $5 tier plan-flag** | WP-14 · REQ-122/124 · `tenants.plan` | Provision the Spark plan on a tenant to activate the tier + caps; numbers `[HYPOTHESIS]` (REQ-130) | Gates the Spark tier | Plan-flag, unset |
| **REQ-125 per-IP edge rate-limit** | WP-14 · REQ-125 | A Cloudflare per-IP edge rule on `/pub/signup` (like REQ-193) before public GA (the in-Worker `SparkMeter` is per-workspace only) | Yes (public GA) | Deploy note |
| **Checkout `metadata.tenant` is the credit's only tenant binding, and nothing in this repo sets it** | `workers/billing/src/credits.ts:184@metadata` · REQ-123/154 | The credit sale resolves its tenant from the Stripe Checkout Session's `metadata.tenant`. Stripe's signature covers it and Zod requires it non-empty — neither establishes that it names the RIGHT tenant. **No code in this repository creates a Checkout Session** (the only `checkout.session` references are consumers), so the value is written by a surface that does not exist yet. Found by tracing every client-suppliable field to its furthest consumer (audit §403); this one's furthest consumer is a credit row on a customer's account | **A requirement on unbuilt work, recorded at the consuming line:** whoever builds checkout creation must stamp `metadata.tenant` from a SERVER-KNOWN slug (the authenticated session's tenant, exactly as `routes/events.ts` stamps `override.by` from `session.sub`) and never from a client parameter. If it comes from a pricing-page query string or form field, a buyer credits any tenant they can name and every downstream check still passes — signature verifies, Zod parses, credit lands on the wrong account | **Low today (surface unbuilt), Med the day it ships** |
| **Async/ACH checkout confirmation** | WP-14 · REQ-123 · `workers/billing/src/credits.ts` | Confirm the (landed) unpaid-branch settle reconciles a covered payment in staging before enabling async/ACH credit checkout | Only async checkout | Landed; verify in staging |

### WP-15 Overlay/authority (appended 2026-07-22 per §1) — DARK/inert until Phase-0 cutover

The overlay machinery is built + fixture-proven; nothing mirrors, flips, or falls back until an operator wires a live feed and the tenant-calendar gates go green. The M-AUTHORITY flip gates are **calendar objects that compress for no one** (genesis/13 §04) — a merge cannot close them.

| Item | Source WP/REQ | Action to complete | Blocks go-live? | Status |
|---|---|---|---|---|
| **Live legacy-feed provisioning** | WP-15 · REQ-152 · `workers/agents/src/mirror-sweep.ts` (`NotConfiguredFeedReader`) + `integrations.config` | Wire the tenant-0 legacy-TMS export/API feed into `integrations.config`; the sweep no-ops until then | Yes (the whole mirror) | DARK / no-op |
| **Tenant-0 config pack** | WP-15 · REQ-058/152/153 · engagement workspace (genesis/13) | The 171 literal column headers + field mapping, the pro-ranges + continuity (REQ-058), the flip/close calendar dates (REQ-153). Generic/config-driven in-repo; literal values are tenant-pack (no identity in-repo, REQ-167) | Yes (a real mirror + flips) | Tenant-pack, unbuilt |
| **M-AUTHORITY calendar gates** | WP-15 · REQ-153 · genesis/13 §04 | 30-day shadow ±2% · two consecutive clean closes · pilot-week <0.5% exceptions — calendar objects; flips gate on the milestone (M-H mirror / M-AUTHORITY flips), NEVER a week number. Non-compressible | Yes (per-module flips) | Calendar-gated |
| **Clean-close signal** | WP-15 · REQ-023 · `workers/api/src/routes/authority.ts` (`cleanCloseCount`) | No in-repo period-close representation → money-module (invoicing/settlement) forward flips are BLOCKED-BY-CONSTRUCTION until a real close signal is wired | Yes (money authority) | Blocked-by-construction |
| **Defer-to-mirror suppression** | WP-15 · REQ-030 · the 8 `resolveAuthority` consult sites (dormant `if(authority==='legacy')` intent-markers) | Light up per-module at cutover: a legacy-authoritative module with a live mirror value presents the legacy value / suppresses the native customer-facing output. Behavior-neutral (dormant) in-repo | Gates a real cutover | Dormant seam |
| **`cursorColumn` monotonic-on-change** | WP-15 · REQ-035 · `packages/adapters/src/legacy-mirror.ts:69` | Verify the tenant-pack feed `cursorColumn` bumps on every in-place edit (a last-modified/version cursor, not creation-only) — else continuous no-silent-drop degrades silently | Gates a correct mirror | Tenant-pack precondition |

## 3. Technical debt & known limitations

Ordered severity-descending. **High** = weakens/blocks a gate or a go-live path; **Med** = correctness/privacy residual or env hazard; **Low** = deferred refinement, fail-safe, or informational.

| Item | Source file / REQ | Nature | Fix | Severity |
|---|---|---|---|---|
| ~~**Identity-leak lint fails OPEN**~~ **REQ-167 unverified in local + WP-exit runs** (title corrected 2026-08-04, audit §197 — it contradicted this row's own superseded body and its `~~High~~ → Med` grade; the gate fails CLOSED in CI) | `tools/checks/identity-leak.ts:35-61` · REQ-167 | ~~The one genuine fail-open gate: `return`s with a warning when `IDENTITY_DENYLIST` absent — a real tenant/person name could ship~~ **Superseded 2026-07-27 (T14/T15 + Task 6 re-verify):** the gate now fails **CLOSED** in CI and under `--mode merge/release`; the fail-open branch survives only in a bare local run (`identity-leak.ts:62-69`). The residual is narrower and real: **REQ-167 is unverified in every local run and at every WP-exit run on this machine** | Wire the CI secret / `.identity-denylist.local`; fail closed | ~~**High**~~ → **Med** (CI is closed; local is unproven). Eight-field row: see the failures ledger at the bottom |
| **Ratecon generation flow unbuilt → dispatch fail-closed** | ~~`workers/api/src/do/sequencer.ts:700-708`~~ **corrected 2026-07-27: `workers/api/src/do/sequencer.ts:937-940@ratecon`** — `:700-708` is the `booking.created` gate, not this one. (Content-anchored 2026-07-27: `@ratecon` is checked by `pnpm check:citations`, so this pointer now fails a gate if the marker moves again rather than silently rotting.) Verified: `grep -n -i ratecon workers/api/src/do/sequencer.ts` → `926`, `929` only; `grep -n "REQ-184" …` → `928`, `929`. The same corrected citation is used at `:368` and in Q1 · REQ-184/043 | Nothing writes a `documents` row kind `ratecon`; REQ-043 dispatch gate cannot pass without a REQ-049 override (correctly blocked, not falsely open) | Build ratecon generation (booking/dispatch step) | **High** (no real dispatch until built) — register **vNEXT** |
| ~~**genesis/07 states a map state the register never authorised**~~ **CORRECTED by §855 — REQ-076 DOES authorise it** *(filed 2026-08-09, audit §854; corrected §855)* | `genesis/07-DESIGN-SYSTEM.md:46` (*Delivered (last 24h) — hollow red outline at 55%, fades from the map after 24h*) · `packages/map/src/entities.ts:141@restHealthy` · no REQ row | `delivered` exists as an `EntityKind` and the demo generator emits it, but the leaf layers filter on **`statusStr`, not `kind`**, so a delivered entity draws at `restPaint()`'s 0.9 with a 1px stroke — no hollow-only treatment, no 55%, no 24h fade. **The map is CORRECT to omit it**: CLAUDE.md ranks `genesis/09` first (*if it isn't a REQ row, it doesn't get built*) and `genesis/07` third, and no register row covers this state | **OWNER DECISION** — either (a) add a REQ row and build it (a new layer keyed on `kind`, since the leaves key on status), or (b) strike the row from genesis/07 so the pixel law matches scope. Same shape as `routes ±10%` (§842): a law named with nothing behind it. Filed, not amended — §795's precedent while REQ-289 is uncommitted | **Low** — a spec/scope mismatch, not a defect; nothing renders wrongly, but a reader of *"every pixel"* expects a state the map will never show |
| **REQ-077's "Visual test" DoD is met by a UNIT test, not a screenshot** *(filed 2026-08-09, audit §854)* | `genesis/09` REQ-077 · `packages/map/test/entities.test.ts:223@setWorldDim` · `tools/acceptance/demos.ts:119` | The five blessed screenshots are command, driver, portal, status and evidence-email — **none captures the world-dim**. What proves REQ-077 is the `setWorldDim` unit suite (which is careful: it names the both-branches-match trap and asserts the fallback). The acceptance manifest already records the gap — demo #5's `browser` field is `null`, *"deferred: the real-browser world-dim on/off spec … the documented next in-repo increment"* | Nothing to fix; recorded because *"Visual test"* in a register row reads like a screenshot exists. The manifest's deferral is the accurate record | **Low** — already tracked in the acceptance manifest |
| **REQ-076 names four shapes; the map draws two — and its DoD gate cannot see geometry** *(filed 2026-08-09, audit §855)* | `genesis/09` REQ-076 (*Entity grammar: chevron/square/hollow/pulse per state* · DoD `Squint CI` · **F0-SPEC'D**) · `packages/map/src/entities.ts:118@restPaint` · `genesis/07-DESIGN-SYSTEM.md:72` (A4) | **chevron** and **pulse** render. **square** and **hollow** do not: every leaf — at-rest, facility, delivered — draws as `type: "circle"` with a filled `circle-color` and a 1px stroke. The only `addImage` in the repo is the chevron; there is no square icon, no SDF, and no recorded decision explaining a move to circles. **And the DoD cannot see it**: the squint CI audits contrast, the five tokens, motion, dividers and case — **no geometry check exists**, so REQ-076's gate is green regardless of shape. A4 states why this is not decoration: *"Status is grammar, not color: states are expressed by geometry"* — with every leaf a filled circle, geometry carries no state | **OWNER DECISION**, three options: (a) build the shapes (a new leaf layer or icon per kind — note the leaves currently filter on `statusStr`, not `kind`) and re-bless the screenshots; (b) ratify circles and amend BOTH genesis/07 §02 and REQ-076, since both name the vocabulary; (c) leave the pixels and add the missing geometry assertion to the squint CI so the DoD can observe its subject. Filed, not fixed — the row is F0-SPEC'D, so a silent code change would leave the record claiming something other than what happened | **Med** — no user harm (marks render, status reads via opacity/colour/dim), but an `F0-SPEC'D` row is partly unbuilt and its gate is structurally unable to notice |
| **I4's `unwitnessed` waiver has no downstream reader** *(filed 2026-08-09, audit §851)* | `packages/contracts/src/events.ts:376@unwitnessed` (the I4 refine) · REQ-011/016 · demo #1 | I4 requires a custody event (`pod.signed`, `custody.transferred`) to be co-signed by a device OR carry `payload.unwitnessed: true`. The waiver is explicit and recorded, which is right. But it is read in exactly ONE place — the refine that waives the requirement — and **nothing downstream reads it**: not the Biller, not the evidence email, not the invoice gate (zero hits across `workers/agents` + `packages/agents`). An unwitnessed POD therefore mints an invoice and sends an evidence email **identically** to a co-signed one, on the path acceptance demo #1 describes as *"signature at a door"* | **OWNER DECISION.** Either (a) accept it — an unwitnessed POD is business-as-usual (paper POD, dead phone) and the ledger's record of `unwitnessed` is sufficient audit; or (b) give it a reader — the Biller declines to auto-send, or the email words the claim differently, or it takes a distinct money treatment. This is a product call, not an audit one; §851 records the state, not the verdict | **Med** — no forgery is possible (the sequencer verifies every device signature, fail-closed) and the waiver is always recorded on the event; the exposure is that a downstream reader treats two different things as one |
| **EDI B2A purpose codes are parsed and then IGNORED — including 01 = CANCELLATION** *(widened 2026-08-09, audit §795: this row previously named only 04/05 and described the outcome as a visible duplicate; that under-stated it)* | `packages/edi/src/parse-204.ts:117@B2A` sets `doc.purpose`; **zero consumers in `workers/translator/src`** (measured) · `map-204.ts:70@B2A` · REQ-205 | Two distinct outcomes. **(a) 04/05 revision/replace** → a PO-only re-tender that later gains a primary id yields a **visible duplicate** shipment (dup preferred over silent merge, rule 10) — this is REQ-205 and is deliberate. **(b) 01 CANCELLATION** → the tender is booked like an original: **200, the full gated chain, one shipment, and ZERO anomalies** (measured end-to-end, §795). In production the committed `quote.accepted` enqueues the Booking agent, so SHUDDL commits freight the partner explicitly cancelled, with no signal anywhere. **(b) is NOT covered by REQ-205**, whose row is scoped to the 04/05 re-tender case | (a) Wire X12 B2A 04/05 same-load convergence before EDI go-live (REQ-205). **(b) needs a REQ row of its own — proposed in §795, NOT written into the register while REQ-289 is uncommitted.** Fail-closed reading: a cancellation should QUARANTINE (the handler's existing never-a-silent-drop branch), never book | **High** — (a) fail-closed today; **(b) is fail-OPEN and is the reason this row is widened**. Both are inert until the CONFIRM-gated EDI transport is wired, so this gates the GO-LIVE, not the merge. Tripwire: `workers/translator/test/inbound.test.ts` "§795 GAP" reds the day `purpose` gains a consumer — **F0-DEPLOY-NOTE** |
| **EDI transport + inbound-204 HMAC resolver unwired** | `workers/translator/src/index.ts:20-41`; `workers/translator/src/inbound.ts:50-59@NotConfigured ` · REQ-034/154 | `NotConfigured*` fail-closed: every live 204→401, no EDI transmitted (also an operator item, §2) | Build live adapter + bind secret store | **High** (for EDI) — deferred |
| ~~**Two of the seven hard budgets are not CI-enforced**~~ **CLOSED 2026-08-05 (audit §205)** — `packages/design/test/tokens.test.tsx` now asserts `Object.keys(TOKENS)` is 5 and `FONTS` is 2; the §203 mutation (6th token + 3rd family) that passed every gate now fails both, and 6 of 7 budgets were already enforced (§204 corrected §203's five) | `packages/design/src/tokens.ts@TOKENS` · `CLAUDE.md:15` | `CLAUDE.md` lists ≤22 tables · 35 event kinds · 12 views · **5 colour tokens · 2 font families** · 0 shadows under *"Hard budgets (CI-enforced; exceeding = the PR is wrong)"*. Planting a 6th token and a 3rd font: `audit:design` exit 0, design suite 9 pass, `design.test.ts` 49 pass — **nothing counts the palette**. What IS enforced is the stronger everyday rule (no raw hex outside `tokens.ts`; everything reaches colour via `var(--token)`/`TOKENS`), which stops ad-hoc colour but not palette GROWTH. Tables (`I8 VIOLATION: 23 > 22`) and event kinds (typecheck exit 2 + `EVENT_KINDS.length is still exactly 35`) ARE enforced | Add a palette-size assertion beside the existing token tests, or amend `CLAUDE.md` to say what is actually gated | **Low** — no defect today (palette is 5/2); the claim of enforcement is what is wrong |
| **~~Six~~ SEVEN read paths full-SCAN their table — no supporting index** (audit §185, extended §213: the glob of §211 had hidden `rate_config` entirely) | `db/tenant/migrations/*.sql` · `legs` (driver manifest, 5 queries) · `invoices(party_id)` · `documents(shipment_id)` · `anomalies(status)` · `approvals(status)` · `messages(drafted_by_agent,direction)` | Confirmed by `EXPLAIN QUERY PLAN` against the real schema, not inferred: each returns `SCAN`, while the sequencer control returns `SEARCH events USING INDEX (stream_id=?)`. `invoices(party_id)` compounds the unbounded read above — the portal list is both uncapped AND scanned | One additive migration: `legs(shipment_id)`, `invoices(party_id)`, `documents(shipment_id)`, `anomalies(status)`, `approvals(status)`, `messages(drafted_by_agent, direction)`, **and `rate_config(kind, effective_ts)` — the hot one: `EXPLAIN` gives `SCAN rate_config` + `USE TEMP B-TREE FOR ORDER BY` on the lookup THREE workers run for every pricing call (`workers/api/src/rate-config.ts:28@effective_ts`)**. Validate against real cardinality before choosing composite vs single | **Med** — invisible at fixture volume; the driver manifest scan runs on every app open |
| ~~**TWO auth lifetimes are undeclared values nothing pins**~~ **PINNED 2026-08-06 (audit §469)** — both are now asserted on the OBSERVABLE `exp` a bearer actually holds, plus a domain bound that survives someone editing the literal (cap < 90 days, session < 24h). Mutations: ×12 on the cap and ×30 on the session each give 1 failed. | `workers/api/src/routes/status-link.ts@CAP_TTL_SECONDS` (30 days, public cap) · `workers/api/src/routes/signup.ts@SESSION_TTL_SECONDS` (8h, an authenticated session) · REQ-187/069 | The status-cap grants UNAUTHENTICATED read of a shipment's status to whoever holds the URL. The expiry MECHANISM is tested (`status-cap.test.ts` — *"an expired cap fails verifyStatusCap"*); the LIFETIME is not, and it is declared nowhere — not in REQ-187 (which governs lens-scoping), not in genesis, not in ops. Changing it 30 days → **10 years** left all 754 api tests green | Decide the policy and record it (a REQ row or an ops line), THEN pin it — pinning first would freeze a number no one chose | **Low–Med** — no defect today; the exposure is that the value can move silently and has consequence |
| ~~**Per-tenant sweep containment is unobserved in 10 of 11 sweeps**~~ **CLOSED 2026-08-06 (audit §411) — all 11 pinned.** `billing/metering` (§408), the eight in `workers/agents/src/index.ts` + `watchtower-snapshot` (§409/§410), and `translator/sweep-214` (§411). Each poisons one binding and asserts the sweep RESOLVES, with a per-row non-vacuity assertion that the failing tenant was NAMED — which caught two would-be-vacuous rows: a weekly `isSnapshotDay` gate that returned before the loop (§410), and a D1 poison on a sweep that reaches R2 first (§411). Each mutation-proved by removing its own containment | `workers/agents/src/index.ts` (8) · `agents/src/watchtower-snapshot.ts` · `translator/src/sweep-214.ts` · `billing/src/metering.ts` · REQ-025/278 | Closed — the eleven guards were correct throughout; this was coverage of a fairness property whose failure is silent | **Low → resolved** |
| **`passports` is a WRITE-ONLY projection maintained on the hot append path** | `packages/ledger/src/projection/passports.ts` · `workers/api/src/do/sequencer.ts:470@projectPassport` · REQ-012 | `projectPassport` is batched into the sequencer's append transaction, so every qualifying event pays an upsert. **Nothing reads the table in production** — the only `SELECT … FROM passports` anywhere are two TESTS (`packages/ledger/test/seed-load.test.ts`, `projections.test.ts`), which verify the projection is correct without anything consuming it. Found by asking each of the 18 tenant tables *"what changes if this is wrong?"* (audit §417) | Either a surface reads it (the party trust/passport view it was built for) or the projection comes off the append path. **Not a deletion decision to make here** — CLAUDE.md requires a written deletion for a table | **Low** — correct and verified, costing one statement per append and returning nothing |
| **`assets` is a table with no code at all** | `db/tenant/migrations/0002_domain.sql:80` · CLAUDE.md hard budget | Zero readers, zero writers, zero tests — the ONLY references anywhere are its `CREATE TABLE` and its entry in `invariants.ts`'s `MUTABLE_TABLES` classification list. (A word-grep also hits `surface-contract.test.ts`, which matches Workers **Static Assets** bindings — a different thing.) So one of the 21 used tables is unused, and the effective spare is **2, not 1** | Owner call: the feature that needs it lands, or the register carries the written deletion CLAUDE.md requires. Recorded rather than actioned — a drop is a schema change under the append-only migration law | **Low** — costs nothing at runtime; it costs a budget slot and a reader's attention |
| **The a11y gate scans three UNAUTHENTICATED landing screens, not "every core flow"** | `tests/e2e/accessibility.spec.ts:18@SURFACES` · REQ-285/158 | The gate's own budget line reads *"zero serious and zero critical axe findings on every core flow"*. `audit(page, url)` does a bare `page.goto(url)` with no session, so for the portal it scans `ReAuthPrompt`, not `Board`; the command and driver surfaces likewise get their pre-auth view. **Proved by two mutations (audit §424):** a critical `image-alt` violation injected into the portal's authenticated `Board` render left the gate **PASS 4/4** — the scan never reaches it; the same violation injected into `ReAuthPrompt` flipped it to **FAIL, `1 failing of 4`**, naming the portal test. So the gate WORKS and its scope is the finding | Authenticate before auditing (the e2e suite already has `signIn`, and the sibling keyboard test in this same file drives the real driver day sheet), or narrow the stated budget to what is scanned. Advisory until WP-10 exits per REQ-158 | **Low–Med** — the surfaces a customer and a driver actually use are the ones not scanned |
| **The visual gate's 2% tolerance is 185× the drift it was measured absorbing** | `playwright.config.ts:23` · `tests/visual/blessed/*.png` · REQ-158 | The gate itself is SOUND — a red day-sheet background and a `22px → 44px` row-padding shift each flip it to `3 failing of 5`, attributed by screen name, and it never re-blesses its refs (audit §425). The finding is the threshold. `maxDiffPixelRatio: 0.02` on a 1440×900 frame allows **25,920 differing pixels**; measured ambient drift against the blessed refs on a clean tree is **140 px (`command.png`) and 44 px (`portal.png`)**, reproducible across four runs, with the other three screens at **0**. So the tolerance IS load-bearing for two screens, and any regression under 25,920 px passes silently on all five. The config comment records that the 2% was sized for a live basemap owning ~3.6% of the frame — which the spec now ABORTS, as the same comment says. The justification was retired; the number chosen for it was kept | Re-derive the threshold from measured drift rather than from the removed basemap. **Blocked on a datum this audit cannot produce**: ambient drift is measured on one machine, and the cross-machine AA figure the 2% is now claimed to be for has never been measured. Tightening it blind trades a silent blind spot for CI flakes on unseen hardware. Advisory until WP-10 exits per REQ-158 | **Low–Med** — the gate catches gross regressions today; sub-2% layout drift is unobserved |
| **In CI the perf gate enforces one of the three budgets its step name advertises** | `.github/workflows/ci.yml:45-46` · `packages/map/perf/perf.spec.ts:146,156` · REQ-079/158/288 | The gate is sound — both live budgets are mutation-proven RED, naming `perf.spec.ts:140` and `:152` (audit §426). But it runs on `ubuntu-latest`, which has no GPU, so the spec's `if (softwareRasterizer)` branch declines to assert the long-task budget **named in the step title**, and `isReferenceMachine` is false because nothing in the repo sets `PERF_REFERENCE_MACHINE=1`. That leaves interaction p95 ≤ 500ms as the only budget CI enforces, against ~21ms measured locally. Both conditionals are honest and documented (`slo.md:36`, `secrets.md:48`); what is undocumented is which set CI actually gets | Run perf on a GPU-capable runner (enforces long-task, and FPS with the env var set), or rename the step and record that CI enforces interaction latency only. Renaming alone makes the gate honest about enforcing less | **Low–Med** — the budgets hold everywhere they are measured; CI's coverage is narrower than its name |
| ~~**The translator judges floors on gross by omission, held only by a sentence**~~ **TRIPWIRED 2026-08-06 (audit §468)** — the premise is now asserted, not written: `map-204.test.ts` fails the day the plan grows `legs`/`tenantParty`/`split_bps` at any depth, naming the CLAUDE.md Law 5 consequence. Proved by adding `legs` to the plan's return: 1 failed, the tripwire. | `workers/translator/src/inbound.ts:615@assessApproval` · `packages/rater/src/approval.ts:195` · REQ-040/030 · CLAUDE.md Law 5 | The EDI inbound path calls `assessApproval(quote, {})` — an empty opts object takes the DIRECT branch, judging the quoted sell rather than an executing share. Correct **today**: the translator constructs no interline legs, and the comment says so. But the rater's fail-loud fires only on a PARTIAL interline signal (legs without tenantParty, or the reverse); passing NEITHER is silently legal, so the day an EDI tender carries legs this path judges gross and violates Law 5 with no signal. Both floor surfaces are otherwise mutation-proven, 5 and 3 named failures (audit §427) | Pin it the way §379/§380 pinned the dormant transports: a tripwire asserting the translator's plan carries no legs, which fails the day one is added — at exactly the moment the omission stops being safe. Latent: EDI transport is CONFIRM-gated and unbuilt | **Low (latent)** — unreachable until an interline EDI tender exists; silent and money-affecting when it does |
| **A certified partner's `refQualifiers` are honoured OUTBOUND and ignored INBOUND** | `packages/edi/src/mapping.ts@applyMapping` · `workers/translator/src/core/map-204.ts:65@LOAD_UNIQUE_REF_KEYS` · REQ-203/034 | `partnerMapping(config)` is resolved and passed into the 214 build (`sweep-214.ts:199`), so the partner's `statusDialect` shapes what we SEND. The inbound 204 path never references a mapping at all: `map-204` keys on **hardcoded X12 qualifiers** (`SID`/`BM`/`PRO`) and namespaces refs by the raw qualifier. `applyMapping` — the function that renames ref keys via `refQualifiers`, i.e. exactly the inbound normalisation — is **built, tested (5 assertions), and has zero production callers** (audit §421). Consequence if it is a gap: two partners using different qualifiers for the same concept store refs under different keys, so a lookup by our canonical name misses. Consequence if it is a decision: nothing, and the function is speculative | **The question is not recorded anywhere and should be, before the live transport binds.** Either inbound applies the partner mapping (wire `applyMapping` into the 204 parse) or `refQualifiers` is documented as outbound-only and the function is marked as such. Latent today — live EDI transport is CONFIRM-gated and unbuilt | **Low** (latent; becomes real the day a second partner with a different dialect is certified) |
| **Seven of the 35 frozen kinds have no emitter; two appear nowhere but the taxonomy** | `packages/contracts/src/events.ts` · `genesis/10-EVENT-TAXONOMY-DATA-MODEL.md` · CLAUDE.md hard budget | Tracing every declared kind to a literal emitter across `packages/*/src`, `workers/*/src`, `apps/*/src` and `tools` (audit §418): **28 are emitted, 7 are not.** Five of the seven are traceable — `call.transcribed` and `credit.checked` carry standing holds; `dispatch.assigned`, `pickup.scheduled` and `quote.sent` are named in WP-07/08/09 (the last superseded in practice by the `messages` projection of `message.*`). **`quote.expired` and `seal.applied` appear in NO WP, NO hold and NO ops document** — only in the taxonomy that declares them. Not a defect: the catalog is frozen by design and declaring ahead of building is the intended pattern | Record which kinds are LIVE and which are RESERVED, in `genesis/10` or the register, so the 35-budget can be read for what is in use rather than only for what exists. No code change — a kind cannot be removed without a register amendment | **Low** — legibility of a governed budget, not a runtime risk |
| ~~**Two N+1 loops on cron paths**~~ **FIXED 2026-08-06 (audit §471/§472)** — `sla-sweep`'s answered-check is now ONE query per 40-pair chunk instead of one per row (500 overdue rows: 13 queries, was 500), semantics unchanged and proven by the 8 pre-existing behavioural tests plus a new cross-stream isolation case. `sweep-214`'s per-marker `r2.head` is now ONE prefix list plus a narrow re-check for survivors only (§472) — the put itself is per-transmission and bounded by real work. | `workers/agents/src/sla-sweep.ts:101@answeredBatchSql` · `workers/translator/src/sweep-214.ts:229@sentKey` | `sla-sweep` loads every overdue inbound (its driving query has no `LIMIT`) then issues **one D1 query per row**; `sweep-214` does an `r2.put` **per marker** over a paginated R2 list. Workers cap subrequests per invocation, so both fail hard at volume rather than slowing down. The SLA one compounds §133: a DAILY cron policing a FOUR-HOUR SLA accumulates ~19 hours of overdue rows per tick, and then N+1s over them | Batch the answered-check into one query keyed by the row set; batch or bound the marker writes | **Med** — invisible at fixture volume, and both are the kind of cron that must complete |
| **Unbounded list reads — 9 sites, no LIMIT and no cursor** (audit §183, re-verified 2026-08-06 §470; **count CORRECTED 2026-08-09 §823 — this row said "7 sites" while already citing 8, and the roster test pinned 8; the agreement test matched only the title, so the disagreement was invisible for two audits. The 9th, `GET /v1/approvals`, was found by enumerating every API list endpoint instead of re-reading the known ones**) | `workers/api/src/routes/invoices.ts:56@INVOICE_COLS_TENANT` (tenant lens: every invoice, NO `WHERE` at all) + `:45` (party lens) · `routes/watchtower.ts:45@anomalies` (`status=all` drops the WHERE) · `routes/dunning.ts:385@drafted_by_agent` · `routes/export.ts:120@DOC_EXPORT_COLS` and `:126` · **+2 found in §211 after fixing a glob that had hidden 132 of 215 production files: `packages/ledger/src/parity.ts:254@backing_kinds` (every native+legacy event of the backing kinds, FULL PAYLOADS — the authority-parity comparison) and `workers/agents/src/watchtower.ts:216@anomaly` (every anomalous quote ever, on the daily cron)** · **+1 found in §823: `workers/api/src/routes/approvals.ts:165@APPROVAL_COLS` — `GET /v1/approvals?status=open` returns EVERY approval of that status with no cursor, while its siblings `/v1/exceptions` (REQ-197 keyset) and `/v1/invoices` (REQ-010 cursor) both paginate** | Every row a tenant has ever accumulated is loaded into one 128MB Worker response. The repo already solved this TWICE and registered it — REQ-197 gave the exceptions queue a `before_ts` keyset, REQ-010 gave the export stream a cursor — so this is an unevenly applied rule, not an unknown risk. `export.ts` paginates its EVENTS and returns ALL documents in the same response. **SECOND CONSUMER, unrecorded until audit §372: the MCP `document` tool** (`workers/mcp/src/tools/document.ts:92@invoiceOnShipment`) requests `/v1/invoices?shipment_id=…` and **the route reads no query parameters at all** — it returns every in-tenant invoice, and the narrowing to one shipment happens CLIENT-SIDE in the tool. So the worst unbounded read is pulled in full to answer a SINGLE-shipment question, on every `get_document(invoices: true)` call, not only on a portal list view | Needs a REQ row: keyset-paginate these reads. **A bare `LIMIT` is the wrong fix** — it truncates silently, which this repo forbids (the Migrator no-silent-drops rule) | **Med** — grows with tenant age; harmless at fixture volume, worst on the highest-volume object (invoices) |
| ~~`credit_status` projection write-ordering (REQ-183)~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `credit.checked`→`parties` · REQ-183 · WP-08 | A hold recorded before the party row exists was a silent no-op UPDATE, weakening the REQ-042 credit gate | Assert rows-affected==0 → durable `anomalies` row (**landed WP-11**) | **High→resolved**. ~~(register still `WP08-DISCOVERED`)~~ **Corrected 2026-07-27:** REQ-183 now reads `F0-SPEC'D` (`wp=WP-11`) — advanced on verified evidence in commit `7c1a0b4`; it is no longer a drift row (`pnpm check:coverage` does not list it) |
| **REQ-170 residual: PLACED-PHOTO hash not byte-verified** | `workers/agents/src/biller.ts:594-601@REQ-170`; `packages/ledger/src/gates/transition-gates.ts:188-194` | The placed-photo hash is not byte-checked (its URL resolver is deferred), and the `routes/evidence.ts` redelivery fast path re-drives the Biller without re-checking. The email frames itself as the delivery record; its proof claim rests on the SIGNATURE bytes, which are verified | Photo-URL resolver + hash-verify on the placed-photo half | **Med** — register `WP06-DISCOVERED`, correctly OPEN; do not "advance" it. **NARROWED 2026-08-03 (audit §124).** This row previously read *"missing-evidence send-gate UNIMPLEMENTED"* and described the exposure as *"a POD with a fabricated hash + no R2 upload still triggers an evidence email over zero stored bytes."* **That half SHIPS and is tested.** `workers/agents/src/biller.ts:483-494@loadActivePodDocument ` requires an ACTIVE tenant-scoped POD document AND a present R2 object for the recorded `signature_hash` before the invoice mints; a miss is `held(evidence_missing)` — no invoice, no send, no terminal marker, and no fabricated-hash POD can produce a proof email. Pinned by four cases in `workers/api/test/biller.test.ts:683` (missing document · missing R2 object · retention-tombstoned document · cross-tenant r2_key). The 2026-07-27 annotation was right that the row must stay open — but it stayed open under a *title and risk statement that had become false*, overstating live exposure to any owner reading this page |
| Photos absent from evidence email (`photos: {}`) — **also the "+ photos" half of acceptance demo #1 (`CLAUDE.md`)**. §734 CORRECTION: this row previously said "and of REQ-087's DoD (\"sig/pallet photos\")". That misquotes the register — "sig/pallet photos" is REQ-087's TITLE; its **DoD is "Design-system email renders across clients"**, and that DoD IS met (the view renders real `<img>` when given URLs — `packages/agents/test/evidence-email.test.tsx`, 3 cases incl. a caption-swap trap). The distinction is load-bearing: a DoD failure would mean a REQ is accounted done while failing its acceptance criterion; what this actually is, is a TITLE promising more than its DoD requires. Demo #1's wording is the real casualty | `workers/agents/src/biller.ts:588@photos` (citation read `:465` until 2026-08-04 — audit §178; that line is an unrelated interline hold marker) | Documentary placeholder slots, no real photo URLs. The view renders them correctly when given URLs (`packages/agents/test/evidence-email.test.tsx` — 3 cases incl. a caption-swap trap); nothing resolves the URLs. **Demo #1 cannot be filmed showing photos in the inbox until this lands.** | **§734 — NOT A WIRING TASK; the resolver already EXISTS.** `mintDocDownloadCap` + `GET /pub/documents/:cap` are built, and `doc-cap.ts` names the Biller as their intended second caller. What blocks it: the agents worker binds **neither `JWT_SECRET` nor an `API` service** (`workers/agents/wrangler.toml` — D1, R2 `EVIDENCE`, queues, a cross-script sequencer DO, nothing else), so the Biller has no secret to mint with and no route to ask. Both remedies are **decisions, not edits**: binding the session secret into the agents worker widens what that worker can mint (session JWTs, not just doc caps), and a mint route is new surface. A third decision is the **TTL** — `expSeconds` is absolute, and a cap embedded in an email needs a lifetime measured against an inbox, not the 5 minutes a portal download uses, while the cap is bearer-only and forwardable by design. Ties REQ-170, whose placed-photo residual shares the same resolver | **Med** |
| Status-page server-side geo generalization | `packages/ledger/src/redact.ts` · REQ-074 | **VERIFIED server-side (WP-16)**: `redactEvent` coarsens PARTY geo to ~11 km + drops accuracy (structural walk, until out-for-delivery); `/pub/status` generalizes server-side (coarse even at OFD); ~~`GET /v1/board` is tenant-only; the portal party fleet is synthetic~~ **superseded 2026-07-27 (V1 close-out, final cross-task review): `GET /v1/board` serves TWO lenses and the portal party fleet is LIVE server-scoped data** — `workers/api/src/routes/board.ts:17-31` resolves the lens from the JWT claim, applies the party-relationship predicate in SQL and generalizes coordinates server-side (REQ-085/074/025); `apps/portal/src/api/board.ts:59-60` fetches and Zod-`.strict()`-parses it; `demoFleet` has one definition (`packages/map/src/demo.ts:112`) with no app importer. Proven by `redact.test.ts`. | Remaining: hold the boundary when public status-page HOSTING stands up (deploy gap, §2). ~~+ when a real party-scoped live feed lands~~ **that feed has landed** — the party lens now guards real geography, so it is a standing re-probe at every exit, not a deferral | **Low** (verified; deploy-gated) |
| Driver auth + lockout deferred | `apps/driver/src/session.ts:68` · REQ-069 | Only a per-device P-256 key exists — no login/magic-link/PIN, no lockout counter | Build driver login + lockout policy | **Med** (driver auth) — F0-SPEC'D |
| ↳ **When that login lands, `AuthSession.clear()` must verify the removal** (audit §182) | `apps/driver/src/auth/session.ts@clear` · REQ-069 | Its catch swallows, so a storage that reads but refuses to write leaves the token behind while the caller believes the session was dropped. Harmless **today** — both call sites are 401 handlers, so the server has already rejected that token. A **voluntary** logout would be asked to drop a token the server still honours, on a shared device | Read back after `removeItem`, overwrite, and surface a failure — do not assume it | **Low today, Med the day a logout exists** |
| Continuous 30s GPS emitter + detention/dwell ± math | `packages/ledger/src/geo/fence.ts:90` · REQ-018 | Gate stamps + accuracy radius ship; no continuous position emitter, no detention money engine | Build emitter + dwell calc disclosing its ± bounds | **Med** (battery test + live board depend) |
| 214-sweep dedup-key not partner-qualified | `workers/translator/src/sweep-214.ts:254@send214` — **re-anchored 2026-08-09 (audit §885): the previous `:182` pointed at a BLANK line.** The claim is unchanged and the source states it itself a few lines up: *"`dedupeKey` is NOT tenant/partner-[qualified]"* | R2 marker is partner-scoped but `dedupeKey`=`edi214/<event id>` is bare; a live partner-side-dedup adapter risks cross-partner collision | Scope dedup by `(partnerId + key)` when live adapter lands | **Med** (safe today, NotConfigured) |
| ~~**PROJECT-STATE.md stale** (dated 2026-07-14, pre-WP-08)~~ **FIXED 2026-07-27** | `docs/ops/PROJECT-STATE.md` | ~~"Done" stops at WP-06; "Parked" list predates 6 WPs; 1,131-test count stale — misleads status reads~~ **Re-baselined onto WP-01..16 + T14/T15 reality in commit `d562d2b`** (corrected at `75ed83b`, which also added it to the annotation-scanner exclusion list so a status pointer cannot annotate itself into evidence — `tools/traceability/orphans.ts:35-41,67`). Its header now reads `As of 2026-07-27 · HEAD 7c1a0b4` | ~~Re-baseline against WP-07→WP-12 + register~~ done | ~~**Med** (documentation)~~ **resolved** |
| iCloud `name 2.ext` duplicate hazard | PROJECT-STATE.md:46; MEMORY | Desktop iCloud sync spawns duplicate files that corrupt file-count budget gates | Check `find . -name "* 2.*"` each session; durable fix = move repo off `~/Desktop` | **Med** (env) |
| Dependency/env fragility | MEMORY `dep-env-fragility` | `node_modules` can drift from lockfile (eslint crash needs rm+reinstall; chai pinned 5.3.3 for vitest-pool-workers; pnpm v11 overrides in `pnpm-workspace.yaml`) | Run full `pnpm verify` at WP exit; reinstall on drift | **Med** (env) |
| Staging evidence sending is LIVE | PROJECT-STATE.md:7; `DEPLOYMENT.md:25` | Real email reaches any address in `parties.contacts` (synthetic tenants) — a live outbound path to be aware of | Disable by unsetting `EVIDENCE_FROM` + redeploy | **Med** (awareness) |
| Airplane-mode soak flake | `fixtures/README.md`; commit `b9e2d47` | Driver airplane-mode soak is timing-sensitive; a 30s ceiling was added to stabilize it | Keep the 30s ceiling; watch for regressions | **Low** (test flake) |
| ~~**Concierge SLA-before-append ordering is unpinned** (3 sites)~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `workers/agents/src/concierge.ts` · REQ-092/095 · audit §96 | The source sets `sla_due_ts` BEFORE appending `quote.requested`, and reasons about why: a crash between the append and the SLA write makes the redelivery guard return `already_handled`, so the SLA would never be set. **Reversed, an owed customer inbound carries no due ts, never surfaces in the overdue sweep, and goes unanswered with nothing alarming** — the fail-SILENT family of C3 (§74). Swapping the two statements at all three sites leaves `workers/agents` 106/106 green: the ordering is a documented claim with nothing executable behind it (§95's class) | Add one case per §95's pattern — make the append fail, assert `sla_due_ts` is ALREADY set (so the retry is safe), then swap the order and confirm it turns red. Needs a concierge queue-handler harness that `workers/agents/test` does not yet have, which is why this is recorded rather than fixed in place | ~~**Med** — fail-silent, narrow window~~ **CLOSED 2026-08-03 (audit §97)** — the harness existed all along in `workers/api/test/concierge.test.ts`; the deferral was based on an unchecked claim. Test added, mutation-proved (swapping all four sites turns only it red) |
| **`CLAUDE.md` rule 9 says "adversarial audit swarm at EVERY WP exit" — the practice changed after WP-03** — OWNER DECISION | `docs/wp/WP-01..16.md` · audit §99 · REQ-119 | WP-02 and WP-03 ran per-WP swarms and recorded them; **WP-04…WP-16 have no exit-audit section at all**, and WP-01 never had a labelled swarm. What replaced it is real and documented — three whole-codebase adversarial audits (2026-07-15, 2026-07-22, 2026-08-01) — but the stated law still says per-WP, and nothing recorded the change | Either amend rule 9 to describe the periodic whole-codebase practice, or resume per-WP swarms. Amending a stated law needs an owner signature | **Low** — the auditing happened; only the framing is stale |
| **Below-floor interline splits HOLD indefinitely — by design, unrecorded until now** | `workers/agents/src/interline-split.ts` + `biller.ts` · REQ-040/019 · audit §87 | `approvalGranted` is **intentionally unwired** in both the Biller and the interline splitter, so a below-floor share (or an anomaly) HOLDS and never appends until the approvals queue lands. Fail-**closed** — the conservative direction, and correct. The gap is operational, not behavioural: an operator watching interline splits sit unresolved had nothing in the record telling them this is designed, not broken, and that the release path is the approvals queue rather than a retry | Keep as-is until the approvals queue ships; when it does, wire `approvalGranted` in BOTH callers (they are deliberately symmetric) | **Low** (fail-closed; operational visibility only) |
| **Multi-guard 403 routes: ~24 guards not yet mutation-checked** | `workers/api/src/routes/*` · audit §81/§82 | §81 found a security test passing for a SIBLING gate’s reason — `positions.ts` has three guards that all answer 403, and its assignment-gate test stayed green with that gate deleted because the fixture was also consent-less. Fixed there, and `events.ts` + `portal-actions.ts` verified pinned. ~~Eleven routes have ≥2 403 guards; roughly two dozen individual guards remain unmutated.~~ **Corrected + partly cleared 2026-08-03 (audit §83): the real field is SIX routes / 18 guards** (§82 counted comment lines). Swept and PINNED: `positions.ts` ×2, `rate.ts` ×2, `board.ts` line 78. `board.ts` line 185 is unpinned and correctly so — `requireRole` excludes `driver`, so that defence-in-depth branch is unreachable through the route. ~~Remaining: `events.ts` (8), `status-link.ts` (2), `portal-actions.ts` (1).~~ **CLOSED 2026-08-03 (audit §84): all 18 guards swept against the FULL suite. 13 pinned; 4 unpinned for verified-correct reasons (1 unreachable via `requireRole`, 1 masked by the DO’s `t:root` check, 3 defensive `LENS_UNRESOLVED`→403 translations); and ONE real hole — `events.ts` line 255, the SOLE enforcement of gate-override authority (a `driver` could have waived a server-side gate; the DO stamps `override` but never re-checks the author’s role). Test added + mutation-proved.** Each needs one revert + one suite run — mechanical, not judgement. **EXTENDED to 404 on 2026-08-05 (audit §375): the §84 sweep covered 403 ONLY.** Routes with >=2 guards sharing a status were re-enumerated across all statuses. `routes/documents.ts`'s three 404s are the sharpest case — identical code AND identical message by design (no existence oracle), so no response can distinguish them — and both authorization guards are PINNED, by tests naming the distinguishing condition. `routes/devices.ts` was the gap: the SAME `404 UNKNOWN PRINCIPAL` guard replicated across enroll/list/revoke, and mutating each independently left all 757 tests green **three times**. Three tests added, each now RED for its own handler. **REPLICATED-GUARD sweep, audit §376:** 12 (code, status, message) triples appear more than once across `workers/api/src`. Swept: `SHIPMENT NOT IN YOUR SCOPE` x3 (three files, all PINNED — 4/1/2 failures) and `UNKNOWN TENANT` x4 (3 pinned; `workers/api/src/tenants.ts:24@binding` green and CORRECTLY so — `pub/quote.ts` 404s an unknown host BEFORE resolving a handle, and `values(HOST_TENANTS) ⊆ keys(TENANT_BINDINGS)` is pinned twice, at module load and in `isolation.test.ts`, so the third layer is unreachable unless both fail). **`SESSION LENS UNRESOLVED` x8 partly swept (audit §377) and ONE CLASSIFICATION CORRECTED:** §84 recorded three of these as *unpinned for verified-correct reasons* as **defensive** translations. They are not defensive — `party_id` is `z.string().optional()` in `SessionClaims` and the auth middleware does not require it for `role: "portal"`, so a portal token without one is schema-valid and `packages/ledger/src/lens.ts:19@LENS_UNRESOLVED` throws for real. Without the translation the plain Error reaches `app.onError` → **500**, not 403. `board.ts:78` was already pinned; `invoices.ts:28` and `documents.ts:30` were green and now have tests. Still-unswept: the five inline copies (`portal-actions`, `status-link`, `copilot`, `events`, `rate`), `SHIPMENT ID TOO LONG` x5, and the `400 x2..x7` clusters (11 routes) — validation guards, lower value than authorization ones | Run the remaining mutations per guard; where one is unpinned, assert the DISTINGUISHING message rather than the shared status. The authoring-time rule (§82): if a fixture trips more than one guard, the assertion must name the one intended | **Low–Med** — no known open hole; this is coverage of guards that are themselves present and correct |
| **`routes ±10%` names a merge gate with nothing behind it** — OWNER DECISION | `genesis/11-REPO-CLAUDE-MD.md:41` (copied into `CLAUDE.md` rule 6) **and `genesis/14-BUILD-EXECUTION-SPEC.md:52`** · audit §61/§68 — it appears in TWO source-of-truth docs, so it is settled intent rather than a stray phrase, which makes the owner decision sharper not softer | Rule 6 lists four fixture gates. Three are real: legacy-export replay ±2% (manifest `legacy-export-replay`, pending), QB penny-reconcile (vendored, 7 tests green), airplane soak (in-repo, 2 tests green). **`routes ±10%` is in none of them** — no row in `fixtures/manifest.json`, no row in `fixtures/README.md` (the file rule 6 itself cites), no REQ row, and no routing/mileage capability anywhere in the register. NOT BUILT, deliberately: `CLAUDE.md` says if it is not a REQ row it does not get built, and inventing a routing-parity gate would be exactly that | Owner: either **add a register row** (if route/mileage parity is real scope) or **strike the clause** from `genesis/11` as vestigial. Until then it is a gate name with no gate, which reads as coverage that does not exist | **Low** (no false green — the gate does not exist, so nothing reports PASS for it) |
| Multi-factor cost surface + per-leg interline floors | WP-04 · `costBasis` seam | Floors ride the linehaul-freight cost proxy; compare executing share vs full-move floor — conservative (over-escalates, never under) | Real op-cost surface (stops/cube-miles/dwell) + per-leg floors (needs op-cost kind + engagement input) | **Low** (conservative) |
| Single-transaction atomicity across the 3 `/rate` events | WP-04.md:36 | Transient infra failure without a client retry can orphan `quote.priced` | True single-txn atomicity (mitigated by required Idempotency-Key) | **Low** |
| POD-gate `serviceClass` exemption unwired | ~~`sequencer.ts:281-287`~~ **corrected 2026-08-02 (audit §55): `workers/api/src/do/sequencer.ts:342@invoice_without_pod_classes`** — `:281` drifted onto the `authority.flipped` stream guard, ~61 lines above the TODO it claims to cite; re-pointed WITH a content anchor so the next drift fails the gate instead of silently mis-aiming a reader · REQ-030 | `invoice_without_pod_classes` inert (fail-**safe**: POD always required) | Thread `shipments.service`; add test before enabling any bypass | **Low** (fail-safe) |
| Precise `deriveOperatingState` (point-in-polygon) — **resolver LANDED (V1 Task 13); LICENSED artifact is a BLOCKED external HOLD** | `packages/ledger/src/geo/{jurisdiction,polygon-source}.ts` · REQ-166 | Coarse five-box stub REPLACED by exact integer point-in-polygon over a **version/hash-pinned** admin-boundary artifact. Fail-closed to `XX` on parse/coverage/**fixture-hash-mismatch**/malformed-polygon, on any boundary edge, outside all coverage, or interior to >1 state — the boxes were fail-**open** on coastal/border cases (ocean-off-Big-Sur and Reno-NV both derived a confident `CA`; PIP resolves both to `XX`). Proven by `packages/ledger/test/jurisdiction.test.ts` (38 tests). The **active** artifact is **SYNTHETIC / coarse / 5-state** (`fixtures/jurisdiction/us-states.synthetic.json`, sha256-pinned in `fixtures/jurisdiction/manifest.json`) — a test/dev stand-in, byte-bound to the embedded const; any coordinate outside its coverage → `XX` (fail-closed). | **BLOCKED external HOLD** — the production-grade, cartographically-accurate, **all-states+territories LICENSED** admin-boundary dataset (`manifest.json` id `us-admin-boundaries-licensed`, status `blocked`; license [CONFIRM] owner=ops+counsel) is NOT committed. **Go-live consumes it by:** (1) vendor bytes under `fixtures/jurisdiction/` via the approved fixture process, (2) pin its sha256 in the manifest and flip status→`vendored`, (3) point the `jurisdiction.ts` active artifact at it, (4) re-run the jurisdiction + consent + position suites green. Until then GPS consent for unsupported jurisdictions stays fail-closed. | **Low (fail-closed) — code landed; data = external HOLD** |
| Round-trip / stop-off driver flows | `apps/driver/src/flow/stop-flow.ts:175` · REQ-053 | `buildFlow` models single-stop only | Multi-leg flows | **Low** |
| Per-ping raw-position co-signing | `workers/api/src/routes/positions.ts:22` · REQ-018 | Open [CONFIRM]: whether each raw GPS ping is per-ping co-signed (3 auth gates ARE enforced) | Decide perf/UX tradeoff | **Low** (open CONFIRM) |
| Server-side GPS plausibility cross-check | WP-05.md:59 | Device signs its own geo = attribution, not server-truth | Speed/teleport/tower corroboration | **Low** (future hardening) |
| **`pod.actor.party` is client-supplied and selects the REQ-040 executing share** | `workers/agents/src/biller.ts:465@resolveInterline` · `packages/rater/src/approval.ts:126@tenantParty` · REQ-040/REQ-016 | The interline floor judges the share of the party named by `pod.actor.party`: `resolveInterline(rows, pod.actor.party)` → `composeInvoice({legs, tenantParty})` → `executingShare(gross, legs, tenantParty)`, which picks `leg.executor === tenantParty`. That field arrives on the generic client-post route, which contains **zero** references to `actor` — the sequencer validates `actor.device === device_id` and the device SIGNATURE, never `actor.party`, and **no gate reads it**. A driver with a registered device therefore signs a valid `pod.signed` naming ANY party. Naming a party that executes no leg is FAIL-CLOSED (unresolved → hold, verified). Naming the PARTNER's party is not: the floor is then judged against the partner's split (e.g. 8000 bps) instead of the tenant's (2000 bps), so a below-floor move clears REQ-040 and auto-invoices — the fail-open direction of the permanent $222K-class regression. Found by splitting the guarantee's clauses (audit §400); the clause *"the POD signer's party — device co-signed, I4 — the authoritative 'who is the tenant here'"* is the one whose input is client-supplied | **Owner decision — which SERVER-SIDE fact establishes the tenant's executing party?** The biller is a queue consumer with no session, so `session.party_id` is unavailable there; candidates are the device→party binding already in the control plane (`packages/driver-core/src/capture.ts:95@deviceCtx` shows the client defaults to it), or a tenant-party column consulted at append. Not fixed unilaterally: it changes what a valid append must carry | **Med** — internal authenticated actor, requires knowing a partner party id; bypasses a named permanent guard, does not move money directly |
| Device signature does not bind `source`/`party_refs` | WP-02.md:137 | **SCOPE VERIFIED 2026-08-06 (audit §415).** `clientView` binds ten fields — `id, shipment_id, kind, payload, evidence, actor, ts, device_id, device_seq, captured_ts`. **Four** envelope fields sit outside it, not the two this row names: `source`, `party_refs`, `confidence`, `override`. The two unnamed are **immaterial and that is now recorded so nobody re-derives it**: `confidence` is written to the events column list and read by NO decision anywhere (grepped across `packages/ledger/src`, `workers/*/src` and `db/`), and `override` requires an elevated role at the route (`ops/admin/finance`), which a device-signed driver append can never carry. `source` is separately forced to `native` on every client post (§373), so its unbound status is moot. **That leaves `party_refs` as the one materially-unbound field** — it feeds the party lens's `EXISTS (SELECT … json_each(party_refs) …)`, and §402 established the exposure is self-scoped: a client can only set it on events it authors | Register note before `clientView` frozen-law change | **Low** |
| CMS/cert-chain TSA receipt verification ~~deferred~~ LANDED (WP-16) | `packages/ledger/src/tsa/cms.ts` `verifyTsaSignature` · REQ-014 | Structural DER parse PLUS crypto verify: SignerInfo signature over signedAttrs (RSA/ECDSA-P256, SHA-256), messageDigest binds TSTInfo, chain to a CONFIGURED trust anchor, validity + timestamping EKU — fail-closed, opt-in on `trustAnchors`. Raw `.tsr` in R2 verifiable offline forever. | Remaining (deploy, not code): real trust anchors = config (F1 CONFIRM, line 99); revocation (OCSP/CRL) is a stamping-time/monitoring concern. **CLARIFIED 2026-08-06 (audit §420): `verifyTsaSignature` has ZERO production callers, and that is BY DESIGN, not an unwired gap.** Ingest (`tsa/client.ts@assertGrantedReceipt`) is structural only — granted status, echoed imprint, echoed nonce — which prevents SUBSTITUTION but not FORGERY; the crypto lives offline over the raw `.tsr` bytes `anchor.ts` writes to R2 first. Recorded because "LANDED" plus a landed-but-uncalled function reads as wired, and the ingest comment previously claimed a "forged/substituted" receipt could never be persisted — true of one of those two words | **Resolved** |
| Live DO board fan-out — the board is a POLLED read, no live push | **CORRECTED 2026-08-09 (audit §877): `notifyBoard` does not exist in code.** `git grep` returns three hits, ALL documentation (this row, `docs/plans/2026-07-09-wp02-ledger-core.md`, `docs/wp/WP-02.md`), each describing a seam the sequencer was going to expose and never did — the row was right about the world and wrong about the artifact | Board is a polled read; real-time push is **REQ-257 (`vNEXT`, V2-E)**, whose own text names hibernatable DO WebSockets *and its polling fallback*, so today's 20s poll is the sanctioned interim (§865) | Nothing to wire until REQ-257 leaves `vNEXT`. Do not re-add a `notifyBoard` seam to satisfy this row | **Low** |
| True OPERATING RATIO | WP-10.md:24 | No op-cost event kind; KPI honestly relabeled "Cost/Rev (quoted basis)" | Add linehaul/driver/asset op-cost kind | **Low** (future) |
| Dedicated `paid_ts` column | WP-10/WP-11 | Paid-when lives in `payment.received` ts | Later amendment if needed | **Low** |
| Exception resolution / claim-adjudication | `workers/api/src/routes/exceptions.ts:20` | "Resolved" = terminal-state heuristic; no `exception.resolved` kind | Formal resolve flow (register amendment) — WP-11 | **Low** |
| Dims stay PRICE-INERT (density/class pricing deferred) | WP-04/07/08/09/10 · REQ-175 | Dims fail-closed on presence only; not price-affecting anywhere (C1 gate regression-guarded) | Density/class pricing = future | **Low** (fail-closed) |
| Concierge SLA = 4h documented default | WP-07.md:66 | No per-tenant SLA-config kind | Per-tenant override kind (later WP) | **Low** |
| Status/claim inbound carries no SLA | WP-07.md:67 | Non-quote inbound resolves `unresolved`, no stream | Exceptions queue (WP-11) | **Low** |
| REQ-177 deterministic-request storage deferred | WP-07.md:30 | Arms only when live LLM bound; prevents concurrent-redelivery inconsistency | Record corroborated request in `quote.requested` on `ClaudeParser` go-live | **Low** |
| REQ-180 register alignment (6 vs 7 kinds) | WP-11.md:28 | Code floor clamps 7 internal kinds (incl `split.computed`); register names 6 (fail-closed superset) | Owner register amendment | **Low** |
| `referralBase` config-drift on evidence fast-path | `workers/agents/src/biller.ts:589@referralBase` ~~`:456`~~ (audit §193 — `:456` is the REQ-040 interline-floor check, unrelated; the hazard is stated verbatim at `:585`) | Redelivery re-renders live `referralBase`; drift if it becomes body-load-bearing | Pin into `invoice.issued` (like `from_name`/REQ-178) | **Low** |
| `signed_by = "Signature on file"` | WP-06.md:70 | Ledger records a signature hash, not a printed name | Thread a name through capture when driver flow collects one | **Low** |
| Accepted quote = latest `quote.priced` before POD | WP-06.md:68 | No `quote.accepted` flow at WP-06 | Switch to booking-referenced quote (booking landed WP-08) | **Low** |
| NULL `bill_terms` → prepaid default | WP-06.md:69 | Payer terms-invariant; unrecognized non-NULL logs loud + proceeds prepaid | Unpick when booking populates terms + third-party column (WP-08) | **Low** |
| `pod.signed` with no `shipments` row | WP-06.md:92 | Logs loud + skips as data fault; REQ-169 sweep can't recover (nothing to project) | Recorded, not Critical | **Low** |
| `composeInvoice` throw burns 5 retries before DLQ | WP-06.md:93 | Noisy but bounded | DLQ + exceptions surface (WP-11) | **Low** |
| SMS evidence fallback throws (email-only) | `packages/agents/src/biller/sender.ts:266` · REQ-097 | `ResendSender` throws on `channel==='sms'` (non-retriable hold) | Wire Twilio adapter | **Low** |
| Voice / `call.transcribed` capture deferred | WP-07.md:27 · REQ-096 | Kind + internal visibility exist and are redacted; capture deferred (CONFIRM-gated do-not-build) | vNEXT | **Low** |
| Credit DECISION engine + credit-officer role | WP-08 · REQ-037 | WP-08 enforces a recorded hold only (no bureau call) | vNEXT | **Low** |
| Customer calendar `.ics` + confirmation surface | WP-08.md:23 | Scheduling events + transit window typed; rendered surface deferred | Portal/Command surface WP | **Low** |
| One leg per (shipment, kind) in v1 | WP-08.md:64 | Multi-stop/multi-leg-per-kind later | Later refinement | **Low** |
| Per-link cap revocation (jti denylist) | WP-09.md:26 | Short `exp` today (blast radius = milestone + 11km geo) | WP-13 | **Low** |
| Rate-card scraping resistance (banded sell) | WP-09.md:27 | Business-confidentiality residual (margin internals already excluded) | WP-12/GTM | **Low** |
| Dedicated portal `GET /v1/shipments?party` list | WP-09.md:29 | Board derives list from `/v1/invoices`; un-invoiced in-flight appear only on map | Add shipments-list route | **Low** |
| Dedicated `STATUS_SECRET`/`DOC_SECRET` | WP-09.md:63 | Derived from `JWT_SECRET` (domain-separated); no new secret to provision | Later hardening | **Low** |
| Weekly telemetry external publishing | WP-11.md:23 | R2 manifest is the telemetry; external publish deferred (CONFIRM-gated) | Post-CONFIRM | **Low** |
| Richer cash-app ambiguous-match surface | WP-11.md:24 | AR-settlement matches `payment.received`→open invoice; richer surface later | Later refinement | **Low** |
| Cash-account deposit journal line | WP-11.md:29 | Journal is AR/AP; deposit (payment→bank) mapping deferred | Later refinement | **Low** |
| `certifyPartner` validates mapping shape only | WP-12.md:43 | Round-trip-clean guarantee lives in test harness (`replayCertify`), not `certifyPartner` | Move the guard into cert flow | **Low** (operational gap) |
| 990 ack best-effort / no-ops when transport unwired | `workers/translator/src/inbound.ts:83@NotConfigured` ~~`:505`~~ (audit §193 — `:505` is the rate/accept-quote payload note; the best-effort port is declared at `:83`) | 204 recorded + chain appended; outbound 990 re-attempts on success | Lands with transport | **Low** |
| EDI 210-out, inbound decoupling queue, Command EDI console | WP-12.md:41 | Out of the lite slice; Command EDI console is design-CI-blocking | Later register-amended WP | **Low** (out of scope) |
| REQ-111 log→ledger unification | WP-01/02 | Would need a 36th event kind (= register amendment); logs stay event-shaped | Deferred WP-11 Watchtower | **Low** |
| Traceability "bare `REQ-xxx` comment" ceiling | WP-02.md:140 | A bare comment satisfies the orphan detector — structural limitation | Accepted | **Low** |
| `mask()` leaks first-char + length | WP-02.md:141 | Pre-existing WP-01 behavior | Accepted / out of scope | **Low** |
| Interline handoff gate `assertInterline` (REQ-045) | WP-05.md:55 | Built ahead of scope; live+tested but register-status `vNEXT`, not DoD | ~~Informational~~ **Diagnosed 2026-07-27: this is a register DATA defect, not a build gap** — genuinely built (`packages/ledger/src/gates/transition-gates.ts:261-275`) and tested (`packages/ledger/test/transition-gates.test.ts:273`, `workers/api/test/gates.test.ts:163`); the register row's `spec` column already says `WP-05` while its `wp` column reads `vNEXT`. Correcting that one cell (plus the status) is the cleanest of the eight drift fixes. See the failures ledger | **Low** (vNEXT) |
| Rater minor follow-ups | WP-04.md:31 | dims-value guard when density load-bearing; `proposed_sell_cents` anomaly coverage; all-zero-floors sanity; body-fingerprint on idempotency key; `effective_ts` ms handling | Non-blocking cleanup | **Low** |
| Dunning matrix deliberately NOT wired | `workers/api/src/routes/dunning.ts:28` · REQ-032 | Human review IS the approval (by design) | None (documented decision) | Informational |
| `settlement.executed` fee events synthetic only | `tools/fixtures/gen-qb-journal-month.ts:18` | Settle-execute is CONFIRM-gated do-not-build; exercised only synthetically | Keep unbuilt until CONFIRM | Informational |
| ~~Design/perf CI advisory until WP-10 exit~~ **THE FLIP HAPPENED — verified 2026-08-05 (audit §258)** | `tools/design/design-ci.json` (squint-test) + `tools/harness/playwright-guard.ts` (perf/browser) · REQ-158 | ~~Squint-test + frame-budget report-only (exit 0); blocking with `--strict` after WP-10~~ — **that described the PRE-flip state and was never updated.** Measured: `design-ci.json` reads `{"mode":"blocking", "note":"REQ-158: flipped to blocking at WP-10 exit — pixel law now gates merges."}`, `tools/design/audit.ts` exits **1** on a violation in that mode (proved §252 by planting a shadow, a radius over budget and a raw hex; §257 by drifting the palette), and `gatesFor("merge")` carries `design-audit` in the **non-skippable** group — so the pixel law blocks a merge outright. The perf/browser half is mode-aware rather than report-only: advisory in a bare local run, but under `--mode merge|release` a real regression is FAIL and an absent/empty/all-skipped run is BLOCKED (proved §256 — the same visual regression exits 0 locally and **1** under `--mode merge`, emitting `{"gate":"visual","status":"FAIL","executed":true,"assertions":5}`). The row also named ONE mechanism for two gates; they are separate files | Automated mode-flip, not manual — **and it fired** | **CLOSED** (was Informational; the entry now records the enforced state rather than the pending condition) |
| **Nothing enumerates the register rows whose DoD names a gate** *(filed 2026-08-09, audit §857)* | `genesis/09` `dod_test` column (free text, 287 distinct values) · audit §856 (reported 14) · §857 (union 25) | §856 swept the DoDs naming a CI gate and found **four of nine diverge from the gate they name** — one real gap (REQ-076), one true-but-unpinned (REQ-078, now gated), two met by a different mechanism than the DoD states (REQ-077 unit test, REQ-115 component test). §857 then found **the sweep's own population was a regex yield, not a census**: a wider reading gives 23, the sets are not nested (REQ-077/149 appear in one and not the other), union **25**, examined **9**. Two attempts, two different frames, neither authoritative — because `dod_test` is prose and a DoD may name a gate, a human audit (`(audit)`, REQ-001), or a deferred one | **OWNER DECISION** — a register amendment, so not mine to make. A `dod_kind` column of `gate` / `human` / `deferred` turns this population from a search into a lookup, and makes "every gate-backed DoD can observe its subject" mechanically checkable. Until then the sweep is a dated hand measurement (§856/§857) that decays as rows are added or edited | **Low** — no defect ships from this; the cost is that DoD pointers rot silently, which is how REQ-076 reached `F0-SPEC'D` with an unbuilt half and a gate blind to it |
| **A built, tested resilience property has no register row — 8 citations point at a feature-flag row** *(filed 2026-08-09, audit §866)* | `workers/agents/test/sweep-containment.test.ts:17,57` · `workers/billing/test/claimed-tenants.test.ts:176,186` · `workers/translator/test/transport-dormancy.test.ts:132` · `tools/checks/sweep-containment-coverage.test.ts:7,63` · `genesis/09` REQ-278 (**vNEXT**) · REQ-025 | All eight citations are ONE property — per-tenant failure containment in a tenant-iterating sweep (*"ONE TENANT'S FAILURE MUST NOT KILL THE TICK"*). **REQ-278 is *"Feature flags separate deployment and exposure and write authority and read authority"***, DoD *"fresh environment grants no V2 authority… rollback restores prior reads"* — unrelated. REQ-025, paired in two of the four files, is *"Tenant isolation at DB level"* with DoD *"cross-tenant suite green forever"*: isolation of DATA, not resilience of a SWEEP. **Neither row covers it, and the property is built, shipped and tested** (9 green cases in agents alone, sibling suites in billing and translator, plus a completeness gate over all of them) | **OWNER DECISION** — the work is good and stays; only its home is wrong. Either (a) add a register row for sweep/tick containment and re-point the eight citations, or (b) widen REQ-025 so isolation covers failure containment as well as data. Both are register amendments, so filed not amended (§795's precedent while REQ-289 is uncommitted). Verdict also recorded in the coverage manifest's REQ-278 disposition | **Low** — nothing is broken and nothing is unprotected; the cost is that CLAUDE.md rule 1 (*if it isn't a REQ row, it doesn't get built*) is not satisfied for shipped code, and that when feature flags ARE built their citations will be indistinguishable from these |
| **A git-ignored duplicate test file inflates the authoritative test counts** *(filed 2026-08-09, audit §867)* | `genesis/09` REQ-276 (**vNEXT**) · `.gitignore:34-41` · `tools/checks/invariants.ts:440@findStraySql` (the shadowing `globSync` wrapper) · `tools/checks/test-file-collection.test.ts` | REQ-276's DoD has three clauses and **two hold**: zero tracked duplicates exist, and a seeded TRACKED duplicate fails the gate (force-adding `db/tenant/migrations/0001_ledger_core 3.sql` turns `check:invariants` RED). The third is **false**: one git-ignored duplicate `*.test.ts` moves vitest from **72 files / 1,122 tests to 73 / 1,124**. Being a copy it PASSES, so it never shows as a failure — it silently inflates counts, and **a test-count floor satisfied by a duplicate would hide a deleted test file.** This checkout is iCloud-synced and currently carries 50 such duplicates, all inside `node_modules/.vite` caches; the hazard is latent, not theoretical | **OWNER DECISION** — the row is `vNEXT`, so closing this would extend deferred scope (the existing `.gitignore` rules and the `globSync` wrapper were themselves built without the row advancing, which is the pattern rather than a licence). Either (a) advance REQ-276 and add a duplicate-pattern `exclude` to the vitest configs, or (b) leave it and rely on the disposition note now recording the measurement. Filed, not fixed | **Low–Med** — nothing is broken today and no duplicate exists outside build caches; the exposure is a count gate that can be satisfied by a file nobody wrote |
| **A public schema export has no consumer, so the rule it carries is dead** *(filed 2026-08-10, audit §913)* | `packages/contracts/src/events.ts:295@EventBase` · `packages/contracts/src/index.ts:23` (`export * from "./events.js"`) · REQ-011/016 | `EventBase` is `eventBaseShape` + the offline-dedupe refine. **Neutralising that refine leaves all 326 contracts tests green**, and the reason is not thin coverage — `git grep EventBase` across every `.ts`/`.tsx` returns only its own definition, its `export type`, and a comment added in §912. **Nothing in the repo uses it.** The union members are built from the raw `eventBaseShape`, never from `EventBase` (a Zod discriminated union cannot take a refined object as a member), which is exactly why both `superRefine` blocks restate the rule. It is re-exported from the package index, so it is public API and its removal is a surface change | **OWNER DECISION** — either (a) delete it, since a second definition of a live rule that nobody evaluates is a trap, or (b) keep it as the documented base and say so at the site. A comment now records that it has no consumer, because in §912 **I cited it myself as though its refine were load-bearing** before measuring | **Low** — nothing is unprotected; the two `superRefine` copies carry the rule and are now mutation-proved on all eight branches. The cost is a reader (including me) mistaking a dead export for the enforcement point |
| **A `path:line@symbol` citation anchor is satisfied by a PROSE MENTION, so documenting a symbol can re-validate a stale pointer to it** *(filed 2026-08-10, audit §913)* | `tools/checks/citation-links.ts:287@includes` (`if (text.includes(symbol))`) · `docs/ops/GO-LIVE-CHECKLIST.md` · `docs/audits/2026-08-01-technical-debt-audit.md` | The anchor check is a substring match over the cited line ±2, so any line MENTIONING the symbol satisfies it — including a comment. **Observed twice in one phase, on my own edit**: a 5-line comment added above `EventBase` shifted 8 citations; `verify:docs` caught 6, and the two it missed (`events.ts:290@EventBase`, `events.ts:371@unwitnessed`) were each rescued by a comment naming the symbol — one of them the comment I had just written. **Measured**: of 97 anchored citations resolving via a hit, **18 (19%) are satisfied only by a comment line**, and many are legitimate (`workers/mcp/src/webhooks.ts:299@mark` cites the docblock that STATES the rule). The narrow decidable signal — prose-only in span AND the symbol declared elsewhere in the same file — is exactly **2** repo-wide, both now resolved | **OWNER DECISION** — requiring a declaration would re-judge all **258** content-anchored citations and turn correct docblock citations red, so it is not a unilateral change. Either (a) add the narrow rule (prose-only **and** declared elsewhere in the same file → FAIL), which today would flag 0 after this phase's fixes, or (b) leave the substring match and rely on the ±2 window. Filed, not amended | **Low–Med** — no claim is wrong today; the exposure is that the anchor form, which the record leans on precisely because it survives insertions, is weakest exactly when someone documents the cited symbol |
| **A half-loaded tariff answers `400 VALIDATION_FAILED` — the caller is blamed for the tenant's unloaded config** *(filed 2026-08-10, audit §930)* | `packages/rater/src/compose.ts` (omit-zero rule) · `packages/rater/src/engine.ts` (`Math.max(asRated, minCharge)`) · `packages/contracts/src/rating.ts` (`NonNegCents` is **>= 0**) · `workers/api/test/rate.test.ts` (§930, behaviour pinned) | **MEASURED**: a contract-legal zero-charge tariff (`cwt_cents: 0`, `min_charge_cents: 0`, fsc 0) makes `compose` emit an **empty breakdown** — freight, fsc and zero accessorials are each omitted by design — so `sell = 0` with `lines: []`. `quote.priced`'s penny-parity refine cannot refuse it (Σ [] = 0 === 0); the array `.min(1)` does, and `/v1/rate` answers **400 VALIDATION_FAILED**. **The ledger is protected — no `quote.priced` is recorded (asserted)** — but the diagnosis is inverted: an integrator is told their REQUEST is invalid when the truth is that this tenant's tariff is not loaded. This is the onboarding shape (a half-populated tariff), i.e. the state a new tenant passes THROUGH | **OWNER DECISION** — the rater already speaks the right language for *cannot price*: `UNKNOWN` with a reason (`no_zone`, `no_rate_group`, `missing_physics`), which is REQ-004's *no price on air*. A zero-charge tariff computes a real zero, so it returns PRICED and the refusal happens downstream at the append. Either (a) add a `zero_tariff` UNKNOWN reason so the route declines to price rather than 400ing, or (b) accept the 400 and document it in the onboarding runbook. **(a) is a behaviour change and therefore a register amendment — filed, not built** (CLAUDE.md rule 1) | **Low** — nothing is mispriced and nothing reaches the ledger; the cost is an integrator debugging their own payload while the real fault is an unloaded tariff |

**Resolved after audit-discovery** ~~(verify register status advanced — several still read `*-DISCOVERED`)~~ **— register CORRECTED 2026-07-27, see the closing note below:** positions.ts consent/auth bypass C-1 (REQ-190, **closed WP-09** via `gate-context.ts`) · duplicate-booking C-2 (REQ-191, **closed WP-09**) · Biller trigger-loss recon sweep (REQ-169, **closed WP-11** `recon-sweep.ts`) · held-reply surfacing (REQ-176) · from-name pin (REQ-178) · invoice.issued GL redaction (REQ-179, WP-09 `redact.ts`) · never-widen internal floor (REQ-180, WP-11) · full-tenant export (REQ-010, WP-11) · QB/statement export (WP-11) · held-invoice/Watchtower surface (WP-11) · OTD passport accrual (WP-08/10). These are **code-closed**. ~~The debt is that `genesis/09` status tags may still read open — advance them at the next register review.~~

**Corrected 2026-07-27 (V1 close-out Task 2, commit `7c1a0b4`):** that register debt is **discharged for eleven rows**. REQ-169, REQ-171, REQ-172, REQ-173, REQ-174, REQ-175, REQ-176, REQ-178, REQ-179, REQ-180 and REQ-183 now read `F0-SPEC'D`, each advanced only on a verified pair (a source annotation **and** a passing test, recorded row by row in that commit). Proof: `pnpm check:coverage` → `8 status-drift row(s) … REQ-045, REQ-170, REQ-184, REQ-249, REQ-276, REQ-284, REQ-285, REQ-288` — none of the eleven appears. **REQ-170 is the deliberate exception**: it was advanced with the rest and then reverted to `WP06-DISCOVERED`, because the placed-photo half is still declared unimplemented in the source (`workers/agents/src/biller.ts:594-601@REQ-170`). The remaining seven drift rows are a *different* defect — their `wp` column names no active WP, so a status edit alone cannot advance them; see the register rows in the failures ledger at the bottom of this file.

---

**WP-13 MCP v1 (appended 2026-07-20):**

| Item | Source file / REQ | Nature | Fix | Severity |
|---|---|---|---|---|
| Caps reserve-at-check over-counts on a post-`accept-quote` api failure | `workers/mcp/src/caps.ts` · REQ-105/106 | After the exit-audit fix the confirm-reject + retry facets are closed; only a rare api failure AFTER the reserve over-counts (fails **closed** — refuses a later booking; self-heals at UTC-month rollover) | A settle/release hook after a confirmed booking | **Low** (fail-closed) |
| Immediate cross-method OAuth revocation deferred | `workers/mcp/src/tools/registry.ts` dispatch · REQ-102 | A revoked/suspended pairing's outstanding OAuth tokens still authenticate the non-minting `initialize`/`tools/list` handshake until token TTL (no data exposure; every data/mutation path re-resolves the pairing + fails closed) | One control-DB pairing re-resolve per `dispatch` after grant resolution | **Low** (hygiene) |
| MCP lane cap is destination-zone-based | `workers/mcp/src/caps.ts` · REQ-105 | SEED-1 zoning is dest-based; origin-side lane restriction isn't expressible until origin zoning lands | Origin zoning + a per-leg lane key | **Low** |
| Full DO-backed live-booking E2E is a staging smoke | `workers/mcp/test/quote-book.test.ts` · REQ-101 | The api tenant D1 + `ShipmentSequencer` DO are unseedable in the mcp vitest pool (aux-worker isolation), so the tests prove the MCP layer + no-bypass via a recording fake api; the api gates are proven by the api suites | A cross-worker staging smoke driving a real booking end-to-end | **Low** (test-harness) |

**WP-14 PLG + metering (appended 2026-07-21):**

| Item | Source file / REQ | Nature | Fix | Severity |
|---|---|---|---|---|
| REQ-124 tier numbers are `[HYPOTHESIS]` | `tenants.plan` · REQ-124/130 | The tier *mechanism* (plan-flag + usage meter, no seats) ships; the actual Pro/Scale prices/limits are unset pending the WP-16 pricing re-base | Set final tiers at WP-16 (REQ-130) | **Low** |
| REQ-126 human escalation + SLA unbuilt | REQ-126 | Copilot-first support = the WP-10 copilot; human escalation + the Scale-tier SLA are product/ops deliverables, not code | Stand up the support model at go-live | **Low** (product/ops) |
| Internal credit-append route hardening | ~~`workers/api/src/internal-platform.ts`~~ **corrected 2026-07-27: `workers/api/src/routes/internal-platform.ts`** · REQ-123 | (Task-10 review, Low, internal-only/DARK) `credit-settle` doesn't bind `payload.invoice_id === invoiceId`; `#resolveDb` memo returns before the platform re-check | Bind payment→invoice + add the memo platform re-check before the secret is bound at R4. **RE-VERIFIED 2026-08-06 (audit §385): both claims still hold** — `#resolveDb`'s `if (this.dbHandle) return this.dbHandle;` is still the FIRST line, ahead of the platform check, and `credit-settle` still never relates `paymentEventId` to `invoiceId`. **And the row understates by one: `amountCents` is CALLER-SUPPLIED and never compared to the payment event's amount**, so the coverage test (`total_cents <= ?`) is asserted by the caller rather than derived from the ledger — the third binding the remedy needs. Severity unchanged (`internalGate` + internal/DARK), but the remedy line now names all three bindings instead of two | **Low** (internal/dark) |
| Platform-chart GL account uncovered | `workers/billing/src/credits.ts` `GL_PLATFORM_CREDITS_AR` · REQ-123 | The `_platform` credit chart is a second, unguarded GL chart (customer `CANONICAL_GL_ACCOUNTS` parity untouched) | Add parity coverage if a platform journal export is ever built | **Low** |
| Metering sweep cadence | `workers/billing/src/metering.ts` · REQ-123 | Hourly cron; the OVERWRITE-recompute makes cadence a freshness knob, not a correctness one | Tune cadence at go-live | **Low** (info) |

**WP-15 Overlay/authority (appended 2026-07-22):**

| Item | Source file / REQ | Nature | Fix | Severity |
|---|---|---|---|---|
| Defer-to-mirror suppression is a dormant seam | the 8 `resolveAuthority` consult sites · REQ-030 | The dormant `if(authority==='legacy')` intent-markers do not yet present the legacy value / suppress the native customer-facing output when a module is legacy-authoritative WITH a live mirror — that is a customer-facing CUTOVER behavior, left inert in-repo | Light up per-module at the tenant cutover (§2) | **Low** (cutover; also §2) |
| Dual-control on money promotions unbuilt | `workers/api/src/routes/authority.ts` · REQ-023 | The flip guard records the single deciding admin (co-sign); a second-approver (dual-control) on invoicing/settlement promotes is not built | Candidate R4 hardening (a REQ amendment) | **Low** (candidate) |
| UNKNOWN-while-native liveness unmonitored | `workers/agents/src/watchtower.ts` · REQ-008 | A native module whose legacy mirror goes UNKNOWN (feed stops) is unmonitored — drift is unassessable so there is correctly no auto-fallback, but no alarm either | A low-severity "mirror absent" liveness alarm (later task) | **Low** (fail-safe) |
| DO trusts `parsed.source` (legacy lock is route-layer) | `workers/api/src/do/sequencer.ts` · REQ-030 · REQ-213 | **Consolidated 2026-08-05 (audit §373): this hold was filed TWICE** — once under REQ-030 and once under REQ-213, same file, same mechanism, both rated Low; the duplicate below is struck. **Trigger NARROWED, and the "not exploitable today" claim is now evidence rather than assertion.** Structural enumeration of every `SHIPMENT_SEQ` stub acquisition finds **nine** append seams: seven build the event envelope themselves and pass client data as a NAMED `payload` field (never a spread), so `source` is a sibling key client data cannot reach *by construction*; only **two** accept a whole client-supplied envelope — `routes/events.ts@source` and `routes/internal-platform.ts@source` — and both coerce. Deleting either coercion goes RED in `test/source-aware-ledger.test.ts` (*"the coercion is real, not just a gate side effect"*). So the real trigger is not "a future route" but **a future route that accepts a whole client event envelope** — a much rarer shape, and the one to grep for | Defense-in-depth: a DO-level restriction of legacy to a verified internal marker (a one-line assertion mirroring `platform:true`) makes it structural rather than roster-dependent | **Low** (defense-in-depth; trigger now precise) |
| Duplicate-fallback TOCTOU under concurrent same-tenant sweep | `workers/agents/src/watchtower.ts` · REQ-008 | Two concurrent same-tenant Watchtower sweeps could emit a duplicate (differently-seeded) `authority.flipped{drift}`; end state is still legacy (never re-promote) | Serialize per-tenant if the cron ever runs concurrently. **REASON CORRECTED 2026-08-05 (audit §382) — the verdict stands, its stated reason did not.** "Sequential" is not a property Cloudflare provides: the row for *Cron sweeps double-fire under overlapping ticks* MEASURED overlap on the `*/5` sweeps in this same ledger. What makes THIS one unreachable is **cadence**: `workers/agents/wrangler.toml` is `0 1 * * *`, so an overlap needs a run exceeding 24h — precluded by the Workers wall-clock limit, not by scheduling order. `runWatchtowerSweep`'s only caller is the scheduled handler (`index.ts:507@contain`), and `contain` catches rather than rethrows per sweep, so a platform retry cannot start while the first is still running. **The trigger that revives this: the agents cron cadence shortening below a sweep's worst-case duration** — not "if the cron ever runs concurrently", which reads as hypothetical and is the condition its sibling row already observes | **Low** (benign — precluded by cadence, not by exclusion) |

**WP-16 launch-gate audit (appended 2026-07-22):** full 8-lens REQ-119 swarm, ZERO open Criticals (report: `docs/audits/2026-07-22-wp16-launch-gate-audit.md`). Findings:

| Item | Source file / REQ | Nature | Fix | Severity |
|---|---|---|---|---|
| ~~Invoice-void AR divergence~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `packages/ledger/src/projection/money.ts` · REQ-209 | An `invoice.corrected` void left the `invoices` row `status='issued'` so AR-aging/DSO/Collector overstated a voided invoice while QB/GL read 0 (latent — no producer emits `invoice.corrected` today) | **FIXED** — a `void` InvoiceUpsert flips the row to `status='void', total_cents=0`; reconciliation-tested | **High→resolved** |
| ~~Redaction forward-guard (payment/settlement `division`)~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `packages/ledger/src/redact.ts` · REQ-210 | `payment.received`/`settlement.executed` weren't in INTERNAL_NESTED — a party lens would leak `division` if a real emitter added it (not reachable today) | **FIXED** — both kinds strip `division` | **Low→resolved** |
| ~~CI didn't run `check:coverage`; product fences not git-scoped~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `.github/workflows/ci.yml` · `tools/checks/invariants.ts` · REQ-211 | The 100% register gate ran only on manual `verify`; the stray-SQL/eslint fences false-tripped on a git-ignored sibling project sharing the repo dir | **FIXED** — coverage+authority-coverage in ci.yml, `REQUIRE_DENYLIST` pins identity fail-closed, fences git-aware | **Med→resolved** |
| ~~Append-only guards don't enumerate every UNIQUE surface~~ **CLOSED 2026-08-05 (audit §383) — BOTH halves of the remedy had already shipped and the row stayed open.** The guard: `db/tenant/migrations/0008_append_only_unique_guards.sql` adds `events_guard_ins_unique` and `money_lines_guard_ins_corrects`, enumerating all THREE surfaces this row names (`hash = NEW.hash`, `device_seq = NEW.device_seq`, `corrects_event_id = NEW.corrects_event_id`). The lint: `tools/checks/invariants.ts:243@checkGuardCompleteness` walks every UNIQUE target on every `GUARDED_TABLES` entry and demands a BEFORE INSERT predicate enumerating it. Mutation-proved LIVE — replacing `hash = NEW.hash` with `1 = 0` makes `check:invariants` fail with *"the UNIQUE target (hash) on events has no BEFORE INSERT guard predicate enumerating it"*, attributed to guard-completeness and not to the forward-only lock | `db/tenant/migrations/0003_insert_guards.sql` · REQ-212 | The `events`/`money_lines` BEFORE INSERT guards omit `hash`/`ux_events_device`/`ux_ml_corrects` — a future REPLACE evasion or new UNIQUE could silently rewrite a chained row. NOT exploitable today (plain INSERT aborts; REPLACE lint-banned). **REPLACE-ban scope CORRECTED 2026-08-05 (audit §378): the ban was lint-enforced on the SOURCE surface only.** §266 derived `scanSourceForForbiddenReplace`'s alternation from `GUARDED_TABLES`; the MIGRATION scanner kept FOUR hardcoded `(events|positions|money_lines)` literals (mutation-verb ban, ALTER ban, upsert ban, trigger-body scan), so a fourth append-only table would have been forced into `GUARDED_TABLES` by `checkTableClassification` and then covered by NONE of them. All four now derived, plus the migration surface's mirror of §266's identity test and a shared evasion corpus fed to BOTH surfaces (22 tests). Re-typing the alternation as a subset now fails 15 tests, each naming the dropped table | Ship the drafted complete guard (`.claude/skills/complete-append-only-insert-guards/reference-0006_...sql`) + a WHEN-completeness lint | **Low** (deferred, vNEXT) |
| ~~DO trusts `parsed.source` (route-layer-only legacy lock)~~ **DUPLICATE of the REQ-030 row above — consolidated 2026-08-05 (audit §373)** | `workers/api/src/do/sequencer.ts` · REQ-213 | `source:'legacy'/'edi'/'email'` is locked by per-route coercion; a future route forgetting to coerce reopens the forgery vector. NOT exploitable today (every route covered + tested) | A one-line DO-side assertion (reject a client non-native source unless an internal flag, mirroring `platform:true`) makes it structural | **Low** (deferred, vNEXT) |
| Comms parity counts internal ops notes | `packages/ledger/src/parity.ts` · REQ-008 | `sla-sweep`/`biller` `message.received` notes inflate the native comms count | Fail-safe (only raises drift, never a false green); documented coarse count — future refinement | **Low** (fail-safe) |
| ~~`usage_credits` row-identity/roster divergence at R4~~ **RESOLVED — struck 2026-08-05 (audit §384) to match this row's own verdict column** | `workers/api/src/provision.ts` vs `workers/billing/src/metering.ts` · REQ-123 | Provisioning keyed `uc-<slot>-<period>` and bound the SLOT id as `tenant_id`; the metering sweep keys `<slug>:<period>` + iterated only static `TENANT_SLUGS` — a claimed pool tenant would carry two rows (one permanently empty) and never be swept. Latent while PLG DARK | **FIXED 2026-08-02 (§12 roster half, §13 identity half)** — the sweep fans out over `allTenantSlugs`; provisioning now keys through the shared `usageCreditsIdFor(slug, period)` (`<slug>:<period>`, the sweep's own shape) and binds the SLUG as `tenant_id`. ONE row per (tenant, period) across all three writers. Pinned by `workers/billing/test/claimed-tenants.test.ts` — the roster pin was mutation-proved RED against a fan-out revert, and the identity test asserts the provisioning call site by source | **Low→resolved** |

## 4. Cross-references (this ledger points, does not duplicate)

| Domain | Authoritative doc | What it owns | This ledger adds |
|---|---|---|---|
| Secrets registry | `docs/ops/secrets.md` | `JWT_SECRET`, `IDENTITY_DENYLIST`, device root, `RESEND_API_KEY`, `TEST_SEND_TOKEN`, `EVIDENCE_FROM` + rotation triggers | Gap rows to append: prod secret set, Stripe, Twilio, TSA URL, Mapbox token, `shuddl-backups-{env}` |
| Deploy runbook | `docs/ops/DEPLOYMENT.md` | Paid plan · D1/KV/R2/Queues provisioning · remote migrations · deploy order (api→agents) · domain verify | Reconcile: backup-bucket omission (#DR), prod-sender domain divergence (apex vs `send.` subdomain) |
| DR / backups | `docs/ops/dr-backups.md` | REQ-117/135: nightly snapshots, RPO 24h/RTO 4h, restore drill | Flags: ~~nightly workflow is a **stub**~~ **corrected 2026-08-04 (audit §189): the workflow is WRITTEN, SCHEDULED (`.github/workflows/nightly.yml`, `cron: 0 8 * * *`) and TESTED — it calls `tools/deploy/backup.ts` (26KB, 35 passing cases in `backup.test.ts`), which derives every committed `[env.<env>]` database, refuses to write a manifest over a partial export, and exits 2 = BLOCKED (never green) when credentials are absent. What is missing is the Cloudflare credential (external, F1-A OIDC) and the operator decision to schedule PRODUCTION — the job is staging-only on purpose, documented in the file.** Restore drill **never run**, backup bucket unprovisioned |
| SLO / monitoring | `docs/ops/slo.md` | REQ-114 per-surface targets, error-budget→merge-pause policy | Flags: monitors **not stood up**, no DLQ runbook, public status page has no deploy steps |
| Security debt | `docs/security/threat-model.md` (path corrected 2026-07-27 — there is no `docs/ops/threat-model.md`; every bare `threat-model.md:N` pointer in §2/§3 resolves here) | Asset map, standing mitigations, TSA/tile provenance, suppression list | TSA CMS/cert-chain verify CLOSED (WP-16, `cms.ts`). Still-open: real TSA endpoint + trust anchors (F1 CONFIRM), revocation (deploy/monitoring), status-page server-side geo boundary |
| Pen-test basics (launch gate) | `docs/security/pen-test-basics.md` | REQ-136 STRIDE-per-surface review mapped to the existing proving suites; the WP-16 hardenings; verdict "report clean" for the in-repo perimeter | Depends on §2 deploy items (per-IP edge rate-limit, real TSA anchors, prod secrets, warmup, CORS) + §3 residuals (REQ-170/069/023/030) |
| Release evidence contract (**added 2026-07-27** — it was missing from this index and from `PROJECT-STATE.md`, so the document defining what a gate proves was reachable only by knowing it existed) | `docs/ops/RELEASE-EVIDENCE.md` | REQ-288/118/119: the three rules (evidence binds to an exact SHA · BLOCKED never promotes · generated records are never committed), the per-gate claim→artifact→expiry table, the exit-code contract, the 2026-07-27 V1 sweep with every verdict quoted, holds H1–H10, and the tip-verdict table | This ledger's own hold rows reconcile 1:1 against that sweep (see the hold ↔ gate table above). Note the file is excluded from the annotation scan for the same reason this one is — it records state |
| Status snapshot | `docs/ops/PROJECT-STATE.md` | Per-WP done/parked list, test count | ~~**STALE (2026-07-14, pre-WP-08)** — re-baseline against WP-07→WP-12 before trusting its status/parked lists~~ **Re-baselined 2026-07-27** (`d562d2b`, corrected `75ed83b`): header reads `As of 2026-07-27 · HEAD 7c1a0b4`, covers WP-01..16 + T14/T15, and grades staging against `V2-EXECUTION-FRAMEWORK.md` §9 (**not** yet R2). Trustworthy as of that SHA |
| Fixture vendor-in | `fixtures/manifest.json` · `fixtures/README.md` · `tools/rater/README.md` | 9 pending hash-pinned fixtures (`status:"pending"`) + parity harness contracts | Adds the README-only **live legacy-TMS mirror feed** (REQ-152), absent from the manifest's "PENDING (9)" count |
| Scope + status of record | `genesis/09-REQUIREMENTS-REGISTER.csv` | Every REQ row + register status tags (`F0-SPEC'D`, `vNEXT`, `*-DISCOVERED`, `F0-DEPLOY-NOTE`, `CONFIRM-GATED`) | This ledger's REQ ids trace here; register is source-of-truth for scope |
| Per-WP DoD & follow-ups | `docs/wp/WP-01.md … WP-12.md` | Each close-out's operator line-items + logged follow-ups | This ledger is the merged, deduped roll-up of all twelve |
| Open-audit history | `docs/audits/2026-07-15-full-audit-and-skill-plan.md` | 60-agent audit + fix plan (positions.ts C-1, skills) | C-1/C-2 now **closed WP-09** (recorded above for traceability) |
---

## External holds — named owners (Task 15, REQ-288 · re-audited to the eight-field schema, Task 6, 2026-07-27)

Every one of these is BLOCKED, not failed, and none may be relabelled PASS. ~~`pnpm verify:release` runs
each as a real command and returns its own verdict, so a hold clears the moment the environment is
genuinely fixed — no code change required.~~ **Qualified 2026-07-27:** true of seven rows. `backup-manifest`
is a *declared* hold (`run-gate.ts:79`) because nothing in a release run can synthesize a manifest, and the
last two rows have no gate at any profile. The rest of the sentence stands: a real-command hold clears the
moment the environment is genuinely fixed, with no code change.

**Every proof below was re-run on 2026-07-27 at HEAD `72b2fc2`.** Two rows are the exception and say so: they have **no gate at all**, which is why they are still here after two close-outs. `pnpm verify:release` itself could **not** be run end-to-end this session — it spawns `unit-tests` first, and the workerd runtime is wedged (see the failures ledger); each hold's own gate command was therefore run directly, which is what `verify:release` would have spawned. **(Updated 2026-07-28: the wedge is cleared and `unit-tests` passes, so that particular obstacle is gone — `pnpm verify:merge` ran end-to-end at HEAD `3fc592b`. `verify:release` still returns BLOCKED, but now on the release-infra holds in this very table rather than on the runtime. No hold below cleared, none was added, and none of the ten proofs was re-run at `3fc592b` — they remain evidence about `72b2fc2`/`c09d9a5` under rule 1.)**

| Hold — and what it is blocked on | Severity | Ownership | Proof — command → verdict | Owner | Status | Blocks grade | Evidence expires |
|---|---|---|---|---|---|---|---|
| **Nothing watches dependencies — Dependabot is DISABLED and CI's audit step has been skipped since 2026-07-23** (measured 2026-08-11, audit §976). `vulnerability-alerts` → **404**, `dependabot/alerts` → **403 "disabled for this repository"**, `automated-security-fixes` → **`{"enabled":false}`**. The only other mechanism is `pnpm audit --prod` (`ci.yml` step 16), which §962 measured as SKIPPED in every run since 07-23 because a failed browser gate skips every later step. **Net: zero mechanisms surfacing dependency vulnerabilities** — the four found in §967 (all `hono`, reached via workers/api and workers/mcp) were found by a manual command, not a signal | **Med–High** (a production dependency graph with no watcher) | External (GitHub repo settings) — the in-repo half is already fixed | `gh api repos/shuddl/shuddl-os/automated-security-fixes` → `{"enabled":false,"paused":false}`; `gh api …/dependabot/alerts` → 403 — run 2026-08-11. In-repo: §962 applied `if: !cancelled()` to the audit step, so it runs again the moment CI does | repo owner / GitHub admin | **OPEN — measured.** NOT enabled by the audit. Two distinct choices: **alerts only** (surfaces advisories, opens nothing, no workflow impact) vs **automated security fixes** (opens PRs — in a repo with **0 pull requests ever** that changes the working model, not just a setting) | **R2** | re-run both `gh api` commands; re-verify after any org policy change, and whenever a new production dependency is added **NARROWED 2026-08-11 (audit §1008): the in-repo half is now a RECURRING mechanism, not an event-triggered one.** §962 restored the `ci.yml` audit step's guard, but that step triggers on `pull_request`+`push` — events that have not occurred, so restoring it changed nothing. A `dependency-audit` job now runs `pnpm audit --prod` on `nightly.yml`'s daily schedule, which is the correct cadence for a **world-time** claim (the tree sits still; the advisory database moves). It activates on the push, since nightly runs against `origin/main`. The EXTERNAL half — Dependabot alerts and automated security fixes — is unchanged and still the owner's call. |
| **Workflow `GITHUB_TOKEN` scope is set OUTSIDE the repo, and recorded nowhere** (measured 2026-08-11, audit §975). Neither `ci.yml` nor `nightly.yml` declares a `permissions:` block, so both inherit the account default. **Current value is the SAFE one** — `default_workflow_permissions: "read"`, `can_approve_pull_request_reviews: false` — and the jobs (build/test, `actions/upload-artifact`, the gitleaks scan) work within it. But flipping that default to `write` in the GitHub UI silently grants both workflows `contents: write` with **no file change, no review and no signal**, and `git grep -i 'workflow permission' docs/ops/` returns nothing, so no operator hardening this repo knows the setting exists | **Med** (correct today; unguarded and unrecorded) | External (GitHub repo/account settings) | `gh api repos/shuddl/shuddl-os/actions/permissions/workflow` → **`{"default_workflow_permissions":"read","can_approve_pull_request_reviews":false}`** — run 2026-08-11 | repo owner / GitHub admin | **OPEN — measured, safe today.** In-repo hardening (an explicit `permissions:` block per workflow) NOT applied by the audit: `"read"` grants a SET of read scopes and `permissions: contents: read` grants exactly one, **removing the others** — a narrowing that would hit the artifact upload and a third-party scanner, on a CI about to run for the first time in three weeks. That change should be made by someone who watches the run | **R2** | re-run the `gh api` command; re-verify after any account/org policy change, and whenever a workflow gains a step that writes to the repo |
| **The remote is 1,016 commits / 11 days behind local `main`** (measured 2026-08-11, audit §957). `origin/main` = `0415148` (2026-07-31); local = `92a8c0b`. This audit and every fix in §938–§956 exist on ONE MACHINE. Consequence beyond the backup risk: `ci.yml` triggers on `pull_request` + `push: [main]`, so **CI has evaluated none of this work** — with §956 (no branch protection) the 26-gate apparatus has neither authority nor execution over those commits. The record already says *"merged to main locally"* in every WP close-out, so local-first is deliberate; what is undocumented is the SIZE — at 5 commits that is a preference, at 1,016 it is the only copy | **High** (single point of failure for the whole audit + remediation) | External (owner's push/remote decision) | `git rev-list --count origin/main..HEAD` → **1016**; `gh api repos/shuddl/shuddl-os --jq .pushed_at` → **2026-07-31** — run 2026-08-11 | repo owner | **OPEN — measured.** NOT pushed by the audit: publishing 1,016 commits to shared state is the owner's call, not a side effect of an audit | **R2** (nothing downstream of a merge gate is real until the work is on the branch the gate runs against) | re-run the two commands above; expires the moment a push lands, and re-check after any long local-only stretch. **Second cost, measured 2026-08-11 (audit §958): the governing records anchor their proofs to SHAs that mostly do not exist on the remote — only **5 of 111** resolvable SHAs in the audit (**4%**), 5/16 in RELEASE-EVIDENCE, 23/38 in this file, 7/12 in PROJECT-STATE. Every *"measured at X"* is verifiable here and unresolvable from a clone, which is precisely the property the stamps exist to give. A push makes all four 100% with no other action.** **Third cost, measured 2026-08-11 (audit §966): the `nightly` workflow has run **ten times at ONE commit** (`0415148`, 2026-08-01…08-10, 1 distinct SHA). Its green `orphan-audit` job is not ten verifications but one, re-emitted nightly over byte-identical input; its red `backup` job is one alarm repeated. **A scheduled audit over a frozen commit is a clock, not a check** — and the Actions tab shows it running, which reads as the register↔code diff being enforced against current work when it is enforced against July's.** |
| **`main` has NO branch protection — every gate is advisory in fact** (measured 2026-08-11, audit §956). The in-repo chain from *a test exists* to *its failure stops a merge* is gated at links 1–6 (`test-collection` §709 · `gate-wiring` §941/§949 unconditional-run + `--no-bail` · §656 polarity · §807 merge roster by name · §691 CI invokes `verify:merge`). **Link 7 — a failing check actually BLOCKING the merge — is a GitHub setting and is OFF.** CI runs (`on: pull_request` + `push: [main]`) and computes the full 26-gate verdict; that verdict has no authority. So CLAUDE.md rule 7's *a violation fails the merge*, rule 8's *a cross-tenant read is a build failure* and rule 1's *traceability CI blocks orphans* are true of the COMMAND and untrue of the REPOSITORY | **High** (it is the authority of all 26 gates) | External (GitHub repo settings) | `gh api repos/shuddl/shuddl-os/branches/main/protection` → **`{"message":"Branch not protected","status":"404"}`** — run 2026-08-11 | repo owner / GitHub admin | **OPEN — measured, not assumed** (the audit had recorded it as an assumption: *"the branch protection this repo assumes is outside it"*; it was never queried). NOT enabled by the audit — turning it on changes governance for everyone who pushes and is the owner's call | **R2** (the first grade at which a gate verdict must actually stop something) | re-run the `gh api` command above; re-verify after any repo transfer, rename, visibility change or admin change, and whenever a gate is added to the merge roster |
| **REQ-289 (GTM / `GTM-0`) carries `status: ACTIVE`, which classifies under no disposition — it is BOTH of the board's two FAILs** (measured 2026-08-11, audit §993). The row is the owner's uncommitted GTM register edit; `ACTIVE` is not in the status vocabulary `coverage.ts:disposition()` reads, and `GTM-0` names no active WP, so the row falls to `unclassified` and `check:coverage` + `check:traceability` both fail. Nothing is broken in the product — this is an accounting vocabulary gap: the register's statuses predate the GTM workstream, and REQ-289 is the **only** `GTM-` row in 289 | **Med** (two red gates; zero product impact) | External (owner's register edit — CLAUDE.md forbids the audit from amending `genesis/09`) | `pnpm check:coverage` → `FAIL 1 unaccounted register row(s) … REQ-289: unknown/empty status "ACTIVE" or buildable row whose wp "GTM-0" names no active WP`; `classified 288/289` — run 2026-08-11 | repo owner | **OPEN — measured, and the remedy menu measured with it.** Status-keyed deferrals win before the `wp` column is read, so **one field edit fixes it and `GTM-0` need not change.** The three are NOT interchangeable — each was run through `computeCoverage` at §993: **`F0-DEPLOY-NOTE` → clean** (unaccounted 0, no drift, no confirm-cite); **`vNEXT` → passes but adds a permanent status-drift line** demanding a per-row verdict, because REQ-289 is annotated in source; **`CONFIRM-GATED` → STILL FAILS** (`confirmCited` sets `failed = true`, exit 1) unless a `confirm_citations` entry is also added to `coverage-manifest.json`. Which one is *semantically* right is the owner's call — none of the three means "GTM phase 0", so a new disposition may be the honest answer. **This row is itself the required recorded home** (§993 tightened `scanRecordedHomes` to demand the `Item`/`Source` column, so the three pre-existing "while REQ-289 is uncommitted" prose mentions no longer account for it) | **R2** | expires the moment REQ-289's `status` changes or the row is committed; re-run `pnpm check:coverage` |
| **Production resources** — all five `[env.prod]` scopes are structurally complete, but every id is an all-zero placeholder — declared, not provisioned | High | External (the resources must exist before the ids can be pasted) | `pnpm exec tsx tools/deploy/preflight.ts --env prod` → **19** × `BLOCK placeholder-resource-id`, e.g. `shuddl-api-prod.CONTROL_DB — database_id for shuddl-control-prod is an all-zero placeholder UUID`; run verdict `preflight: BLOCKED — 26 unsatisfied prerequisites. This is not a green.` | infrastructure | ~~BLOCKED~~ **CLEARED 2026-07-30/31** (recorded here 2026-08-01, audit D5 — the reconciliation table below said PROVISIONED + DEPLOYED while this row still read BLOCKED): `provision:prod --apply` created 6 D1 + 2 KV + 1 R2, 19 real ids pasted across the five configs, and `preflight --env prod --state <file>` → **PASS, 72 checks** (`RELEASE-EVIDENCE.md` § *Production preflight — PASS, 2026-07-31*) | ~~**R4**~~ none | ~~when any `[env.prod]` block in the five `wrangler.toml`s changes (SHA), or the moment the D1/KV resources are provisioned — whichever is sooner~~ the provisioning happened; the PASS expires with any `[env.prod]` edit or account-side change (rule 1) |
| **Staging placeholder ids** — all-zero D1 UUIDs (`PLATFORM_TENANT_DB`, both `TENANT_POOL_*`) and the mcp `GRANTS` KV id | High | External (same: provision, then paste) — but the placeholder ids themselves are repo-visible defects | `pnpm exec tsx tools/deploy/preflight.ts --env staging` → **1** × `BLOCK placeholder-resource-id`: `shuddl-mcp-staging.GRANTS` (re-measured 2026-08-05 at `2a8a107`; was 5 — the four D1 ids cleared in `b961dfc`); run verdict `BLOCKED — 8 unsatisfied prerequisites`, of which **7 are account-side facts the run cannot see** without `--state` (4 × `missing-secret`, `no-origins`, `tsa-unconfigured`, `no-backup`) and exactly **1 is repo-visible** | infrastructure | ~~BLOCKED~~ **4 of 5 CLEARED 2026-07-31** (commit `b961dfc` — the three staging D1 ids, shared into billing, are real UUIDs at HEAD); the mcp `GRANTS` KV placeholder (`0000…staging`) REMAINS. **Re-run 2026-08-05 at `2a8a107`** (the trigger this row carried): exactly **1** `placeholder-resource-id`, confirming 4 of 5 cleared and the KV outstanding. Detection is symmetric across kinds — the D1 branch rejects `/^0{8}-/` and the KV branch `/^0+$/` (audit §277), so a re-run cannot under-report either kind. **New trigger:** this verdict dies when `shuddl-mcp-staging.GRANTS` is provisioned, or when any staging resource id changes | **R2** (the KV remainder) | same rule as the prod row, against the `[env.staging]` scopes |
| **Secrets bound** — `JWT_SECRET`, `RESEND_API_KEY`, `STRIPE_WEBHOOK_SECRET`, `PLATFORM_INTERNAL_SECRET` (the last three were required by DEPLOYMENT.md but absent from the checker's contract, so their absence read as satisfied until 2026-07-25) | High | External | Both envs → **4** × `BLOCK missing-secret`, e.g. `JWT_SECRET is not bound in prod (wrangler secret put JWT_SECRET --env prod)`. **Read this verdict precisely:** the same run prints `preflight: no --state supplied — account-side facts (secrets, origins, sender, TSA, backups) are UNPROVEN and therefore blocked.` On staging `JWT_SECRET` and `RESEND_API_KEY` *are in fact bound* (that is how staging sending works — ~~`PROJECT-STATE.md:182`~~ **`PROJECT-STATE.md:244`, repointed 2026-07-28**), so the staging verdict proves **unproven**, not **absent**; the prod verdict is a genuine absence | infrastructure | ~~BLOCKED~~ **prod CLEARED 2026-07-31** — all four bound via `wrangler secret put` and the preflight PASS carried them (`state: PREFLIGHT_STATE sha256:a89a9229dc99`); **staging remains UNPROVEN** (no `--state` run has covered it) | **R2** (staging, unproven) · ~~**R4** (prod, absent)~~ none for prod | on any secret rotation, and on the first staging `preflight --state <file>` run — whichever is sooner. Nothing in-repo can extend it |
| **CORS origins** — no served allowlist; `.example` placeholders remain in `cors.ts` | High | External for the real origins; **Repo** for the placeholder (`workers/api/src/middleware/cors.ts:15-20`) | Both envs → `BLOCK no-origins  cors — no origin allowlist is configured for <env>; every browser surface would be refused` | backend | ~~BLOCKED~~ **CLEARED 2026-07-31** | **R2** | ~~when `cors.ts` changes (SHA) or when the Portal/status deploy origins first exist~~ **The origins now exist AND are served.** `cors.ts` lists all four; `command`/`portal`/`driver`/`track.shuddl.tech` are deployed as assets-only Workers on custom domains; the live cross-origin preflight returns 204 echoing the requesting origin, and `tests/e2e/prod-surface.spec.ts` asserts from a real browser that each surface reaches `api.shuddl.tech` and no other host. Note the preflight's own `no-origins` BLOCK is answered by the `--state`/`PREFLIGHT_STATE` file, which is what carries account-side facts into the checker **RE-VERIFIED 2026-08-11 (audit §998) — the trigger FIRED (`tests/e2e/prod-surface.spec.ts` changed 2026-08-01 against a row dated 2026-07-31) and the evidence HOLDS:** `workers/api/src/middleware/cors.ts:21` still lists all four `shuddl.tech` browser origins, and `tests/e2e/prod-surface.spec.ts:66` still asserts from a real browser that no surface calls a foreign host. Found only when §998 widened the expiry gate to the EXTERNAL table — §995's version read the repo-owned rows alone, so this row's file trigger was invisible to it. |
| **TSA endpoint** — no RFC 3161 authority configured — anchors cannot be timestamped | High | External (an `integrations` row per prod tenant + the F1 CONFIRM) | Both envs → `BLOCK tsa-unconfigured  tsa — no RFC 3161 timestamp authority endpoint is configured` | infrastructure (F1 CONFIRM: owner) | ~~BLOCKED~~ **NARROWED 2026-08-01**: the preflight's `tsa` fact is satisfied (`https://freetsa.org/tsr` in the prod state — the PASS-72 includes it); what REMAINS BLOCKED is the per-tenant `integrations` row `kind='tsa'` + the F1 CONFIRM | **R4** (the remainder) | when the F1 CONFIRM closes or an `integrations` row `kind='tsa'` is inserted. Until then every day is left UNANCHORED, never faked (`anchors.ts:19`) |
| **Backups** — `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` unbound; **no backup exists** | High | External | Two independent verdicts: (1) both envs → `BLOCK no-backup  backups — no backup manifest exists for this environment`; (2) the release profile declares `backup-manifest: BLOCKED — OIDC/external backup credentials (.github/workflows/nightly.yml) — absent in-repo` (`tools/release/run-gate.ts:79` — a *static* hold, the only one left, because nothing in a release run can synthesize a manifest) | infrastructure | ~~BLOCKED~~ **NARROWED 2026-08-01 — "no backup exists" is FALSE at HEAD**: a prod backup ran 2026-07-31 (6 databases, manifest digest `47e4d9ec…`, `backup-manifest` PASS assertions=6) and a staging backup fed the reconciled restore drill (`dr-backups.md`). What REMAINS BLOCKED: the nightly credentials are unbound, so backups do not RECUR — a one-shot backup ages out and the `backup-manifest` release hold stands | **R2** (recurrence) | on the first successful nightly run, or on binding the two credentials — whichever is sooner |
| **Restore drill** — ~~no backup artifact to reconcile~~ **the release profile supplies the gate no snapshots** | High | ~~External~~ **Repo** | `pnpm -s restore:verify -- --mode release` → `##SHUDDL-GATE## {"gate":"restore-verify","status":"BLOCKED","executed":false,"assertions":0,"detail":"no --source/--restored snapshots supplied; a restore has not been reconciled"}`. **Re-diagnosed 2026-07-31: the premise was wrong.** The gate never ran because NOTHING in the repo produced a `LedgerSnapshot` — the two files its `main()` reads had no writer, on any environment. `tools/deploy/snapshot-ledger.ts` now writes them, and a real drill ran (staging tenant-a, a real backup, a scratch D1, deleted afterwards): **PASS, 11 checks, 0 problems** *with `--rows`* — `dr-backups.md:86`. (The drill's first run omitted `--rows` and was reported as an 11-check PASS; it had exercised 9. At HEAD that same invocation reports 9 and BLOCKED, so the over-claim is no longer expressible.) The hold stays BLOCKED because `run-gate` still invokes `restore:verify` with no `--source/--restored`, and because the drill surfaced a second defect: `--rows` walked all rows as ONE chain and so failed on any multi-stream (i.e. any real) tenant — **that second defect is now FIXED** (per-stream walk, re-run PASS, negative controls hold) | on-call | BLOCKED | **R3** | ~~the moment a backup artifact exists~~ ~~and when `verifyChainOfRows` walks per stream~~ **when `run-gate` passes the release profile a snapshot pair, or the profile declares this a manual drill — the ONLY remaining condition** |
| **Deployed smoke** — `SMOKE_API_BASE` + JWT secret **are not bound in this checkout**, so the gate has nothing to drive. **Qualified 2026-07-27 (final cross-task review): this is a visibility gap, not an absence.** A staging environment **is** deployed (`shuddl-api-staging`, `shuddl-agents-staging`) and a smoke has already driven a gated stop over HTTPS to a penny-exact `invoice.issued` — ~~`PROJECT-STATE.md:172-179`~~ **`PROJECT-STATE.md:218-225` (repointed 2026-07-28: the post-reboot re-baseline of that file moved the block)**. Read the verdict's wording accordingly: the gate reads an env var, never the account | Med | External | `pnpm -s smoke:staging -- --mode release` → `staging-smoke: BLOCKED — SMOKE_API_BASE is not set — there is no deployed environment to smoke` / `nothing was exercised; this is a named external hold, not a pass.` (verbatim; the phrase "no deployed environment" is the tool's wording for "no base URL was supplied to me") | infrastructure | BLOCKED | **R2** | ~~on the first deploy that publishes a reachable base URL~~ **on binding `SMOKE_API_BASE` + the staging JWT secret here, or on any redeploy of the environment already running** |
| **On-call rota** — no human is named; alerts have no recipient | Med | External | **NO GATE EXISTS.** No command in this repo can verify that a human is named — the original "Gate: all SLO alerts" column named a policy, not a runnable check. The claim rests on two documents: `docs/ops/slo.md:25` (*"The rota is **not yet staffed** — naming a human is a launch prerequisite"*) and `docs/ops/dr-backups.md:76`. Recorded as a finding in its own right: **this is a hold with no gate** | founder | BLOCKED | **R3** | never expires automatically — a document assertion, not a measurement. Re-read both lines at every close-out |
| **7-year monthly snapshots** — retention is 30 days; the REQ-116 archive tier is unimplemented **— and note the TIME PROPERTY (audit §167): this hold is the only one in this ledger whose cost is partly IRREVERSIBLE. Scope first, because it is narrower than it reads: live events are append-only and never deleted (I3), and evidence bytes ARE retained seven years today (`retentionClassFor` gives POD and `tsa_receipt` a 7yr class and the sweep excludes them by kind AND by duration). What is missing is only the MONTHLY POINT-IN-TIME DATABASE SNAPSHOT. But a snapshot of an elapsed month cannot be created retroactively once that month's nightly artifacts age past the 30-day window — so every month that passes without the archive tier is one snapshot that will never exist. Low is the right grade for operational recovery; the grade does not capture that waiting costs something unrecoverable rather than nothing.** | Low | External | **NO GATE EXISTS** (the original row's gate column was literally `—`). Rests on `docs/ops/dr-backups.md:23-24`: *"Monthly snapshots kept 7 years (matching the POD lifecycle, REQ-116) are **not yet implemented** — the nightly artifact retention is 30 days."* Second hold with no gate | infrastructure | BLOCKED | **R4** | document assertion; re-read `dr-backups.md:23-24` at every close-out |

### Hold ↔ gate reconciliation (Task 6 Step 3 — run 2026-07-27, HEAD `72b2fc2`)

**No gate verdict without a hold.** Every `BLOCK` either preflight emits maps to exactly one row above:

| Preflight verdict | prod | staging | Hold row |
|---|---|---|---|
| `placeholder-resource-id` | 19 | — | ~~Production resources~~ **PROVISIONED + DEPLOYED 2026-07-30/31** |
| `placeholder-resource-id` | — | 5 | Staging placeholder ids |
| `missing-secret` | 4 | 4 | Secrets bound |
| `no-origins` | 1 | 1 | CORS origins |
| `tsa-unconfigured` | 1 | 1 | TSA endpoint |
| `no-backup` | 1 | 1 | Backups |
| **total** | **26** | **12** | — |

Both totals match the aggregate the tool prints, so no BLOCK is unaccounted for. The three non-preflight release gates each map to exactly one row as well: `restore-verify` → Restore drill, `staging-smoke` → Deployed smoke, `backup-manifest` → Backups (which is why Backups cites two verdicts).

**No hold without a gate — FAILS for two rows.** *On-call rota* and *7-year monthly snapshots* have no executable gate at all; nothing in this repo can observe either fact. They are true, they are named, and they are unmeasurable, so they can only be re-read, never re-run. Closing that asymmetry (a gate that reads a named-owner file; a gate that asserts the archive tier exists) is itself deferred work.

**Re-confirmed 2026-07-27 at HEAD `c09d9a5` (the V1 evidence sweep, Task 7).** The staging column was re-run — `pnpm preflight -- --mode release` → `deploy-preflight … "12 blocked: placeholder-resource-id, missing-secret, no-origins, tsa-unconfigured, no-backup"`, with the same 5/4/1/1/1 split — and `restore-verify` and `staging-smoke` returned the same two BLOCKED sentinels quoted in their rows above. No hold cleared, no hold was added, and no verdict changed. The prod column was not re-run this session; it is unchanged since `72b2fc2` because no `[env.prod]` block has been touched. Full sweep, including every gate that could **not** be run and why: [`RELEASE-EVIDENCE.md`](./RELEASE-EVIDENCE.md) § *Sweep — 2026-07-27*.

## Repository-owned failures & debt (NOT external holds)

These are ours: reproducible from this checkout, closable by a commit. Same eight fields.

| Item | Severity | Ownership | Proof — command → verdict | Owner | Status | Blocks grade | Evidence expires |
|---|---|---|---|---|---|---|---|
| **`CLAUDE.md` rule 6 names four fixture gates; one — *routes ±10%* — has no fixture, no checker and no register row** | Low | Repo (the governing doc) | audit §344 — the other three all resolve: *legacy-export replay ±2%* → `legacy-export-replay` (pending, `gates: WP-02/04/15 ±2% aggregate` — the ±2% only, routes not mentioned); *QB export to the penny* → `qb-journal-month` (**vendored**, proved §342); *airplane-mode soak* → `airplane-soak` (**in-repo-test**, proved §343 with 47 REDs). For *routes ±10%*: no entry among the 17 manifest fixtures, ~~no tolerance implementation in `tools/` or `packages/`~~ **EVIDENCE CORRECTED 2026-08-10 (audit §932): there IS one.** `packages/ledger/src/parity.ts@PARITY_TOLERANCE_BPS` is a typed, tested map whose own header says the per-module tolerances **ARE** rule 6's replay gates, naming *`rating ±10% (1000 bps) — the routes replay gate (a rated quote is the "routes" family)`*, and `parity.test.ts` pins `rating: 1_000`. **This does not resolve the decision, it moves it**: that constant is the LIVE parity overlay's tolerance (native vs legacy mirror), whereas rule 6 enumerates FIXTURE replay gates compared against vendored fixtures. So either the comment is right and rule 6's *routes ±10%* is already implemented by the rating overlay (making the checklist's premise wrong), or the comment over-claims by calling a live-overlay tolerance a fixture replay gate. **Third option, previously unconsidered.** Also unchanged: no `routes` entry among the 17 manifest fixtures, and no register row for route or mileage accuracy (the six route/mileage hits are UI progress-fill, escrow review, GTM, status links, host routing and positions gates — none about route comparison). | register owner | **OPEN — needs a decision, not a fix**: either the criterion is part of the legacy-export replay and rule 6 over-enumerates, or it is unscoped work needing a REQ row (`ADD A ROW FIRST`) | no — the merge aggregate already BLOCKS on the legacy-export replay being pending, so nothing ships on this either way | when a routes fixture or REQ row appears, when rule 6 drops the clause, or when the legacy-export replay's `gates` field is amended to cover routes explicitly |
| **`CLAUDE.md`'s CONFIRM-GATED prohibition names 3 items; the register has 14** — the rule itself is general and CORRECT (*"anything whose REQ row says CONFIRM-GATED while the CONFIRM is open"*), and its parenthetical *(Direct merchant, voice recording, escrow settle)* is illustrative. Nothing says so, and an illustrative list in a prohibition reads as the prohibition's scope. | Low | Repo (the governing doc) | audit §338 — the three named map to real rows (REQ-104, REQ-096, REQ-033); the register carries ~~**14**~~ **15** `CONFIRM-GATED` rows *(re-counted 2026-08-10, audit §932 — the row's own expiry trigger said "when the count changes materially"; it moved by one, which does not change the argument but does date the evidence)*, including REQ-138 (ToS/Privacy/DPA for PLG signup) and REQ-130 (pricing re-based before public launch), which a reader taking the three as complete would not know are gated. **Not a defect — the rule reaches all 14; the risk is a builder who reads the list and not the register.** | register owner | **OPEN — needs one word ("e.g.") or a pointer, not a fix** | none (a legibility gap; source-of-truth order already puts the register first, so a careful reader is safe) | when the parenthetical is marked illustrative, or replaced by a pointer to the register's `CONFIRM-GATED` rows, or the count changes materially |
| **`CLAUDE.md`'s hard-budget line mixes CEILINGS and EXACT COUNTS without saying which is which** — seven numbers read as one kind. Two are ceilings (`≤22 tables`, `12 canonical views`) and five are exact (3 surfaces · 35 event kinds · 5 colour tokens · 2 font families · 0 shadows). The tables entry labels itself in eight words — *"21 used; the spare requires a written deletion"* — and the views entry does not. | Low | Repo (the governing doc) | audit §336 — all seven diffed against their implementations: five exact matches; `CANONICAL_VIEWS` declares **11** against `MAX_CANONICAL_VIEWS = 12` (a spare, exactly like the tables); and the colour count reads 6 hex declarations against a budgeted 5 because `--field-on-dark` is a dark-mode variant the gate deliberately excludes. **Zero defects — every implementation is compliant.** | register owner | **OPEN — needs eight words, not a fix** (adding "(11 used, one spare)" to the views entry is a statement about scope, which belongs to the register owner, not this loop) | none (a legibility gap — a reader auditing "12 canonical views" against a registry of 11 sees a discrepancy that does not exist, and pays two commands to learn it does not) | when the views entry states its usage like the tables entry does, or when a future budget line adopts a uniform convention for ceiling-vs-count |
| **REQ-030 authority-registry completeness has no recurring owner** — `check:authority-coverage` is a static scan over a HAND-LISTED roster (`AUTHORITATIVE_FILES`, 5 modules / 8 files). Its own header states the limit: registration is MANUAL and *"a NEW emitter in a NEW file passes for free until a human adds it"*, with completeness deferred to *"the WP-15 Task-10 exit audit"* — which has run and closed. So the registry was owned once, by a past event, and is owned by nothing recurring. | Medium | **Repository** (process, not code) | `pnpm check:authority-coverage` → exit 0 (it verifies the 8 REGISTERED pairs consult `resolveAuthority`; it cannot see an unregistered emitter). Completeness re-derived by hand at `501ea9d`: 12 files reference an authoritative kind, 8 registered, **4 correctly excluded** — `routes/kpis.ts` + `mcp/src/webhooks.ts` are readers, `routes/events.ts` is the generic append route whose gated-kind consult lives in the sequencer DO (registered), and `billing/src/credits.ts` emits on the reserved `_platform` revenue tenant (SaaS revenue, not tenant freight authority). | register owner | ~~OPEN~~ **TRIPWIRED 2026-08-05 (audit §326)** — registry COMPLETE, and the population is now PINNED by `tools/checks/authority-population.test.ts` (merge profile): 12 kind-referencing files, 8 registered + 4 adjudicated exclusions each carrying a stated reason. A 13th file fails the merge with the re-adjudication question attached. Ownership is no longer unassigned — it is the gate's | No (nothing is currently bypassed) | **RE-VERIFIED 2026-08-10 (audit §935) — the evidence HOLDS.** Re-ran the comparison: **13** files reference an authoritative kind AND carry an append seam; **8 consult `resolveAuthority`** (the registered set, unchanged) and the **5 that do not are each correctly excluded, by emitted kind rather than by roster**: `booking.ts` emits `booking.created`, `sla-sweep.ts` emits `message.received` (not `.sent`), `portal-actions.ts` emits `quote.accepted`/`message.received`, `credits.ts` emits `invoice.issued` against the **PLATFORM** tenant (not a tenant DB, so outside this row's scope), and `routes/events.ts` is the generic append route this row already explains. **Two proxies nearly reported decay**: counting files that MENTION a kind gives 28, and adding an append seam gives 13 — both look like drift against the recorded 12, and only reading what each file EMITS settles it. **Expires when** any new file appends `quote.priced`, `invoice.issued`, `settlement.*` or `message.sent` against a TENANT database. Re-verify by re-running the twelve-vs-eight comparison — one grep, four file reads (audit §313/§314). A discovery gate was considered and rejected: a kind-mention rule raises 4 false positives out of 4 candidates, because "authoritative" is semantic, not greppable. |
| ~~`anchors/run` backfill flake~~ **FIXED 2026-07-27** — original text preserved: *"INTERMITTENT, pre-dates this branch. `POST /v1/anchors/run` 500s in ~half of full-suite runs of `workers/api` and passes in isolation. Root cause: the route backfills every unanchored day in the tenant DB, which under `isolatedStorage:false` + `singleWorker:true` holds whatever every other test file wrote, so a per-day exception escapes `anchorDay` and fails the whole request. ~~Two fixes are available and neither was taken here (out of scope for the T14/T15 plan): scope the run test to its own tenant (the precedent in 43661ff), or catch per-day in `runDailyAnchor` and push to the `failed[]` array the contract already has.~~"* **Resolution (Task 1): the second fix was taken, and more** — per-day containment (`1aa0db5`), a durable contained `anchor.build_failed` record with escalation (`8f24e68`, `packages/ledger/src/anchor.ts:317`), three recorder guards + a narrowed contained region (`501330c`), and the actual root cause: a **non-hex hash at epoch 0** in `workers/api/test/driver-manifest.test.ts` | High (was) → resolved | Repo | `git log --oneline` → `1aa0db5`, `8f24e68`, `501330c`, `6e33e89`, approved after three review rounds. ~~**The fix has NOT been observed green in this environment**: `pnpm test` and `pnpm -F @shuddl/ledger test` cannot execute — `workerd` is wedged (next row). That is why this row carries a †~~ **OBSERVED GREEN 2026-07-28 at HEAD `3fc592b`, after the reboot cleared the wedge:** `pnpm -F @shuddl/ledger test` → **34 files / 598 tests PASS**; `pnpm -F @shuddl/api test` → **65 files / 719 tests PASS, three consecutive runs**; the full `pnpm test` → **258 files / 3,243 tests PASS** across every workspace and the tools suite. The original defect failed roughly four runs in five, so three consecutive clean runs of the suite that carried it is the disposition this branch itself asked for. The † is removed | backend | ~~FIXED †~~ **FIXED** (2026-07-28) | — (was R1) | ~~**already expired for the verification half.** Re-run `pnpm -F @shuddl/ledger test` and `pnpm -F @shuddl/api test` after a reboot; until someone does, "fixed" rests on the diff and the review, not on a green~~ **Re-run and observed 2026-07-28; "fixed" now rests on a green, not on the diff and the review.** Expires when `packages/ledger/src/anchor.ts`, `workers/api/src/routes/anchors.ts` or `workers/api/test/driver-manifest.test.ts` changes — the run is evidence about `3fc592b` and nothing else **RE-VERIFIED 2026-08-11 (audit §995) — this row expired the DAY AFTER it was written and stayed expired for two weeks.** `packages/ledger/src/anchor.ts` and `workers/api/src/routes/anchors.ts` both moved 2026-07-29 against a row dated 2026-07-28; `workers/api/test/driver-manifest.test.ts` moved 2026-08-04. Re-ran all three subjects at HEAD: `pnpm -F @shuddl/ledger exec vitest run anchor` → **19/19 PASS**, `pnpm -F @shuddl/api exec vitest run anchors driver-manifest` → **17/17 PASS** (2 files). The evidence HOLDS. Found by the §995 gate, NOT by the hand-sweep in the same session that re-checked five sibling rows and missed this one. |
| ~~**The `workerd` runtime is wedged on this machine** — `packages/ledger`, all five `workers/*` suites, `pnpm test`, `test:acceptance`, `verify:dev`, `verify:merge` and `verify:release` could not be run during this entire close-out~~ **RESOLVED 2026-07-28 by reboot — original text preserved above.** Every one of those suites and gates now runs. **Read this before the next close-out: the condition RECURS.** It is minted by interrupted `vitest-pool-workers` runs, it is not load, `kill -9` does not touch a `UE` process, and nothing in this repository can clear it. The remedy is a reboot, and the reboot is now tested rather than assumed. Full diagnostic kept in `docs/ops/PROJECT-STATE.md` § *Environment gotchas* | Med (environment) | External (the machine, not the code) | ~~`workerd --version` never returns. Diagnostic: `ps -eo stat,command \| grep '[w]orkerd' \| grep -c '^UE'` → **127** (of 128 workerd processes) stuck in uninterruptible-exit, with load average 5.18 on an otherwise idle box. Remedy: **reboot**~~ **Post-reboot, 2026-07-28 at `3fc592b`: `workerd --version` → `workerd 2025-10-11`, returned instantly; `ps -eo stat,command \| grep '[w]orkerd' \| grep -c '^UE'` → **0**.** The suites followed: `packages/ledger` 598 tests PASS, `workers/api` 719 tests PASS ×3, `pnpm test` 3,243 tests PASS, `pnpm test:acceptance` green, `pnpm verify:merge` executed end-to-end and wrote a record | on-call / founder | ~~BLOCKED~~ **RESOLVED** (2026-07-28) | ~~**R1** (the authoritative merge gate cannot be run at all)~~ **none — the merge gate runs; it now BLOCKS on absent private fixtures instead, which is a different row** | ~~at reboot. Every † in this file expires with it~~ **the reboot happened; every † expired with it, as written.** This resolution expires the moment a `workerd` process is seen in `UE` again — re-run the diagnostic at the start of any session that must produce ledger or Worker evidence |
| ~~**A stale anomaly marker can outlive a day that anchors** — if `clearAnchorFailures` rejects *after* the `documents` row commits, the markers survive; the next run sees the documents row, marks the day `skipped`, `anchorDay` is never called again, and the markers can never clear or escalate~~ **FIXED 2026-07-29, commit `cadb115`** — the row's reasoning was re-verified against the code before anything was written, including the cheaper fix that was **REJECTED**: clearing the markers *before* the `documents` row would close the window, but it makes anchoring hostage to the `anomalies` table — a persistent fault there would stop a witnessable day from ever being witnessed, and recovery bookkeeping must never gate anchoring. What landed instead is a run-level backstop (`packages/ledger/src/anchor.ts:470@sweepAnchoredDayMarkers`, called at `packages/ledger/src/anchor.ts:316@sweepAnchoredDayMarkers`): ONE statement per run that deletes every anchor marker whose day already carries its `tsa_receipt` row. It keys on the anchored-ness of the DAY, not on this run's outcome, so it collects a marker whichever run stranded it — including a day beyond `MAX_DAYS_PER_RUN` — and its id predicate is built from the same `idPrefix` constants that write the marker, re-deriving the tenant, so it can only ever delete a row the writer itself would have produced | Low | Repo | ~~Source read, no command reproduces it: `packages/ledger/src/anchor.ts:216` awaits `clearAnchorFailures(...).catch(...)`, logs, and returns `"anchored"`; `anchor.ts:245` then skips any day already carrying a `tsa_receipt` documents row. Raised by an adversarial reviewer in Task 1, judged real, consciously deferred. **UNKNOWN** how reachable it is in practice — it needs a D1 fault confined to that one statement, and nothing measures that~~ **A command reproduces it now** — the fault this row called unmeasurable is measured by a seam that faults exactly one statement role. `pnpm -F @shuddl/ledger test` → **34 files / 604 tests PASS**, including *"a marker that OUTLIVES its clear is swept on a later run"* (`packages/ledger/test/anchor.test.ts:440`): it anchors a day with every `DELETE FROM anomalies` faulted, asserts the day really is witnessed AND the stale marker really is standing, then asserts the next healthy run sweeps it. Teeth: without the sweep that third run still finds the marker (the failing test that opened the fix); with the `EXISTS` clause deleted **two** tests fail — the new precision test (`packages/ledger/test/anchor.test.ts:474`, a still-failing day must keep its alarm to escalate) and the pre-existing `anchor.build_failed` containment test | backend | ~~OPEN~~ **FIXED** (2026-07-29) | none (Low; R1 requires zero Critical/High) | when `packages/ledger/src/anchor.ts` changes — the green above is evidence about `08e2156` and nothing else |
| ~~**`runDailyAnchor`'s two pre-loop queries are uncontained** — so `POST /v1/anchors/run` can still 500 on a fault there. **The claim "the endpoint cannot 500" is false**; what shipped was *per-day* containment~~ **FIXED 2026-07-29, commit `08e2156`** — the claim is now true, and the fix is deliberately more than a `try`/`catch`: bare containment would return an empty `{anchored:[],skipped:[],failed:[]}`, which asserts *"nothing to anchor"* — a different fact from *"no day was examined"*, and a false one. The two queries are contained as ONE stage-tagged region and the result gains `scan_failed: {stage, error}`, present IFF no day was examined (`packages/ledger/src/anchor.ts:275@scan_failed`; the wire shape is `packages/contracts/src/anchors.ts:70`, strict). The fault is reported three ways, because the cron discards the result: returned, logged, and durably recorded as an escalating `anchor.scan_failed` alarm (`packages/ledger/src/anchor.ts:431@noteScanFailure`) visible to the default `GET /v1/watchtower?status=open` — a run that anchors NOTHING was previously quieter than a single day failing. It clears on any run that CAN determine its work, including the two early exits (`packages/ledger/src/anchor.ts:283@clearScanFailures`, outside the day work on purpose: a tenant that keeps taking an early exit would otherwise strand a critical alarm forever). **A second defect fell with it:** `runAllTenants` in `workers/agents/src/index.ts` documents per-tenant containment but has no `try`/`catch`, so this same fault abandoned every remaining tenant after the first faulting one. **Deliberately NOT changed — the endpoint still answers 200:** the body reports what the RUN did, the contract already reports per-day failure inside a 200 (`failed[]`), and minting a new code + status for "the ledger could not be read" would amend the stable error-code enum (`packages/contracts/src/errors.ts`) to say strictly less than the body already says | Med (it corrects a claim a reader would otherwise trust) | Repo | ~~Source read: `packages/ledger/src/anchor.ts:226-228` (the `MIN(recorded_at)` union) and `:237-239` (the anchored-days sweep) both `await db.prepare(...)` **outside** the per-day try/catch that `1aa0db5` introduced~~ **Run, not read.** `pnpm -F @shuddl/ledger test` → **34 files / 604 tests PASS**; `pnpm -F @shuddl/api test` → **65 files / 719 tests PASS**. Three new ledger tests fault each scan query in turn (`packages/ledger/test/anchor.test.ts:372,400,419`); before the fix all three failed by *throwing `D1_DOWN` out of `runDailyAnchor`* — the 500 itself, observed rather than inferred. One of them asserts the run anchors NOTHING when the anchored-days set is unreadable: proceeding blind would re-run `anchorDay` for days already anchored, and `anchorDay` puts to R2 before its `INSERT OR IGNORE`, so it would overwrite a stored TSA receipt with a freshly stamped one and destroy the witness time that IS the evidence | backend | ~~OPEN~~ **FIXED** (2026-07-29) | ~~none by grade — but any doc that claims the endpoint is 500-proof is wrong until this closes~~ none — the code and the claim now agree; a reader may trust the endpoint not to 500 | when `packages/ledger/src/anchor.ts` or `workers/api/src/routes/anchors.ts` changes |
| ~~**The anchor test seam discriminates on a SQL substring and stubs only `.bind()`** — a future unbound `anomalies` statement would throw a `TypeError` that the guard swallows, leaving the test green for the wrong reason~~ **FIXED 2026-07-29, commit `05f140a`** — the hazard was demonstrated before it was fixed rather than argued: an unbound `anomalies` read in the source made the production guard log `TypeError: db.prepare(...).first is not a function` while **all 13 tests passed**. The seam now discriminates on a statement's ROLE — verb AND table, anchored at the head of the statement, one shared matcher table (`packages/ledger/test/anchor.test.ts:90`) — implements the full statement surface on bound AND unbound statements, so an unbound execution rejects like any other fault instead of throwing a `TypeError` into a guard, and carries three counters that every consumer asserts: `faults > 0` (the seam matched something at all), `unbound === 0` (a `?`-parameterised statement executed without binding it — real D1 refuses that too, so a non-zero count is a genuine caller bug rather than a seam artifact) and `unmatched === 0`, the tripwire for a role matcher that has gone blind. The loose substring is not gone: it is demoted from deciding the fault to reporting the miss | Low | Repo | ~~Source read: `packages/ledger/test/anchor.test.ts:60` (`anomaliesBrokenDb()`) and `:69` (`tsaMarkerBrokenDb()`), consumed at `:251` and `:266`~~ **Proved by mutation, twice, each mutation restored.** (1) An unbound parameterised `anomalies` read → **11 tests fail** — the seam's `unbound` counter plus every real-DB escalation test, since D1 rejects an unbound parameterised statement as well. (2) `FROM main.anomalies` in the one statement this seam prepares → **exactly 2 tests fail, and only on the tripwire assertion**: every behavioural assertion in both tests still passed, which is precisely the "green for the wrong reason" this row named. `pnpm -F @shuddl/ledger test` → **34 files / 604 tests PASS** with both mutations reverted | assurance | ~~OPEN~~ **FIXED** (2026-07-29) | none (Low) | when `packages/ledger/test/anchor.test.ts` changes **RE-VERIFIED 2026-08-11 (audit §995) — the trigger FIRED (the file changed 2026-08-04) and the evidence HOLDS:** the two fault seams live at `packages/ledger/test/anchor.test.ts:158@anomaliesBrokenDb` and `:163@tsaMarkerBrokenDb`, consumed at `:332` and `:351`. The struck citation to `:60`/`:69` is superseded text, deliberately frozen — not repointed. |
| ~~**`recordAnchorFailure`'s `ON CONFLICT` does not reset `status='open'`** while `raiseAlarm` does — and `raiseAlarm`'s comment claims to mirror it. An operator who resolves an anchor marker gets a row that keeps re-failing while reading `resolved`, invisible to the default `GET /v1/watchtower?status=open`. Pre-existing for the TSA path; now extended to a second failure kind (`anchor.build_failed`)~~ **FIXED 2026-07-29, commit `579a167`** — `, status = 'open'` is now part of the UPDATE (`packages/ledger/src/anchor.ts:403@excluded.detail`), so a re-failing condition comes back to the default ops lens and keeps counting. `raiseAlarm`'s comment in `workers/agents/src/watchtower.ts` is true rather than aspirational: the two SQL fragments now agree. The operator-facing semantics DID change on the pre-existing TSA path — that is what was authorised — and the change is bounded: only the kind that re-failed re-opens, so a re-raise is never a blanket un-resolve | Med | Repo | ~~Exact SQL diff. `packages/ledger/src/anchor.ts:344` → `... ON CONFLICT(id) DO UPDATE SET severity = excluded.severity, detail = excluded.detail` (no `status`). `workers/agents/src/watchtower.ts:161` → `... DO UPDATE SET severity = excluded.severity, detail = excluded.detail, status = 'open'`. `watchtower.ts:146` calls itself *"the anchor.ts self-clearing UPSERT pattern"* — it is not the same pattern~~ **Behaviour, not a diff.** `pnpm -F @shuddl/ledger test` → **34 files / 604 tests PASS**, including *"a marker an operator RESOLVED re-opens when its condition re-fails"* (`packages/ledger/test/anchor.test.ts:501`): it raises both anchor kinds, resolves both the way an operator would, re-fails ONE, and asserts that kind is back to `open` with `consecutive: 2` while the other stays `resolved`. Written first and observed red for the right reason (`expected 'resolved' to be 'open'`); the fix is the one clause that turns it green. No test asserted the old behaviour, and no `anomalies` consumer depended on it — searched before the change. `pnpm -F @shuddl/agents-worker test` → **16 files / 90 tests PASS** (the `raiseAlarm` side is unchanged) | backend | ~~OPEN~~ **FIXED** (2026-07-29) | ~~**R2** (an alarm that hides itself is a monitoring defect, and R2 requires alerts)~~ none — an alarm that hid itself is the defect this closed | when either file changes |
| ~~**The release-record binding is self-satisfied** — `evaluateEvidence` SHA/environment/fixtures/deployment mismatch checks can never fire~~ **FIXED 2026-08-02 §16** | Low (latent) | Repo | `tools/release/run-gate.ts:142-144` builds the `PromotionContext` out of the `record` it just constructed, so every comparison compares a value with itself. `git grep -n "evaluateEvidence" -- . ':(exclude)docs'` → the only non-test consumer is `run-gate.ts:144`. Latent, **not live**: no promote step exists yet. Already recorded at `docs/ops/RELEASE-EVIDENCE.md:171` and `docs/security/threat-model.md:46` **The wiring was the whole defect** — the detection logic in `evidence.ts` is real and covered by `tools/release/evidence.test.ts`; only the one live consumer made it inert. The context is now RE-OBSERVED after the gates run (`gitHead()`, `fixturesHash()`, and the two env vars read afresh) instead of copied from the record, which turns the dead comparisons into a genuine STALENESS guard: a commit landing, a fixtures-manifest change, or an environment/deployment move DURING a multi-minute gate run now invalidates the record instead of being certified by it. In CI the checkout is fixed, so a mismatch is always a true positive. Pinned by a source-reading test in `tools/release/run-gate.test.ts` (main() is not exported and shells out to git, so the wiring is not unit-testable from outside) and mutation-proved: restoring the copied form fails 3 of its 4 assertions. | assurance | **FIXED** (2026-08-02) | none — the latent High is retired ahead of the promote step rather than left as a tripwire for it | when `tools/release/run-gate.ts` or `evidence.ts` changes, or the day a promote step is added **RE-VERIFIED 2026-08-11 (audit §995) — the trigger FIRED (`run-gate.ts` changed 2026-08-07) and the evidence HOLDS:** the context is still built INDEPENDENTLY of the record at `tools/release/run-gate.ts:194@context` (`gitHead()`, `fixturesHash()`, env-derived environment/deployment), so every comparison can still fire. |
| **Cron sweeps double-fire under overlapping ticks** — Cloudflare gives `scheduled()` no mutual exclusion, and both `*/5` sweeps use a presence CHECK, not a claim. MEASURED by driving two sweeps concurrently: `run214Sweep` → **2 transmits of the same 214**, partner counter 41 → **43** (so the two interchanges carry DIFFERENT ISA13/GS06 and the partner CANNOT dedupe them); `runWebhookSweep` → **2 deliveries** of one event. Both suites test only SEQUENTIAL re-runs ("a second run transmits NOTHING"), which is why neither caught it. **DORMANT** — `NotConfiguredTransport` transmits nothing and `NotConfiguredWebhookTransport` refuses by construction. **TRIPWIRED 2026-08-05 (audit §379): that dormancy was asserted by NOBODY on the EDI half.** `transportFor` (the composition root) and `NotConfiguredTransport` both had zero test references, so the single fact keeping a High-severity double-send latent was this sentence. `workers/translator/test/transport-dormancy.test.ts` now fails the moment a live adapter is bound, with this hold quoted in the failure message — mutation-proved by binding `RecordingTransport` (3 RED). **Both halves tripwired as of audit §380** — `workers/mcp/test/webhook-dormancy.test.ts` pins all THREE fail-closed deps of `webhookDepsFor` (transport, event source, secret resolver), each asserted separately so a partial wiring cannot pass by leaning on its neighbours, plus a non-vacuity check on the deps that ARE wired. Mutation-proved by swapping a live event source (1 RED). | High (latent) | Repo | Two concurrent `run214Sweep`/`runWebhookSweep` calls sharing a marker store → 2 external sends (audit §236) | Owner (design decision) | **OPEN — proposed, unregistered REQ** | Blocks wiring either live transport, not a grade today | Expires when EITHER transport is wired — that is the activation event, not a re-measurement |
| **The duplicate-vs-strand decision is unmade** — the fix for the row above is not mechanical. Claim-before-send closes the race but strands a 214 permanently if the process dies between claim and send (a silent omission, worse than a duplicate for freight status); a per-tenant DO sweep lease is structurally right but needs a lease-expiry policy nobody has specified; accepting at-least-once for the 214 means allocating the control number BEFORE the dedupe check, reversing a deliberate REQ-204 choice. A test can enforce this decision; it cannot make it. | — | Repo | audit §236 | Owner | **OPEN — needs a decision before a fix** | — | Expires when the claim protocol is chosen |
| **`CLAUDE.md`'s "a fifth primitive" prohibition has an undefined subject** — the rule presupposes four primitives and no genesis doc or register row enumerates them. All ten occurrences of the word were read: the register uses it in three unrelated senses (network primitives Passport/settle/mesh, REQ-009; Migrator output rows, REQ-035; a Proposal React component, REQ-259), and `packages/design/src/primitives.tsx` exports ELEVEN components under the name. Ten of the other do-not-build items are gated or verifiably unbuilt (audit §246); this one cannot be applied by a gate OR a reviewer. | Low | Repo (the governing doc) | audit §246 — read every `primitive` mention in `genesis/` + the register; none enumerates a set of four | register owner | **OPEN — needs a definition, not a fix** (naming the four would be defining scope, which the surrounding sentence reserves to a signed register amendment) | none (a legibility gap, not a defect — no release grade depends on it) | when the four are named in `genesis/11` + `CLAUDE.md`, when the clause is struck, or when anyone is asked to judge whether a change adds one |
| **No line/branch coverage is measured anywhere** — no `coverage` config in any vitest config, so "is this line exercised?" has no mechanical answer. NOT a defect and not a gap in the register: nothing in `CLAUDE.md` or genesis asks for it, and `check:coverage` is register↔test traceability (REQ-118/119), a different thing. Recorded because it bounds what may be CLAIMED: measured 2026-08-05 (audit §275) — **280 source files, ZERO orphans** (nothing in `packages/`/`workers/`/`apps/` is unreachable from both the test corpus and the rest of the source; the 14 lacking a direct test import are each imported by 1–6 source files that tests exercise). So "every module is reachable from something that runs" is supportable; "every line is executed" is not. | Low | Repo (tooling scope) | audit §275 — reachability measured three ways (39 → 14 → 0 as the definition tightened) | owner (tooling call) | **OPEN — observation, not a defect.** Adding instrumentation is new tooling scope; its marginal value against this repo's mutation-proving discipline is a judgement | none (no release grade depends on it) | when coverage instrumentation lands, or when a claim of "full coverage" is made anywhere in the record |
| **REQ-167 is unverified in every local run** — `check:identity` reports `Lint SKIPPED — no denylist available` and fails closed only in CI (the eight-field restatement of the §3 row, which this re-audit downgraded from High to Med) | Med | Repo (the gate) + External (the denylist secret) | `pnpm -s check:identity` → `REQ-167: no denylist available … Lint SKIPPED — wire the secret before external contributions. NOTE: this gate fails CLOSED in CI (or when REQUIRE_DENYLIST is set); the skip is local-dev only.` exit 0. `pnpm -s check:identity -- --mode merge` → `##SHUDDL-GATE## {"gate":"identity-leak","status":"BLOCKED","executed":false,"assertions":0,"detail":"no denylist …"}`. CI binds it at `.github/workflows/ci.yml:49` | register owner (denylist contents) / infrastructure (the secret) | OPEN | **R1** for any *locally-made* mergeable claim; CI is closed | on binding `IDENTITY_DENYLIST` or creating `.identity-denylist.local`, or when `tools/checks/identity-leak.ts` changes. **Note (2026-08-01 convergence audit):** the U1–U3 commits (`6a6c88e`) moved sixteen days of untracked prose (12 skills + 4 plan docs) into append-only history while this gate was BLOCKED — the first denylist run must therefore screen those blobs (the `6a6c88e` file list), not only HEAD; the three marketing docs known to carry a person-derived slug were deliberately left untracked | **MECHANISM PROVED 2026-08-05 (audit §247) — the residual is now purely the SECRET's contents, not the gate.** The lint had never been observed firing anywhere, so "unverified" was ambiguous between *no denylist* and *no proof it works*. Both directions measured with a temporary `.identity-denylist.local`: a term PRESENT in the tree → **exit 1**, naming each offending file; a term ABSENT → **exit 0** (the negative control, so it is not simply always-red). It also **masks the matched term in its own output** (`g*****`), so a CI failure log does not itself leak the name it caught — the gate is safe to read in a public build log. What remains unproven is only whether the CI secret contains the RIGHT terms, which no repo-side test can establish.
| ~~**REQ-288 is recorded as unbuilt while ~25 files implement it — and the fix is entangled**~~ **RESOLVED 2026-07-27 — the record was RIGHT; this row's premise was wrong** | ~~Med~~ n/a | Repo (register + manifest data) | `tools/traceability/coverage-manifest.json:90` reads `"REQ-288": "vNEXT — P0 exact-artifact release evidence contract defined by the approved V2 framework; not built."` while `git grep -l "REQ-288"` returns 25 files including `tools/release/evidence.ts`, `tools/release/run-gate.ts` and `.github/workflows/ci.yml:47`; `pnpm check:coverage` lists it among the 8 drift rows. **Entanglement (why this is not a one-line fix):** the register row also carries `wp=P0`, which names no active WP, so advancing `status` alone routes it to `unclassified` and fails `check:coverage` (`tools/traceability/coverage.ts:105`) — while the manifest's own rule forbids a *built* row holding a deferral disposition. The `wp` amendment and the manifest removal must land together. ~~**Deliberately not attempted in Task 6**, which owns this file, not the register~~ · **DETERMINATION 2026-07-27 — do NOT advance this row.** The premise above is wrong: REQ-288 is **not built**, so the manifest disposition and the `vNEXT` status are both CORRECT and the drift advisory is a false positive. Read the requirement: *"Release **promotion consumes** only complete evidence records tied to the exact commit and environment and fixtures and deployment and assertions"*, DoD *"**Promotion rejects** skipped or stale or mismatched or zero-assertion evidence"*. **There is no promotion step.** Verified: `grep -rn "evaluateEvidence"` returns exactly two callers — `tools/release/run-gate.ts:200@evaluateEvidence` and its own unit test — and `run-gate.ts:194@PromotionContext` builds the `record` and the `context` from the *same local variables*, so the commit/environment/fixtures/deployment comparisons check each value against itself and can never fire. The ~25 citing files implement the evidence **producer** (the record); the requirement specifies the **consumer** (promotion), which does not exist. Same shape as REQ-170, REQ-284 and REQ-285 — a requirement naming more than what shipped — and resolved the same way: left deferred. **The drift advisory will list REQ-288 on every run; that is expected and must not be "cleaned up" by advancing the row** | register owner | NOT_APPLICABLE (record is correct) | none — no release grade depends on correcting a record that is already right | when a promotion consumer of `evaluateEvidence` is built |
| ~~**REQ-045 is a register DATA defect, not a build gap** — and it is the cleanest single fix among the eight drift rows~~ **FIXED 2026-07-27, commit `13161e9`** | ~~Low~~ n/a | Repo (one register cell) | Built: `packages/ledger/src/gates/transition-gates.ts:261-275` (`assertInterline`). Tested: `packages/ledger/test/transition-gates.test.ts:273`, `workers/api/test/gates.test.ts:163`. Register row: `spec=WP-05`, `wp=vNEXT`, `status=vNEXT` — the `spec` column **already names the WP** the `wp` column is missing, so the correct value is not a judgement call. Listed by `pnpm check:coverage` as drift · **Fixed 2026-07-27 (commit `13161e9`)**: `wp` `vNEXT`→`WP-05`, `status` `vNEXT`→`F0-SPEC'D`. The evidence was unambiguous on inspection — 65 of 288 rows legitimately carry a WP value in `spec`, and both immediate neighbours (REQ-043, REQ-044) mirror `spec` into `wp` while REQ-045 alone did not. Verified by gate, not argument: `built-annotated` 168→169, `vnext` 89→88, drift 8→7, still 288/288 classified with 0 unaccounted | register owner | FIXED | none | evidence stands until `genesis/09-REQUIREMENTS-REGISTER.csv` changes |
| ~~**§3's status-page row now misstates the portal** — it still reads "`GET /v1/board` is tenant-only; the portal party fleet is synthetic". That was true at WP-16 and was superseded by remediation Task 12; it is a stale record, **not** a regression~~ **FIXED 2026-07-27 (final cross-task review)** — superseded in place at `:186`, and the same stale claim was found and corrected in **two further files the original row did not cover**: `docs/security/pen-test-basics.md` §5 (the REQ-136 launch-gate artifact — the reason this mattered: a reviewer reading §5 would skip a real party-lens privacy check) and `docs/security/threat-model.md` (the 2026-07-22 WP-16 REQ-074 log entry, superseded by a dated 2026-07-27 entry) | Med | Repo (this file) | Source read — no command reproduces a doc claim. The route serves TWO lenses: `workers/api/src/routes/board.ts:17-31` documents and implements the PARTY lens (party-relationship predicate in SQL + `generalizePosition`, REQ-085/074/025). The portal consumes it: `apps/portal/src/api/board.ts:6-7` (the seam's own header) and `:59-60` fetch `GET /v1/board` and Zod-`.strict()`-parse it. Corroborating grep: `git grep -n "demoFleet" -- ':(exclude)docs'` → the only definition is `packages/map/src/demo.ts:112`, **no app imports it**, and `apps/portal/src/App.test.tsx:77,88,124,145,156` asserts it is never called on five paths | assurance | **FIXED** | none (the code is right; the record now is too) | when `workers/api/src/routes/board.ts` or `apps/portal/src/api/board.ts` changes **RE-VERIFIED 2026-08-11 (audit §995) — the trigger FIRED (both files changed 2026-08-06/08-09) and the evidence HOLDS:** `workers/api/src/routes/board.ts:18` still documents and implements the TWO-LENS split (party-relationship predicate + `generalizePosition`), and `apps/portal/src/api/board.ts:6` is still the seam header naming the server-scoped board. |
| ~~**Two path citations in this file do not resolve**~~ **FIXED 2026-07-27 (final cross-task review) — all three corrections applied in place at `:154`, `:264` and `:180`** | Med | Repo (this file) | Link-check of every backticked path in this file (113 distinct, `existsSync` each): `workers/api/src/internal-platform.ts` — cited twice, the file is `workers/api/src/routes/internal-platform.ts`; and `workers/api/src/do/sequencer.ts:700-708` for REQ-184 — that range is the `booking.created` gate, while the ratecon fail-closed marker is at `:926-929`. The two other misses are **not** defects: `docs/ops/threat-model.md` is cited as a negative example, and the `.claude/skills/…/reference-0006_….sql` name is elided prose | register owner | **FIXED** | none (record accuracy) | when this file or either cited source changes |
| ~~**The 2026-07-15 60-agent audit — the closure record for both V1 Criticals — is not in the repository**~~ **FIXED 2026-07-29 — committed; and the row's description was imprecise (see Proof)** | ~~Med~~ n/a | Repo | `ls docs/audits/` → exactly one file, `2026-07-22-wp16-launch-gate-audit.md`. `git log --all --oneline -- docs/audits/2026-07-15-full-audit-and-skill-plan.md` → empty; it was never committed. (That command also prints `fatal: bad object refs/heads/codex/cl-d-driver 2` on this machine — an iCloud-spawned duplicate of a branch ref, i.e. the §3 iCloud hazard reaching `.git/refs`, not a result.) §4 of this file and `docs/audits/2026-07-22-wp16-launch-gate-audit.md:8` both cite it as the record that C-1 (REQ-190) and C-2 (REQ-191) are closed.  · **FIXED 2026-07-29:** the file is committed (30KB, with a dated preamble stating its `path:line` citations are frozen as-of 2026-07-15 and deliberately NOT content-anchored — anchoring a dated observation to today's symbols would misrepresent when it was made; all 44 bounds-check clean, and it is enrolled in the citation ratchet exactly as the pre-ratchet plan documents are). **Accuracy correction to this row's own description:** the audit records **one** Critical — C-1, the `POST /v1/positions` consent/scope/signature bypass — not two. It is the discovery record for C-1 and the provenance for the 12 SHUDDL skills; C-2's discovery lives elsewhere. Both closures remain independently evidenced at WP-09. The **closures** are independently checkable — both register rows read `F0-SPEC'D`, both are annotated, `pnpm check:traceability` is clean — but the audit that graded them cannot be re-read from a checkout | register owner | FIXED | ~~**R0**~~ none — the record is in the tree and re-readable at any later SHA | evidence stands while `docs/audits/2026-07-15-full-audit-and-skill-plan.md` is tracked |
| ~~**The V2 phase vocabulary diverges between the register and the framework**~~ **FIXED 2026-07-29 — the equivalence is declared** | ~~Low~~ n/a | Repo (register/doc data) | The 75 V2 register rows carry `wp` cells `P0` / `V2-A`…`V2-F` / `Cross-cutting` (`awk -F, '$1~/^REQ-2[1-8][0-9]$/' genesis/09-REQUIREMENTS-REGISTER.csv`); `docs/ops/V2-EXECUTION-FRAMEWORK.md` §6 allocates those same rows to phases named `P0` / `PA`…`PF`, and the string `V2-A` appears nowhere in it (`git grep -n "V2-A" -- docs` hits only the design plan and this file).  · **FIXED 2026-07-29:** `docs/ops/V2-EXECUTION-FRAMEWORK.md` gains §6.0, a one-to-one mapping table (`PA`=`V2-A` … `PF`=`V2-F`, `P0`=`P0`, plus the two shared-row spellings `P0/D` and `V2-E/F`). It also names the three register values with no phase in that document and says why: `V2.5` (11 rows) and `V3` (1 row) are outside V2 and unplanned there; `Cross-cutting` (8 rows) spans phases and is allocated by §6.1's ranges rather than by its `wp` cell. NEITHER spelling was changed — the register's `wp` column is append-only scope data that `tools/traceability/coverage.ts` keys its dispositions on, so renaming 75 cells to settle a naming question would churn the scope authority. The declaration was the missing thing. Nothing is unallocated — §6's ranges cover the register's contiguous 75 rows with no gaps — but no document declares that `V2-A` and `PA` are the same phase | register owner | FIXED | none (naming) | when the register `wp` column or framework §6.0 changes |
| **~~Seven of the eight~~ Six of the seven remaining drift rows cannot be advanced by a status edit** — the register needs a `wp` amendment pass *(updated 2026-07-27: REQ-045 fixed at `13161e9`, REQ-288 determined NOT-A-DEFECT above — the row is correctly deferred)* | Med | Repo (register data) | `pnpm check:coverage` → `coverage: 8 status-drift row(s) … REQ-045, REQ-170, REQ-184, REQ-249, REQ-276, REQ-284, REQ-285, REQ-288`. Their `wp` cells read: REQ-045 `vNEXT` · REQ-184 `vNEXT` · REQ-249 `V2-E` · REQ-276 `P0` · REQ-284 `P0` · REQ-285 `V2-F` · REQ-288 `P0` — **none names an active WP** (`tools/traceability/active-wps.json` = WP-01..WP-16), so a status edit routes each to `unclassified` and fails the gate (`coverage.ts:105`). Verified end-to-end on a scratch CSV in Task 5. **REQ-170 is the eighth and is different**: a genuine unbuilt residual, deliberately reverted (`workers/agents/src/biller.ts:594-601@REQ-170`). **Caution for the next auditor:** "unadvanceable by a status edit" ≠ "built". REQ-184's annotation is a fail-closed marker at ~~`workers/api/src/do/sequencer.ts:700-708`~~ **corrected 2026-07-27 (citation gate): `workers/api/src/do/sequencer.ts:937-940@ratecon`** — the same `:700-708` slip this file's §3 row already corrected, surviving in a third place because a line-existence check cannot see an in-bounds wrong range; it is now content-anchored — not an implementation — whether each of the seven is genuinely built is a separate, per-row question | register owner (owner-signed amendment) | OPEN | **R1** | when `genesis/09-REQUIREMENTS-REGISTER.csv` or `active-wps.json` changes |
| ~~**`provision-prod --apply` can leave orphan resources if pointed at the wrong account** — it creates first and refuses second, so a wrong `--account-id` that still passes the marketing-account heuristic can produce up to 9 unreferenced Cloudflare resources before the post-create clobber-check aborts without writing any config~~ **FIXED 2026-07-31, commit `f4337a5`** — the clobber check now runs against the PLANNED set before the create loop | ~~Med~~ n/a | Repo (`tools/deploy/provision-prod.ts`) | ~~Design read, reproducible by construction: the pre-create `planPatches` sees no resolved ids (nothing adopted in an empty account), so every resource is created, and only the post-create pass raises `would-clobber-real-id` and aborts. The abort is correct — no config is written — but the resources persist. Surfaced by the agent that built the tool, 2026-07-30~~ **Reproduced by a test written first, then fixed.** *"refuses before creating anything when the configs carry ids it would have to clobber"* (`tools/deploy/provision-prod.test.ts:508@refuses`) — configs carrying a real `CONTROL_DB` id, an account holding none of it — observed **9 creates where 0 were expected** (6 D1, 2 KV, 1 R2) against the old ordering. `planCreateRefusals` (`tools/deploy/provision-prod.ts:630@planCreateRefusals`) now refuses a resource the account lacks whose config already holds a real id, before the first create; the post-create pass stays as belt and braces for a resource that appears between the two passes. `placeholderReason` is still the one law (via `isOverwritable`), dry-run is still the default, and `--force` still bypasses the refusal — pinned by its own test so the guard cannot become a wall. Mutation-proved: dropping the pre-create refusals from the abort returns the same 9 creates. `pnpm test:tools` → **22 files / 573 tests PASS** | backend | ~~OPEN~~ **FIXED** (2026-07-31) | none — provisioning is an operator command, not a release gate | when `tools/deploy/provision-prod.ts` changes |
| ~~**The preflight could not see `[[env.<scope>.routes]]` routes**~~ **FIXED 2026-07-31** — a routed worker parsed as unrouted | ~~Med~~ n/a | Repo (`tools/deploy/preflight.ts`) | `targetFromWrangler` read `scope["routes"]` and kept only `typeof r === "string"`, so the array-of-tables form the prod scopes use (`[[env.prod.routes]]` + `pattern`) parsed to `[]` for all three routed workers — api, mcp and billing were reported unrouted while wrangler had deployed them correctly. A route the reader cannot see is indistinguishable from a worker that is genuinely unreachable, so a future route check would have passed a deployment it never inspected. **Fixed**: all three wrangler spellings are read (bare string, route table, `route =`), and an INLINE table — which this reader's TOML subset splits into fragments — now THROWS naming the supported form rather than registering a junk pattern. Two tests pin it, mutation-proved RED against the old reader | backend | FIXED | none | evidence stands while `tools/deploy/preflight.ts` parses routes **RE-VERIFIED 2026-08-11 (audit §995) — the trigger FIRED (`preflight.ts` changed 2026-08-05) and the evidence HOLDS:** `tools/deploy/preflight.ts:488` still parses all three route forms, and `:486` names the array-of-tables case the prod scopes use. |
| **The citation gate is blind to a PATH-ONLY citation — the form that carried two of the five historical rot defects** | Low | Repo (the gate) | `pnpm check:citations` → `citation-links OK — ~~492 path:line citations~~ ~~544 (2026-07-29)~~ **945 (2026-08-02)** … ~~10 of them are content-anchored~~ ~~20~~ **24 content-anchored**`. It parses `path:line` only; a backticked path with NO line number is not a citation to it. Extending it was measured and **rejected 2026-07-27**: over non-plan markdown with `~~struck~~` spans masked, **411 checked → 25 unresolved → ~19 of those false** — elided prose (`packages/agents/.../compose.ts`), gitignored build artifacts (`tools/design/report.json`, `seed/SEED-1.json`), aspirational tenant config-pack paths (`tools/rater/README.md:81-85`), the deliberate negative example (`docs/ops/threat-model.md`), and untracked `.claude/skills/**`. A ~76% false-positive rate is the cry-wolf mode that has already cost this repo a gate: `binding-drift` reported three false rows and trained readers to skim the one line that must never be noise. **Three filters are prerequisites**, not niceties: (1) an elision filter — drop any path containing a `...` segment; (2) a generated-artifact allowlist — gitignored outputs are cited legitimately and never exist in a clean checkout; (3) a `docs/plans` exclusion — a plan proposes a path that later lands elsewhere, which is not rot. One TRUE positive it would surface today is already its own row above (the uncommitted 2026-07-15 audit). **RE-MEASURED 2026-08-05 (audit §240) — the expiry trigger fired, and the answer got WORSE, not better.** All three prerequisite filters were implemented and run against the current (much larger) corpus using the gate's OWN `resolveCandidates`, so relative and partial paths resolve exactly as they would in production: **710 checked → 22 unresolved → 21 clearly FALSE and 1 marginal** (a README's relative depth off by one, since fixed). That is a ~95%+ false-positive rate against July's ~76% — the filters removed the *mechanical* noise they were designed for (8 elisions, 1 range notation `WP-01..16.md`, 9 gitignored artifacts, 28 plan files) and the residual is entirely **semantic**: generic illustrations (`path/file.ts`, `worker/index.js`), shorthand whose referent exists under a different intermediate segment (`mcp/isolation.test.ts` → `workers/mcp/test/isolation.test.ts`), aspirational config-pack paths, and — decisively — **deliberate quotations of known-bad paths inside the rows that DOCUMENT citation rot** (every `docs/ops/threat-model.md` hit is in prose whose point is that no such file exists). A path-only gate would fire on this very row. No mechanical filter separates those from real rot, so the conclusion inverts: the three filters are necessary but **not sufficient**, and the blocker is not filtering but the absence of an opt-in marker distinguishing a citation from a mention | assurance | **OPEN — and now measured twice as not-worth-building without an opt-in marker** | none (a gate-coverage gap, not a defect — no release grade depends on it) | when an opt-in citation marker exists, NOT on another corpus re-run — two measurements now agree the corpus is not the variable |
| **A permanently-failed EVIDENCE EMAIL surfaces nowhere** — the Concierge's equivalent case is specified and built (REQ-176: a permanently-held reply records a hold note through the DO append surface and is not counted as answered). The **Biller has neither**: on a non-retriable send failure it returns `issued_send_pending`, the queue consumer `console.log`s the outcome, and nothing records it. `recon-sweep`'s anti-join cannot catch it either — that query finds a committed POD with **no invoice**, and here the invoice exists. So demo #1's payload (POD → invoice + photos in the client's inbox) can fail permanently with no trace outside a log line | **Med — LATENT** | Repo (the surfacing) — **needs a REQ row first**; REQ-176 covers the Concierge only | Source read: `workers/agents/src/biller.ts:77@issued_send_pending` returns `issued_send_pending` on `SendError` with `retriable === false`; `packages/agents/src/biller/sender.ts:183@NotConfiguredSender` throws a **retriable** error *(both were bare basenames until 2026-08-05, audit §249 — `biller.ts` and `sender.ts` read as siblings and are not: they sit in DIFFERENT packages, and the shorthand sent a reader to a `workers/agents/src/sender.ts` that has never existed. Now anchored, so the citation gate checks them)* (`"retriable: binding a provider and redelivering succeeds"`), so TODAY every send fails retriably → queue redelivery → DLQ, which IS a recoverable surface. The unsurfaced branch needs a LIVE provider issuing a permanent rejection | backend / register owner | **OPEN — proposed scope, not built.** ~~Dormant while no provider is bound~~ **TRIGGER FIRED — verified 2026-08-11 (audit §944): the provider IS bound in staging.** `workers/agents/wrangler.toml` sets `EVIDENCE_FROM` under `[env.staging.vars]` and its own note records `RESEND_API_KEY` as a scoped secret, so `index.ts:237@evidenceSender` returns a **ResendSender** and the unsurfaced-failure branch is reachable in a deployed environment today. (The same file's staging header claimed the opposite; corrected in place, and `wrangler-absence-claims.test.ts` now checks that class of claim.) The DLQ still covers the current path, so this stays Med — but it is LIVE-and-unsurfaced, not dormant | **R2** (staging — it goes live with the provider) | **when `RESEND_API_KEY` + `EVIDENCE_FROM` bind** (the event that makes the branch reachable), or when a Biller hold-note equivalent to REQ-176 lands |
| **The one agent with variable cost has no COST/LATENCY metering** (its convenience COUNT is metered by the SparkMeter DO, REQ-122/125 — a monthly allotment, count-only, no cost and no latency in its source) — REQ-113's Watchtower drift alarm averages `agent_runs`, which is projected only from `agent.acted`. The Rater, Translator and Migrator all emit it (honest `cost_cents: 0` + real wall-clock `latency_ms`). The **Concierge emits none** — verified with a control against two files that demonstrably do — so it has no `agent_runs` rows and its budget can never be breached, because it is never measured. It is also the only agent that calls an LLM, i.e. the only one whose cost is variable at all | **Med — LATENT** | Repo (the emit) — **needs a REQ row first** | Source read: `routes/rate.ts` and `translator/inbound.ts` emit `agent.acted` with `cost_cents`/`latency_ms`; no Concierge source contains the kind. `agent-runs.ts` is honest about absence (`cost` `{}` not a fabricated 0), so the metric reads UNKNOWN rather than wrong — the alarm simply has nothing to average | backend / register owner | **OPEN — proposed scope, not built.** Mitigating and load-bearing: `ClaudeParser` is selected only when `ANTHROPIC_API_KEY` **and** `ANTHROPIC_MODEL` are bound; today it is `NotConfiguredParser`, so no LLM cost is incurred anywhere and the gap is dormant | **R2** (staging — the gap goes live the moment the key binds) | **when `ANTHROPIC_API_KEY` binds** (the event that makes it live), or when the Concierge emits `agent.acted` with cost + latency |
| **A 4-hour SLA is policed by a 24-hour detector** — `SLA_REPLY_WINDOW_MS` is 4h, and the only thing that reads `sla_due_ts` is the SLA sweep, which rides the agents worker's single daily cron (`crons = ["0 1 * * *"]`). An inbound breaching at 06:00 is not surfaced until 01:00 the next day — ~19h late, a detector 6x coarser than the SLA it measures. No live read path computes overdue (verified: `sla_due_ts` is referenced only by the projection that writes NULL, the setter, and the sweep) | **Med** | Repo (the cadence) — **needs a REQ row first**; REQ-095's DoD is "Timer events fire", which a daily tick satisfies as written | Source read: `concierge.ts` `SLA_REPLY_WINDOW_MS = 4 * 60 * 60 * 1000`; `workers/agents/wrangler.toml` `[triggers] crons = ["0 1 * * *"]`. Comparison: `workers/billing` already runs an HOURLY cron (row above), so sub-daily cadence is a choice here, not a platform limit | backend / register owner | **OPEN — proposed scope, not changed.** Mitigating: the primary path is the auto-reply (seconds); the sweep only catches replies that died mid-append, so the affected population is small | **R3** (pilot — an unanswered customer email is tenant-visible) | when the agents cron gains a sub-daily expression, when a live overdue read lands in the Command queues, or when REQ-095 gains a latency bound |
| **A lost booking trigger has NO backstop** — `quote.accepted` commits, then `AGENT_QUEUE.send()` fails transiently; the shipment stays UNBOOKED forever. The identical window for `pod.signed`→invoice was judged serious enough to build REQ-169's recon sweep (`queries/unbilled.ts` anti-join + `recon-sweep.ts` re-drive); bookings got no equivalent. None of the **eight** contained sweeps (sla · collector · recon · credit-recon · watchtower · retention · mirror · **watchtower-snapshots**) reconciles bookings, and no unbooked-quote query exists. *(Count corrected 2026-08-05, audit §248: this read "seven" and omitted `watchtower-snapshots`, which `index.ts` contains alongside the others — its own source calls it "the NINTH fan-out … outside index.ts", a shape that has escaped enumeration before. Re-verified against `grep -c 'contain("'` = 8; the VERDICT is unchanged and now rests on the complete list — snapshots persist weekly evidence to R2 and touch no booking, and `handleQuoteAccepted` is reached only from `queue()`, never from `scheduled()`.)* | **Med–High** | Repo (the sweep) — **needs a REQ row first** (CLAUDE.md: no build without one) | Source read: `do/sequencer.ts` fire-and-forget `AGENT_QUEUE.send` for `quote.accepted`; its log line CORRECTED 2026-08-03 (audit §131) — it previously claimed "the sweep recovers it", naming a recovery that does not exist. Control: `pod.signed`+`invoice.issued` co-occur in `queries/unbilled.ts` and `recon-sweep.ts`; `quote.accepted`+`booking.created` co-occur in no reconciliation | backend / register owner (the REQ row) | **OPEN — proposed scope, not built.** Recorded rather than built, per the working agreement | **R3** (pilot — a stranded accept is customer-visible) | when a booking-reconciliation sweep lands, or when the accept→book path stops being a fire-and-forget enqueue |
| **`POST /pub/signup` returns a distinct 409 for a taken admin EMAIL** — an email-existence enumeration oracle on the unauthenticated surface, whose only fence is the unbuilt REQ-125 per-IP edge rule. It breaks the pub surface's own uniform-deny discipline (`pub/status.ts` maps every failure to ONE envelope precisely to deny a prober an oracle; `provision.ts` explicitly refuses to add a slug-existence oracle for reserved names). A taken workspace SLUG is inherent to the signup UX; a taken email need not be | Med | Repo (the response shape) + External (the edge rule that would fence bulk probing) | Source read: `workers/api/src/routes/signup.ts:94` maps `EMAIL_TAKEN` → 409 "THAT EMAIL IS ALREADY REGISTERED", fed by the `SELECT 1 FROM users WHERE email = ?` pre-check. DARK today (the route 404s behind `PROVISIONING_ENABLED`) | backend / founder (UX call) | OPEN — accepted-for-now, recorded so the pre-GA pen-test row inherits it knowingly | **R4** (public GA) | when `signup.ts`'s error mapping changes, when `PROVISIONING_ENABLED` flips, or when the REQ-125 edge rule lands |
| **Pool-binding exclusivity is enforced on ENUMERATION but not RESOLUTION** — a hand-added duplicate ops row (two claimed tenants naming one `pool_binding`) is refused by every sweep, but the resolve path — the path a WRITE travels (the translator inbound-204 handler persists parties/shipments/anomalies through it) — still returns the shared handle, so one tenant's rows would land in the database another reads | Med | Repo | Source read: the guard exists in `claimedTenantSlugs` (all four workers) and NOT in `resolveClaimedTenantDb`. **The fix was written and REVERTED 2026-08-02 (§12)** — a COUNT-based refusal on the resolve path failed six real api tests, because only two pool slots exist while the shared api control-plane carries standing claimed rows on both (`parity.test.ts`, `source-aware-ledger.test.ts`) alongside files that claim slots dynamically: no arrangement of 65 shared-DB test files can satisfy one-tenant-per-binding, so enforcing it would have failed on a harness artifact rather than a product truth | backend | OPEN (fix identified, not shipped) | **R4** (dark today behind `PROVISIONING_ENABLED`) | the structural answer is a control-plane UNIQUE index on the claimed `pool_binding` — it makes the duplicate unrepresentable and needs no runtime COUNT; land it with a pool-slot expansion or with per-file control DBs in the api harness **EXTENDED 2026-08-09 (audit §882): the named fix is NECESSARY, NOT SUFFICIENT.** *"Needs no runtime COUNT"* reads as *therefore it lands cleanly*; measured, it does not. §12's COUNT failed at READ time, a UNIQUE index fails at WRITE time — the moment a test claims a slot a standing row already holds — so it meets the SAME harness wall one step earlier. `workers/api/vitest.config.ts:30` runs `isolatedStorage:false` + `singleWorker:true` (a SQLite-backed DO leaves a `.sqlite-shm` sidecar the storage snapshot rejects), so 66 files share ONE D1 with no per-test rollback; `t-parity-mirror`→POOL_01 and `t-lgproof`→POOL_02 are on DIFFERENT slots and do not collide with each other, but four files re-seed the `_pool_0N` sentinels and claim one, and re-seeding a sentinel does not remove `t-parity-mirror`. **The harness work is a precondition either way** — either give the standing claimants their own bindings (only two pool slots exist) or scope the pool fixtures to a control plane the shared one cannot see. Note also that `pool_binding` lives inside the JSON `policy` column (`db/control/migrations/0003_tenant_pool.sql:26`), so the index must be a PARTIAL EXPRESSION index, not a column constraint. |
| ~~**`hashPath`'s digest stream is unframed**~~ **RESOLVED — struck 2026-08-09 (audit §877): the stream IS framed** | Low | Repo (`tools/fixtures/verify.ts`) | ~~Source read: `h.update(f).update(readFileSync(f))` per file, no framing~~ — **that source read no longer exists.** `tools/fixtures/verify.ts@hashPath` now length-prefixes BOTH the path and the bytes (`h.update(`${buf.length}:`)`), with a docblock stating the framing makes the stream injective and noting the digest re-pin it required. The row outlived its own cited evidence | assurance | ~~OPEN (accepted; fix only alongside a re-pin)~~ **FIXED 2026-08-01, commit `3a0d412`** — framed on its own terms: each entry's path and bytes now carry an explicit length prefix, and all seven vendored pins were recomputed under the framed digest IN THE SAME COMMIT (no fixture bytes changed; the manifest note records the re-pin). Two tests prove injectivity against real files in a temp tree rather than arguing it | none | evidence stands while `tools/fixtures/verify.ts` and the seven pins agree — `check:fixtures` is the check |
| **The agents worker cannot serve claimed pool tenants** — `workers/agents/src/tenants.ts` is a static two-slug map with zero `TENANT_POOL_*` bindings, while the api worker + sequencer DO fully serve claimed tenants; all eight cron sweeps (anchor, SLA, recon, collector, watchtower, retention, mirror, snapshots) enumerate `TENANT_SLUGS` only, so the REQ-169 recovery sweep and the unbilled alarm exclude exactly the tenants whose triggers this worker cannot route. **HARDENED 2026-08-01** (audit C3, commit `0d2f883`): an unknown-tenant trigger now RETRIES toward the configured DLQ instead of being ACK-destroyed, and the sequencer's "the sweep recovers it" logs carry the static-roster qualifier — the loss is now parked and visible, not silent. ~~The REMAINDER: a claimed-aware resolver … + claimed-tenant enumeration for the crons, **before any `PROVISIONING_ENABLED` flip**~~ **RESOLVER BUILT 2026-08-01** (commit `494765f`; the adversarial review then caught + closed the ninth fan-out hiding outside index.ts, pool-binding exclusivity, and the static-slug shadowing hole in `provisionTenant` — `def564c`) | ~~Med~~ n/a for the agents worker — ~~**the surviving instance is the TRANSLATOR worker**~~ **both the translator (commit 805dbb8) and the billing metering sweep (§12/§13) are now ported.** ~~Stated per-artifact, not absolutely (the §12 lesson): no worker's *cron/sweep fan-out* iterates a static-only roster — api, agents, translator and billing all enumerate `allTenantSlugs`, each pinned by its own source-glob test. The one static tenant MAP that remains is `HOST_TENANTS`…~~ **CORRECTED 2026-08-02 §15 — the sentence above was written in the same paragraph that invokes the §12 lesson, and was itself over-broad.** Verified by source: `workers/api/src` contains **zero** occurrences of `allTenantSlugs`, `workers/api/test` contains no `import.meta.glob`, and `workers/api/src/index.ts` has no `scheduled()` handler at all. The api worker has **no cron and therefore no fan-out** — its sweeps live in the agents worker — so it is correctly out of scope for this class, not an instance of it. The accurate statement is: **the three workers that DO fan out (agents, translator, billing) each enumerate `allTenantSlugs` and each carries its own source-glob pin; the api worker has no fan-out to pin.** Static tenant MAPS remain in all four workers (`TENANT_BINDINGS`) plus `HOST_TENANTS` in `workers/api/src/pub/quote.ts` — these are allowlists, not fan-outs, and a claimed pool tenant reaches its D1 through the claimed resolver rather than through them; the vanity-host half is the separate deferred row at §1 (`tenants.host`, WP-09) | Repo | `resolveTenantDb` mirrors the api contract (static hot path → control row plan-filtered → `pool_binding` ∈ the parity-pinned POOL allowlist; platform sentinel shared from contracts); ALL NINE cron fan-outs iterate `allTenantSlugs` (a source-glob pin bans bare `TENANT_SLUGS` iteration in EVERY src module); exclusivity violations exclude BOTH slugs fail-closed; `provisionTenant` structurally refuses static-roster slugs. `pnpm -F @shuddl/agents-worker test` → ~~17 files / 104 tests PASS~~ **17 files / 105 PASS (2026-08-02)**; api provision/cors pins green; pool bindings in all three wrangler scopes, ids converged with the api worker. **2026-08-02 §13 — the pin itself was hardened twice after review:** the glob was `../src/*.ts`, blind to every nested directory (it checked zero of the translator's three `src/core/` modules), and the matcher was for-of-only, so a `TENANT_SLUGS.map(...)` fan-out — the shape a `Promise.all` sweep takes — passed straight through. Both widened in all three workers and each mutation-proved RED. Billing gained the suite it shipped without: `workers/billing/test/claimed-tenants.test.ts`, **billing 5 files / 56 PASS** | backend | **FIXED** (2026-08-01, resolver + review hardening) | none for agents (translator instance rides the EDI flip line) | when `workers/agents/src/tenants.ts`, `index.ts`, `watchtower-snapshot.ts`, or `workers/api/src/provision.ts` changes, or when the `PROVISIONING_ENABLED` posture changes **RE-VERIFIED 2026-08-11 (audit §995) — the trigger FIRED (`workers/api/src/provision.ts` changed 2026-08-08, a week after the fix) and the evidence HOLDS:** the static-roster refusal survives at `workers/api/src/provision.ts:144` (throws `SLUG_TAKEN` — *a reserved static-roster tenant can never be claimed*), and it is exercised, not merely present, at `workers/api/test/provision.test.ts:126`. |
| **A pickup custody handoff cannot record REAL parties** — the driver capture layer fails CLOSED (CAPTURE_INPUT_MISSING) rather than fabricating them (the 2026-08-01 convergence audit found `p:shipper`→`p:carrier` recorded on real shipments; closed at `a3b667a`), but the REAL pair has no source: the driver manifest carries no party refs, the executing-carrier party is the documented `executor_party_id = bill_to` placeholder until dispatch refines it, and the driver principal rides the deferred REQ-069 login seam (the device party is likewise a fixed `p:carrier` in `apps/driver/src/session.ts`). Until this lands, a real pickup's sign step surfaces an honest blocker — the flow cannot complete a custody handoff | High for any real driver run (inert until a real driver can authenticate at all — REQ-069 is itself deferred) | Repo | `pnpm -F @shuddl/driver test` → **10 files / 54 tests PASS** incl. the fabrication-refusal pins; the count step records the driver's REAL entered pieces | backend | OPEN (fail-closed; needs manifest party refs + resolved executing party + REQ-069 identity) | **R3** (a real driver completes a gated stop — acceptance demo 3's filmed half) | when `apps/driver/src/flow/captures.ts`, the driver-manifest contract, or the REQ-069 seam changes |
| **`§702 GUARDED_FNS` completeness test is INTERMITTENT under full-suite load** *(filed 2026-08-11, audit §998)* | **Low** | Repo (the test) | Observed once in a `pnpm test:tools` run (5071 ms, the slowest assertion in the suite); **did not reproduce in 2 further full runs**, and passes in isolation both with and without the session's working-tree changes (`pnpm exec vitest run --config vitest.tools.config.ts -t "GUARDED_FNS"` → 1 passed). Not caused by this session: it scans tenant entry points, and nothing outside `tools/checks/` and `docs/` was touched | assurance | **OPEN — intermittent, 1 occurrence in 3 full runs.** NOT diagnosed: a single non-reproducing failure is not enough to locate a cause, and guessing one would put a wrong remediation in the record (§960's shape). Recorded so the SECOND occurrence is recognised as a pattern rather than re-triaged from zero — which is the whole cost of an unrecorded flake | none (it is a completeness gate, and its steady state is green) | on the next observed failure — capture the full run's output and the file ORDER, since vitest orders by cached duration and this is the suite's slowest assertion |

Cleared 2026-07-25 (all four browser gates now PASS in `--mode merge`):

| Was failing | Gate | Now |
|---|---|---|
| 1,000-entity long task | `perf` | NOT a defect — it was compositor rasterization in a GPU-less harness (a zero-entity board blocked 358ms; a real GPU blocks 0ms). The budget is enforced where a hardware rasterizer exists. The REAL finding it masked — 58-62% main-thread occupancy drawing a static picture — is closed: **measured 58.6% → 20.3%**. |
| No blessed screenshots | `visual` | five blessed, each opened and reviewed. `visual: PASS — 5 passed`. |
| Stale visual ready-selectors | `visual` | repaired — and a fourth screen was found rotten the same way: `command.png` was capturing five NETWORK REQUEST FAILED panels, because its ready selector waited for a `canvas` that the failure state also has. |

Those visual failures were invisible until Task 14 installed the browser: the harness self-skipped to
exit 0, so a suite that could never pass reported green.

That table is **pre-schema history** and is deliberately not retrofitted to the eight fields — a cleared
row has no severity, no owner and no expiry to carry. Its clearance was nevertheless **re-proved on
2026-07-27** at HEAD `72b2fc2`, because a cleared gate that is never re-run is exactly the failure mode
Task 14 found:

| Command | Verdict |
|---|---|
| `pnpm -s test:a11y -- --mode merge` | `##SHUDDL-GATE## {"gate":"a11y","status":"PASS","executed":true,"assertions":4}` |
| `pnpm -s test:e2e -- --mode merge` | `##SHUDDL-GATE## {"gate":"e2e","status":"PASS","executed":true,"assertions":6}` |
| `pnpm -s test:visual -- --mode merge` | `##SHUDDL-GATE## {"gate":"visual","status":"PASS","executed":true,"assertions":5}` |
| `pnpm -s perf:map -- --mode merge` | `##SHUDDL-GATE## {"gate":"perf","status":"PASS","executed":true,"assertions":1}` |

Sixteen assertions, all executed, none skipped. That evidence expires at the next change to
`apps/**`, `packages/map/**` or the blessed baselines under `tests/visual/`.

---

## 5. The V1 close-out — the objective, question by question (Task 8, 2026-07-27, HEAD `dc26ea8`)

The close-out question is not "did the tasks finish" but **is the objective met**. Each answer below cites
either a command run at this SHA, or a file a reader can open. Where the proof **could not be produced
here**, the answer names the suite, states its last known verdict *and where that verdict came from*, and
marks it **unverified at this SHA**. A "no, and here is what would prove it" is a complete answer; a
green-sounding paragraph resting on an unrun suite is not.

Two facts govern every answer:

- **The environment hold** — **NO LONGER TRUE; see the superseding block immediately below this list.**
  ~~`workerd` is wedged on this machine (failures ledger, row 2), so
  `packages/ledger`, all five `workers/*` suites, `pnpm test`, `pnpm test:acceptance`, `pnpm verify:dev`,
  `pnpm verify:merge` and `pnpm verify:release` could not be run during this entire close-out.~~ Full
  diagnostic and remedy: [`RELEASE-EVIDENCE.md`](./RELEASE-EVIDENCE.md) § *Sweep — 2026-07-27*.
- **Which SHA the Task-7 sweep measured.** Its verdicts were obtained at `c09d9a5`. `dc26ea8` differs from
  it in two documentation files only (`git diff --name-only c09d9a5 dc26ea8` → `docs/ops/GO-LIVE-CHECKLIST.md`,
  `docs/ops/RELEASE-EVIDENCE.md`) — no source, test, fixture or config — so nothing those gates measure could
  have changed. **Rule 1 of `RELEASE-EVIDENCE.md` still binds an artifact to its exact SHA**, so they are
  formally evidence about `c09d9a5`. Where an answer below needed a verdict *at this SHA*, the command was
  re-run here and is marked as such.

**Superseded in place — 2026-07-28, HEAD `3fc592b` (post-reboot). The first of those two governing facts is
no longer true, and every answer below inherits the correction.** The machine was rebooted:
`workerd --version` returns `workerd 2025-10-11` instantly and
`ps -eo stat,command | grep '[w]orkerd' | grep -c '^UE'` returns **0**. Everything that could not be run
during the close-out has now been run, at `3fc592b`:

| What was unrunnable at `dc26ea8` | Verdict at `3fc592b`, 2026-07-28 |
|---|---|
| `pnpm -F @shuddl/ledger test` | **PASS — 34 files / 598 tests** |
| `pnpm -F @shuddl/api test` | **PASS — 65 files / 719 tests, three consecutive runs** |
| `pnpm test` (every workspace + the tools suite) | **PASS — 258 files / 3,243 tests** |
| `pnpm test:acceptance` (the five demos' in-repo spine) | **GREEN — all 7 spine tests pass** |
| `pnpm verify:merge` (the authoritative merge gate) | **16 gates PASS, 5 BLOCKED — aggregate BLOCKED, exit 2, NOT PROMOTABLE** |

Read the rest of §5 with that applied: wherever an answer says a suite "could not be run" or is "unverified
at this SHA", it is a true statement about `dc26ea8` and a **false** one about `3fc592b`. What did **not**
change: the five BLOCKED gates (all five are absent private fixtures or an unset secret, none is a code
defect), every external hold, that production is unprovisioned, and that V1 is **not promotable**. The
reboot converted *unmeasured* into *measured*. It provisioned nothing and vendored nothing.

### Q1 — Is all V1 Critical/High **code** debt closed? · **Criticals yes. Highs no: four survive.** *(Superseded 2026-07-28: **three** survive — the first was observed green at `3fc592b`.)*

- **Criticals: zero open.** The two from the 2026-07-15 60-agent audit — C-1 `POST /v1/positions` consent/auth
  bypass (REQ-190) and C-2 duplicate `booking.created` (REQ-191) — closed at WP-09; both register rows read
  `F0-SPEC'D`, both are annotated, and `pnpm check:traceability` reports no orphan in either direction. The
  WP-16 launch-gate swarm re-probed the crown jewels and recorded
  `docs/audits/2026-07-22-wp16-launch-gate-audit.md:6` — *"Verdict: ZERO open Criticals. CLEAR-TO-CLOSE."*
  **Caveat, now a debt row:** the 2026-07-15 audit document itself was never committed, so its narrative
  cannot be re-read from a checkout; only its outcomes can.
- **Highs closed with a fix commit.** WP-16's one High — invoice-void AR divergence (REQ-209) — is **FIXED at
  `1d3cf83`** ("invoice void flips AR out of issued"). Its regression test exists and is readable
  (`packages/ledger/test/money-projection.test.ts:120@VOID`) but is a `workerd` suite, so **it was not run here**;
  the "reconciliation-tested" claim is the WP-16 audit's, at that SHA, not this close-out's. The four
  code-unremediated Highs the 2026-07-15 audit carried (identity-leak fail-open, idempotency-4xx memoization,
  design-CI blind spots, map bearing) were fixed and reconciled at WP-16 as REQ-167/206/207/208 — REQ-206/207/208
  read `F0-SPEC'D`, REQ-167 reads `F0.2-SPEC'D` (a standing process row), and all four are annotated. The
  `credit_status` write-ordering High landed at WP-11 (REQ-183, §3).
- **Four Highs survive, and each is a *stated hold*, not a closed row:**
  1. ~~`anchors/run` backfill containment — **`FIXED †`** (`1aa0db5`, `8f24e68`, `501330c`, `6e33e89`, approved
     after three review rounds) but **never observed green in this environment**. The † is the whole point.~~
     **CLOSED 2026-07-28 at `3fc592b`:** observed green — `packages/ledger` 598/598 and `workers/api` 719/719,
     the latter three consecutive times against a defect that failed roughly four runs in five. The † is off;
     three Highs survive, not four.
  2. **Ratecon generation unbuilt** (§3, REQ-184) — nothing writes a `documents` row of kind `ratecon`, so the
     REQ-043 dispatch gate cannot pass without a REQ-049 override. Fail-**closed**, at
     `workers/api/src/do/sequencer.ts:937-940@ratecon`. Register `vNEXT`.
  3. **EDI B2A 04/05 convergence deferred** (§3, REQ-205) — `workers/translator/src/core/map-204.ts:70@B2A`; a
     PO-only re-tender yields a visible duplicate. Deliberate: a visible duplicate beats a silent merge.
  4. **EDI transport + inbound-204 HMAC resolver unwired** (§3, REQ-034/154) —
     `workers/translator/src/index.ts:27,36` return `NotConfiguredTransport` / `NotConfiguredSecretResolver`;
     every live 204 401s and nothing transmits.
- **Consequence for the grade.** §9 R1 reads *"zero open repository Critical/High debt **and** the
  authoritative merge gate passes."* Both clauses fail: three of the four Highs above are still graded High in
  §3, and ~~`pnpm verify:merge` cannot be run at all~~ **(superseded 2026-07-28) `pnpm verify:merge` runs and
  returns BLOCKED — exit 2, NOT PROMOTABLE, on five absent private inputs.** The second clause therefore still
  fails, but for a materially different reason: not because the gate cannot start, but because it started,
  executed sixteen gates green, and refused to promote on inputs that never enter this repository. Either those
  three Highs are re-graded with a written rationale
  (they are unbuilt scope behind fail-closed gates, not live defects), or R1 waits on them.

### Q2 — Is there a demo/mock success path in production clients? · ~~**No fabricated data.**~~ **SUPERSEDED 2026-08-01 (convergence audit): the verdict was a FALSE GREEN, and its proof command could not have caught it.** The recorded grep's pathspec (`'apps/*/src'` — a glob with no trailing component) matches ZERO files, so "zero hits" proved nothing — a proof command must itself be exercised against a file known to contain the pattern (share-lint-matchers law). The truth it missed: the driver capture layer fabricated freight facts into signed, gate-satisfying, counterparty-visible ledger events — a hardcoded `pieces: 6` on every real pickup, fixed dims, and fictional custody/actor identities (`u:driver`/`p:shipper`/`p:carrier`) — **CLOSED same day, commit `a3b667a`** (real count input; every physical fact threads through CaptureContext or the capture throws; the custody party pair is the pre-pilot row below). Also missed: the portal's fictional evidence-email specimen was publicly reachable at `?screen=email` (**closed, `1b4e494`** — harness/dev builds only), and `fleet1k()` (the synthetic 1,000-mark generator) IS imported by the production command surface for the `?perf` harness path — documented (REQ-079), chrome/network suppressed, but it is a synthetic render path in a production bundle and stays named here. The tile-host half of the original verdict stands. · *(original heading preserved:)* **Yes, a third-party demo tile host.**

The prescribed grep, run at this SHA, returns **six hits across five files, and all six are comments**:

```text
apps/command/src/lib/board.ts:3          "replaces the synthetic demoFleet() the map used to render"
apps/portal/src/App.tsx:70               "No demoFleet anywhere: a failure shows an honest stale/empty/…"
apps/portal/src/api/board.ts:7,40        "REPLACES the synthetic demoFleet…" / "a re-introduced demoFleet would be dropped"
apps/driver/src/components/GatedFlow.tsx:40   "the FOREGROUND geolocation reader that REPLACES the hardcoded MOCK_GEO"
apps/driver/src/flow/captures.ts:12      "Task 11 removes the hardcoded MOCK_GEO"
```

Proof, not assertion, that nothing behind them ships:

- `git grep -n "demoFleet" -- ':(exclude)docs'` — the only **definition** is `packages/map/src/demo.ts:112`; **no
  app imports it**. Every remaining reference is a test spy, and `apps/portal/src/App.test.tsx:77,88,124,145,156`
  asserts on five separate paths that it is never called.
- `git grep -n "MOCK_GEO"` — **no definition anywhere.** Three comments record its removal.
- The boards are server-scoped reads: `apps/portal/src/api/board.ts:59-60` fetches `GET /v1/board` and
  Zod-`.strict()`-parses it; `workers/api/src/routes/board.ts:17-31` resolves the lens from the JWT claim,
  applies the party-relationship predicate in SQL and generalizes coordinates server-side (REQ-085/074/025).
  A forged `?party_id` buys nothing.
- `git grep -niE "synthetic|fake|stub|placeholder" -- 'apps/*/src'` minus tests → **zero hits.**

**The honest residual.** All three shipping surfaces hardcode a public third-party demo basemap:
`apps/command/src/App.tsx:209`, `apps/portal/src/App.tsx:85` and `apps/portal/src/status.tsx:78` pass
`DEMO_TILE_URL` / `DEMO_GLYPHS_URL` = `https://tiles.openfreemap.org/{planet,fonts}`
(`packages/map/src/demo.ts:119,123`) — constants, not build vars. It fabricates no freight, no fleet and no
position, but it *is* a demo dependency on a shipping path and it sits against REQ-075's no-third-party rule
until the R2 self-host lands. Already recorded in §2 (*Self-hosted Protomaps vectors on R2*, blocks go-live).
**And a record defect found here:** §3's status-page row still says the portal party fleet is synthetic — three
tasks out of date. New failures-ledger row. **(Closed 2026-07-27, final cross-task review: `:186` superseded in
place, and the same stale claim corrected in `docs/security/pen-test-basics.md` §5 and
`docs/security/threat-model.md` — the failures-ledger row now reads FIXED.)**

### Q3 — Is financial / evidence authority proven at this SHA? · **No. It is unproven, and the largest unproven thing in this close-out.** *(Superseded 2026-07-28 at `3fc592b`: **proven by suite, unproven by DoD fixture** — see the note under the table.)*

| Claim | Suite / gate | Verdict, and where it was obtained |
|---|---|---|
| Invoice math matches the Rater **to the penny** on a 500-case replay (REQ-031, WP-06 DoD) | `pnpm check:invoice-parity -- --mode merge` | **BLOCKED, run at `dc26ea8`** — exit **2**, `##SHUDDL-GATE## {"gate":"invoice-parity","status":"BLOCKED","executed":false,"assertions":0,…}`. `invoice-500-replay` and `zone-tariff-v1` are not vendored. The harness's in-repo smoke did run: `invoice parity smoke — 5/5 in-repo synthetic cases: invoice === rater, penny for penny (harness live; NOT the 500-replay DoD)`. **Five synthetic cases are not the DoD** |
| ~~**QB export reconciles to the penny** (CLAUDE.md rule 6)~~ **VERIFIED 2026-08-06 at `6285a8c` (audit §473)** — the blocker (`workerd` wedged) has expired; all four suites RUN and pass: ledger trio **3 files / 22 tests**, `export-journal` **15**. The named assertions executed, including *(b) the AR side reconciles … to the penny*, *(c) the AP side …*, and *the IIF transaction balances to the penny (Σ amounts === 0)*. | `packages/ledger/test/qb-journal.fixture.test.ts`, `.../gl-netting.fixture.test.ts`, `.../iif.test.ts`, `workers/api/test/export-journal.test.ts` | **NOT RUN at this SHA — unverified.** All four are `vitest-pool-workers` suites (`packages/ledger/vitest.config.ts:1` is `defineWorkersConfig`), and `workerd` is wedged. The last recorded execution of those pools is the close-out plan's starting-state table (`docs/plans/2026-07-27-v1-closeout-t16-t17.md`, HEAD **`f269f95`**: api 719 tests / 65 files) — a SHA *before* the four Task-1 ledger commits, and **the same table records that suite as intermittently failing**. What *did* run here: `tools/checks/gl-accounts-parity.test.ts` → **6 passed at `dc26ea8`**, which proves the Biller's `GL_MAP` and the ledger money projection agree on the canonical GL chart — **not** that any export reconciles. **Superseded 2026-07-28: run at `3fc592b` — see the note under this table** |
| Evidence hash / chain / anchor authority (canonical byte law, Merkle root, device signature, CMS receipt, upload hash-verify) | `packages/ledger/test/{canonical,roundtrip,chain,merkle,sign,cms,anchor}.test.ts`; `workers/api/test/{evidence-upload,documents,anchors}.test.ts` | **NOT RUN at this SHA — unverified.** Same pool, same hold, and no later verdict exists for these files anywhere: ~~`PROJECT-STATE.md:97` lists all six pools under *"Not measured on 2026-07-27"*~~ **(that paragraph was superseded 2026-07-28 — `PROJECT-STATE.md` now records measured totals for all eighteen projects).** The most recent recorded execution is the same `f269f95` starting-state table above. Nothing in this close-out re-observed any of them. **Superseded 2026-07-28: run at `3fc592b` — see the note under this table** |

So: **this SHA proves nothing about the ledger, the sequencer, the gates, the queues or the API surface** — the
same line Task 7 drew. What would prove it, in order: reboot the machine → `pnpm -F @shuddl/ledger test` and
`pnpm -F @shuddl/api test` → `pnpm verify:merge` (one artifact replaces most of this page) → and, for the DoD
half of the money claim, vendor `invoice-500-replay` + `zone-tariff-v1` from the engagement workspace.

**Superseded 2026-07-28 at HEAD `3fc592b` — the first three steps of that list were taken, and the answer to
Q3 changes.** Rows 2 and 3 above are no longer unverified: the reboot cleared the wedge, `packages/ledger` ran
**34 files / 598 tests PASS** and `workers/api` ran **65 files / 719 tests PASS** (three consecutive runs), and
the QB-export, canonical-byte-law, chain, Merkle, signature, CMS-receipt and anchor suites those rows name live
inside those two projects. The full `pnpm test` ran **258 files / 3,243 tests PASS**, and `pnpm verify:merge`
executed end-to-end and wrote a record. **Row 1 is unchanged and still BLOCKED**: `invoice-500-replay` and
`zone-tariff-v1` are still not vendored, so the 500-case penny-parity *DoD* remains unproven — a fixture hold,
never a runtime one, and no amount of rebooting touches it. The honest restatement, then: financial and
evidence authority is now **proven by suite and unproven by DoD fixture**, where before it was unproven by
both. The one thing that would close the remainder is the fourth step, unchanged: vendor the two fixtures.

### Q4 — Are the release checks non-skippable? · **Yes mechanically, and its own guard tests execute here — but the gate they protect has never been run at this SHA.**

- `tools/release/run-gate.ts:41-81` — 12 plain + 9 skippable gates under `--profile merge`; 4 more under
  `--profile release`. Every skippable gate is spawned with `--mode <profile>` (`run-gate.ts:91-92`), so it never
  chooses the advisory branch.
- `tools/release/evidence.ts:148-151` `unavailableStatus` — the single disposition rule: `local` ⇒ `PENDING`
  (exit 0), `merge`/`release` ⇒ **`BLOCKED` (exit 2)**. `evidence.ts:130` — BLOCKED and PENDING both fail to
  promote; `evidence.ts:70-71` — a PASS must additionally carry `executed: true` and `assertions > 0`
  (*"a PASS asserting nothing is a skip in disguise"*), so a skip cannot wear a green coat.
- `tools/harness/playwright-guard.ts:59-64,89-92` — under `--mode merge|release` an absent browser, an empty
  suite or an all-skipped suite is BLOCKED with exit 2: *"every test was skipped … a skip is not a pass."*
- **Executed at `dc26ea8`:** `pnpm exec vitest run --config vitest.tools.config.ts tools/harness/playwright-guard.test.ts
  tools/release/evidence.test.ts tools/release/ci-contract.test.ts` → **3 files, 69 tests passed**, plus
  `tools/fixtures/fixtures.test.ts:39-52` (REQ-288: *"pending + merge → BLOCKED"*). Live behaviour at the same
  SHA: `check:invoice-parity -- --mode merge` exited **2**, not 0.
- **Three limits stated plainly:** (1) ~~`pnpm verify:merge` itself cannot be run here, so the aggregate has never
  been observed at this SHA — under rule 1 this SHA has **no evidence record at all**~~ **superseded 2026-07-28:
  `verify:merge` ran at `3fc592b` and wrote this branch's first evidence record — 16 gates PASS, 5 BLOCKED,
  aggregate BLOCKED (exit 2). The limit that replaces it is narrower and still real: an evidence record exists,
  it is complete, and it says NOT PROMOTABLE**; (2) `verify:release` is
  wired into no workflow (`RELEASE-EVIDENCE.md:129`) — a release record exists only if a human made one;
  (3) ~~the record's own commit/environment/fixtures/deployment binding is **self-satisfied** by construction
  (`run-gate.ts:142-144`; failures ledger row 7), so it cannot catch a stale record until a separate promote
  step exists.~~ **SUPERSEDED 2026-08-02 (audit §16, commit `10a95f5`):** `run-gate` re-observes the context
  after the gates run instead of copying it from the record, so all four comparisons are live — and they bite
  today rather than only once a promote step exists, because a gate run spans minutes and a commit or
  fixtures change mid-run now invalidates the record. (1) and (2) above are unchanged.

### Q5 — Is all technical debt documented? · **No at the start of this task; yes at this commit — and, by design, with the eight fields only in the two bottom ledgers.**

- **Scope of the schema.** §1.1 states it: the eight fields govern the **External holds** (10 rows) and
  **Repository-owned failures & debt** (11 rows before this task, **15 after**) ledgers. *(Current at 2026-08-03, audit §128: External holds still **10 rows**, exactly as stated. The repo-owned ledger has since grown across **3 tables to 32 rows — 19 struck through as closed, 13 live**. The "15 after" above is correctly scoped to Task 15 by its own before/after phrasing and is not restated as current; this note exists so a reader need not count.)* §2's operator tables and
  §3's debt tables keep their original columns as narrative history. That is a deliberate, declared exclusion —
  but the arithmetic is worth stating: this file carries ~~**245 table rows**~~ **244 table rows at `dc26ea8`
  (corrected 2026-07-27: the original figure was never measured; an `awk` pass over lines beginning `|`, with
  the 23 header and 23 separator lines excluded, returns 244). This commit adds one §4 cross-reference row, so
  the count is 245 today — right by arrival, not by measurement**, and **25** of them carry the eight
  fields (10 external holds + 15 repository-owned, re-counted the same way). Every known defect is *recorded*
  here; only those 25 are recorded to the schema.
- **This audit found four record defects that were not documented anywhere.** All four are now rows in the
  failures ledger with all eight fields: the stale portal-is-synthetic claim; two unresolvable path citations;
  the never-committed 2026-07-15 audit; and the V2 phase-vocabulary divergence.
- **A property worth knowing before editing this file.** Every `REQ-nnn` written here is a *recorded home* for
  `pnpm check:coverage` (`tools/traceability/coverage.ts:134`), and this file is excluded from the annotation
  scan (~~`tools/traceability/orphans.ts:66`~~ **`:71` as of 2026-07-27 — `docs/ops/RELEASE-EVIDENCE.md` joined
  the exclusion list at `:73` for the same reason, pinned by `tools/traceability/coverage.test.ts`; it is
  excluded but is *not* a recorded home, so an id cited only there is still unaccounted**). Citing an id here therefore records a **deferral** and can never
  mint a false claim that code shipped — the failure mode Task 3 hit in `PROJECT-STATE.md`. Rewording a row is
  safe; **deleting the last citation of a deferred id is not** — it drops that row's home and fails the gate.

### Q6 — Is the V2 design, requirements, DAG, owners, estimates, acceptance and evidence contract authoritative? · **Six of the seven are complete; estimates are complete for five of seven phases, by explicit design.**

| Part | Where | State |
|---|---|---|
| Design | `V2-EXECUTION-FRAMEWORK.md` §3 (storage/authority ownership + write-and-recovery rules), §4 (the single durable proposal state machine), §14 (failure semantics); design source `docs/plans/2026-07-23-v1-remediation-v2-framework-design.md` — **present in the tree** | Complete |
| Requirements | `genesis/09-REQUIREMENTS-REGISTER.csv` REQ-214…REQ-288 — **75 rows, verified contiguous, no gaps**, every row `vNEXT` with a phase in its `wp` cell | Complete |
| DAG | §5 — V1 R2 → P0 → PA → {PB, PC, PE} → PD → PF → R3/R4 → 30-day shadow → R5, with the shadow declared noncompressible | Complete |
| Owners | §6 (accountable lead per phase) + §11 (nine accountable roles, with *"no role may self-approve a control for which it produced the sole evidence"*) | Complete — **per phase and per role, not per requirement row**; the register carries no owner column |
| Estimates | §13 — P0 3–5 · PA 7–10 · PB 10–15 · PD 10–15 · PF 5–8 engineer-days | **5 of 7.** PC and PE are deliberately un-baselined until export size and the device matrix are measured; external lead times are named as lead times rather than hidden in engineering estimates |
| Acceptance | §8 — AT-1…AT-6, each requiring API assertions, role/cross-tenant probes, browser interaction, accessibility, degraded behaviour, reconciliation, a recording where material, and an exact-SHA evidence record | Complete |
| Evidence contract | §10 (the per-gate record schema + the anti-skip rules) with §9's six release grades R0…R5 | Complete |

Two caveats a reader must carry: (a) the register's `wp` cells say `V2-A`…`V2-F` while §6 says `PA`…`PF`, and
no document declares the equivalence — nothing is unallocated (§6's ranges cover all 75 rows), but the mapping
is inferred (new failures-ledger row); (b) **"authoritative" means recorded and approved, never built** — §1 of
the framework says so itself, and five V2 rows (REQ-249, REQ-276, REQ-284, REQ-285, REQ-288) already show status
drift, code annotated while the row still reads `vNEXT`.

### The close-out statement

> **At `dc26ea8`, V1 is a repository whose static, traceability, design and browser gates all pass and whose
> twelve runnable test suites pass 1,470 assertions with zero failures — and whose six ledger/Worker suites,
> five-demo acceptance spine and authoritative merge gate could not be executed here at all, so this SHA proves
> nothing about the ledger, the sequencer, the gates, the queues or the API surface⟨4⟩. Staging is partially
> provisioned and certified in no respect: five placeholder resource ids, four secrets the checker cannot see,
> no CORS origins, no timestamp authority, no backup, and ~~nothing deployed to smoke~~ **no `SMOKE_API_BASE`
> bound in this checkout, so the smoke gate cannot see the deployment that does exist**⟨1⟩. Production is
> unprovisioned — nineteen placeholder ids (seventeen D1 + two KV)⟨2⟩, its secrets absent. Ten external holds are
> open, seven of them High, and two of those ten have no gate that could ever observe them; ~~fifteen
> repository-owned rows are open~~ **fifteen repository-owned rows are RECORDED — at `dc26ea8` thirteen `OPEN`,
> one `FIXED †`, one `BLOCKED`**⟨3⟩,
> none Critical and none open-High — ~~though the one High among them reads `FIXED †` precisely because it has
> never been observed green here~~ **and at `3fc592b` that row reads plain `FIXED`: the reboot cleared the
> wedge and the fix WAS observed green — `packages/ledger` 598/598 and `workers/api` 719/719 three consecutive
> times, against an original failure rate of roughly four runs in five**⟨4⟩, and §3 still grades three unbuilt,
> fail-closed rows High. V1 is not ready to
> launch, and no promotion is available from this commit: ~~the merge gate has not run, so under rule 1 of
> `RELEASE-EVIDENCE.md` this SHA carries no evidence record at all~~ **the merge gate has now run, at
> `3fc592b`, and the record it wrote says BLOCKED — 16 gates PASS, 5 BLOCKED, NOT PROMOTABLE**⟨4⟩**.**
>
> *Where those green verdicts came from:* the gate list and the 1,470 assertions are the Task-7 sweep at
> `c09d9a5`, from which this SHA differs in two documentation files and no executable byte
> (`git diff --name-only c09d9a5 dc26ea8`). `check:coverage`, `check:traceability`, `check:invoice-parity` and
> the anti-skip guard tests were re-run at `dc26ea8` itself. Nothing in this statement is inferred from a gate
> that did not run.
>
> *Corrected 2026-07-27 (final cross-task review — three figures above did not survive re-measurement; the
> verdicts they describe are unchanged). The `⟨n⟩` markers are deliberately **not** daggers: §1.1 `:20` gives †
> a specific meaning — "not verified in this environment" — and ~~exactly one row in this file carries it
> (`:358`)~~ **as of 2026-07-28 no row carries one: that row was observed green at `3fc592b` and the dagger
> came off. See ⟨4⟩.**
>
> **⟨1⟩ "nothing deployed to smoke" was wrong, and wrong in the unsafe direction.** `shuddl-api-staging` and
> `shuddl-agents-staging` **are** deployed; a smoke has driven a gated stop over HTTPS through `pod.signed` to a
> penny-exact `invoice.issued` (first green run 55,800¢); and the deployed Biller sends **real** evidence email
> from `pod@send.shuddl.tech` on a live `RESEND_API_KEY` — `PROJECT-STATE.md:19-27`, ~~`:172-179`~~ `:218-225`
> (repointed 2026-07-28). The true claim is
> narrower: `SMOKE_API_BASE` is unset **in this checkout**, so `staging-smoke` has no base URL and returns
> BLOCKED. That verdict is correct and stands; it is about this checkout, never about the account. The failure
> this correction prevents: an engineer reads "nothing is deployed", seeds or replays data, or re-enables
> `EVIDENCE_FROM` "since nothing is deployed anyway" — while a live Biller mails whatever address a party row
> carries.
>
> **⟨2⟩ the nineteen prod placeholder ids are seventeen D1 + two KV.** Re-measured
> `pnpm exec tsx tools/deploy/preflight.ts --env prod` at `79ae54d`: 17 all-zero D1 `database_id`s (api ×6,
> agents ×3, billing ×4, mcp ×1, translator ×3), plus `shuddl-mcp-prod.GRANTS` **and
> `shuddl-api-prod.IDEMPOTENCY`**. The api idempotency KV appeared in no document before this correction, so a
> provisioner working the old "every prod D1 …; the mcp `GRANTS` KV" wording ships an api worker whose
> idempotency store does not exist.
>
> **⟨3⟩ "fifteen repository-owned rows are open" counted rows, not statuses.** The ledger holds fifteen rows: at
> `dc26ea8`, **thirteen `OPEN`, one `FIXED †`** (the `anchors/run` containment fix, `:358`) **and one `BLOCKED`**
> (the wedged `workerd`, `:359`). **This commit closes two more of the thirteen** — the stale
> portal-is-synthetic record (`:368`) and the unresolvable path citations (`:369`), both record defects in this
> file — leaving **eleven `OPEN`**. "None Critical and none open-High" is unchanged and still holds.
>
> **⟨4⟩ superseded 2026-07-28 at HEAD `3fc592b` — the two unmeasurable clauses are now measured, and the
> conclusion survives them.** The machine was rebooted and the `workerd` wedge cleared. Everything the statement
> called unexecutable ran: `packages/ledger` **34 files / 598 tests PASS**; `workers/api` **65 files / 719 tests
> PASS, three consecutive runs**; the full `pnpm test` **258 files / 3,243 tests PASS** across every workspace
> and the tools suite; `pnpm test:acceptance` **GREEN — all 7 spine tests pass**. So *"this SHA proves nothing
> about the ledger, the sequencer, the gates, the queues or the API surface"* was true of `dc26ea8` and is
> **false of `3fc592b`**. And *"this SHA carries no evidence record at all"* is superseded outright:
> `pnpm verify:merge` ran and wrote one, at
> `artifacts/release/3fc592b0064086850393d01acc6a2a16be19e650/merge/gate-merge-2026-07-28T20-28-28-799Z.json`
> — gitignored under rule 3 of `RELEASE-EVIDENCE.md`, because the repository holds the contract and the run
> holds the output. **Its verdict: 16 gates PASS, 5 BLOCKED, aggregate BLOCKED — exit 2, NOT PROMOTABLE.**
> PASS: runtime, typecheck, lint, **unit-tests**, invariants, rater-purity, authority-coverage, traceability,
> coverage, seed, **acceptance**, design-audit, perf (1), visual (5), a11y (4), e2e (6). BLOCKED:
> `identity-leak` (no denylist — set the `IDENTITY_DENYLIST` secret or a gitignored `.identity-denylist.local`),
> `fixtures` (9 not vendored: rater-48-tests, rater-504-sweep, zone-tariff-v1, invoice-500-replay,
> concierge-parse-50, customer-roster, legacy-import-formats, legacy-export-replay, synthetic-blitz-3100),
> `rater-parity`, `invoice-parity` and `concierge-parse` (engagement fixtures not vendored). **All five are an
> absent private input; not one is a code defect, and no commit can close any of them.** Read the change
> precisely: *not promotable* is unchanged, and its reason is now completely different and much narrower — no
> longer "no evidence exists" but "the evidence exists, it is complete, and it names five absent inputs".
> Nothing else in the statement moves: production is still unprovisioned, staging is still certified in no
> respect, the ten external holds are still open, and V1 is still not ready to launch.

What would change that sentence, in dependency order: **reboot** (clears H1 and ~~the four † claims~~ **the one
† claim — corrected 2026-07-27: §1.1 `:20` defines the dagger and exactly one row carries it, the `anchors/run`
`FIXED †` at `:358` — as the close-out statement above already says, "the one High among them"**) → run `pnpm verify:merge`
(produces the first evidence record this SHA can have) → vendor the nine engagement fixtures and bind
`IDENTITY_DENYLIST` (clears five fail-closed gates) → provision staging resources and supply
`preflight --state` (converts *unproven* to *proven*) → ~~deploy and smoke~~ **bind `SMOKE_API_BASE` + the
staging JWT secret and smoke the environment that is already deployed** (clears H9; field rows 4 and 7
additionally need the three undeployed workers, two provisioned tenants and a device) → back up and reconcile a
restore (clears H8, then the drill) → name the on-call human and build the archive tier (the two holds no
command can see). Nothing in this repository can do any of them.

**Updated 2026-07-28 (post-reboot, HEAD `3fc592b`) — the first two steps of that chain are DONE; the rest is
unchanged.** The reboot happened: `workerd --version` answers, zero processes sit in `UE`, H1 is RESOLVED and
the last dagger is off this file. `pnpm verify:merge` then ran and produced this branch's first evidence
record — **16 PASS, 5 BLOCKED, still NOT PROMOTABLE** (⟨4⟩ above). What remains, in the same dependency order:

1. **Vendor the nine engagement fixtures and bind `IDENTITY_DENYLIST`.** These five gates are now the *only*
   non-PASS rows in the **merge** profile at this SHA, so this step alone is what stands between the build and
   a promotable *merge* record. Nothing else in `verify:merge` is red. It buys nothing beyond R1: R2 and
   everything above it still need steps 2–5, and the release profile adds four gates none of this touches.
2. **Provision staging resources and supply `preflight --state`** — converts *unproven* into *proven*.
3. **Bind `SMOKE_API_BASE` + the staging JWT secret and smoke the environment that is already deployed**
   (clears H9; field rows 4 and 7 additionally need the three undeployed workers, two provisioned tenants and
   a device).
4. **Back up and reconcile a restore** (clears H8, then the drill).
5. **Name the on-call human and build the archive tier** — the two holds no command can see.

Nothing in this repository can do any of them, which was true before the reboot and is still true. The reboot
did not move the build one step closer to launch; it moved it from *unmeasured* to *measured*, which is a
different and smaller thing, and the whole point of this page is not to confuse the two.
