# WP-03 — Design system package + map shell — checklist

## DoD (genesis/08 §03)
- [x] **Squint CI passes** — all six doc-07 §06 audits (color/contrast/radius/shadow/gradient/font + the WP-03 additions: case, motion, 1px-divider) run clean on the real primitives + 5 screens. `pnpm audit:design` → `design audit: clean`. The audits catch both CSS-kebab and JSX-camelCase inline styles (the syntax this codebase writes).
- [x] **5 canonical screens** built on the map shell (Command, Portal, Status, Driver-A2, Evidence-email), composing `@shuddl/design` + `@shuddl/map`, each dogfooded audit-clean. Playwright screenshot diff (audit #6) ships as `tests/visual/screens.spec.ts` — **advisory** (REQ-158); blessed refs generate on the first browser-capable CI run.
- [x] **Map renders 1K entities** — architecture proven: GL `circle`/`symbol` layers (never DOM markers), `setFeatureState` for state, one clustered source + throttled `setData`. Deterministic 1,000-entity fixture (`fleet1k`, seeded, 861 healthy / 138 at-risk / 1 exception). The 60fps/30fps measurement is a Playwright perf harness (`pnpm perf:map`, advisory, p95 vs 16.6ms) that runs on a browser-capable machine.

## Delivered (REQ → evidence)
| REQ | Evidence |
|---|---|
| REQ-073 full-viewport map backdrop | `packages/map/src/MapCanvas.tsx` (full-viewport); Command/Portal/Status screens |
| REQ-074 party-scoped + city-gen | `useFleet` party scoping + `generalize.ts` (coarse pre-OFD, exact post-OFD), unit-tested |
| REQ-075 self-hosted greige style | `packages/map/src/style.ts` `greigeStyle(tileUrl,glyphsUrl)` — tile/glyph URLs are config; self-host is deploy-time |
| REQ-076 entity grammar, no new colors | `entities.ts` `entityLayers()` — chevron/square/hollow/pulse via geometry+feature-state; clusters sized by count |
| REQ-077 exception dims world to 35% | `setWorldDim` (leaves via feature-state, clusters via `maxStatus`); the pulse sine in MapCanvas |
| REQ-078 teal = progress only | audited: teal (`#00C4B4`) appears only on the `eta` layer; nowhere in primitives/screens |
| REQ-079 1K entities perf | `fleet-1k` deterministic fixture + `perf.spec.ts` advisory p95 harness |
| REQ-080 click → dark lens panel | `MapCanvas` `onSelect` + `LensPanel.tsx` (`--ink-dark`); map never navigates away |
| REQ-115 error state | `ErrorState` (FAILED display + retry), tested |
| REQ-145/146/147/148/149 design law | `tools/design/audit.ts` six audits + `packages/design` primitives; contrast lock unchanged |

## Deferred slices (owned by later WPs)
- Self-hosted Protomaps/OpenFreeMap tiles on R2 + offline service-worker cache → deploy / WP-05 (Driver airplane-mode). Style + entities built now against a config tile URL.
- Self-hosted JetBrains-Mono glyph PBFs (on-map mono) → deploy line item; HTML chrome uses real fonts now.
- Live Durable-Object board fan-out (marks move as events land) → WP-10; the `useFleet` seam + synthetic source ship now.
- Command-bar NL actions, queue actions, KPI click-through → WP-10 (visual shells ship now).
- Design CI advisory→blocking flip → WP-10 exit (REQ-158).
- Playwright blessed screenshots + measured perf numbers → first browser-capable CI run (harnesses self-skip in a headless/no-network sandbox so `pnpm verify` stays green).

## New REQ rows proposed
- (none — no scope discovered outside the register)

## Assumption log
- Self-hosted tiles/glyphs are deploy-time infra; WP-03 parameterizes them.
- Screenshot diff + perf are advisory (REQ-158) until WP-10 exit; they report, never block.
- The fleet/event contract the map consumes is the WP-02 lens shape via a typed adapter + synthetic source; live fan-out is WP-10.
- Demo screens use invented generic names; for zero REQ-167 risk they should adopt the SEED-1 "SEED CUSTOMER NN" convention (flagged for the exit audit).

## WP-exit audit swarm (REQ-119)
- [ ] Adversarial audit at WP-03 exit; findings → REQ rows/defects; no open Criticals at close.
