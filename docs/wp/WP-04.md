# WP-04 — Rater service — checklist

## DoD (genesis/08 §03)
> "All existing tests pass in service form; tenant-0 config reproduces the ported engine's quotes exactly (REQ-165); below-floor emits approval event."

- **Engine + service built and adversarially reviewed** against a config we control (SEED-1). Freight → FSC/accessorials → three floors → approval matrix (interline executing-share) → anomaly detection → `POST /v1/rate` (server-side gate, tenant-scoped, ledger events). — **OBSERVED**.
- **Below-floor emits an approval event** (`approval.requested`, dual for a LOSS) server-side. — **OBSERVED** (`workers/api/test/rate.test.ts`).
- **"48 legacy tests pass" (REQ-027) and "reproduces tenant-0 quotes exactly" (REQ-165)** — **[CONFIRM]-PENDING**. The 48-tests, 504-sweep, and tenant-0 tariff live in the engagement workspace (`fixtures/manifest.json` → `pending`; genesis/13; REQ-163 "reference never merge"). The **parity harness** (`tools/rater/parity.ts`) runs them the moment they are vendored+hash-pinned and **loud-skips (exit 0 advisory) until then — it cannot false-green** (proven: a self-consistent unpinned set FAILS exit 1). Closing REQ-027/REQ-165 is a `[CONFIRM]` engagement step, not code.

## Delivered (REQ → evidence · OBSERVED unless marked)
| REQ | Evidence |
|---|---|
| REQ-004 pricing from measured physics, zero hardcodes | `packages/rater/src/engine.ts` (zone+deficit-weight freight from config); `tools/checks/rater-purity.ts` class-as-edge-adapter lint; no rate/zone hardcoded — all from `rate_config` |
| REQ-041 no price on air / weightless → UNKNOWN | `engine.ts` weight+dims gate (whole-pound integer or UNKNOWN `missing_physics`); `routes/rate.ts` server-side half; tested (`freight.test.ts`, `rate.test.ts`) |
| REQ-048 below-floor named approval; LOSS = dual | `packages/rater/src/approval.ts` matrix (below-target → single/ops; below-contribution → dual/finance); `approval.test.ts` |
| REQ-040 interline floor = executing share, never gross · $222,084/35-lb permanent regression | `approval.ts` `executingShare` (split must total 10000; fail-loud on partial); `anomaly.ts` `detectAnomaly`; `fixtures/anomaly/the-222084-case.json` (in-repo, hash-pinned `vendored`, REQ-167-clean); `anomaly.test.ts` |
| REQ-005 agent.acted cites basis | `routes/rate.ts` emits `agent.acted` (agent "rater") citing the `quote.priced` id + every `rate_config_id` |
| REQ-024 no LLM in the engine | `tools/checks/rater-purity.ts` `llm_in_rater` rule (universal over `packages/rater/src`), in `verify` |
| REQ-025 tenant isolation | `routes/rate.ts` + `rate-config.ts`: tenant strictly from the JWT session → `tenantDb`/`idFromName`; `RateBody.strict()`; `rate.test.ts` isolation (events in TENANT_A_DB, zero in B) |
| REQ-030 server-side gate | `routes/rate.ts` enforces the floor gate + emits `approval.requested`; a UI cannot bypass it |
| I5 every quote pins rate_config versions | `packages/rater/src/price.ts` pins `[zone_tariff, floors, fsc, accessorials]` as `id@version`; `QuotePricedPayload.versions` non-empty (contract) |
| REQ-027 48 tests + 504-sweep in service | **[CONFIRM]-PENDING** — harness `tools/rater/parity.ts` (runs when vendored+hash-pinned); in-repo stand-ins: 504-cell monotonic sweep (`packages/rater/src/sweep.ts`, deficit-weight proven at a trap boundary) |
| REQ-165 tenant-0 quote parity | **[CONFIRM]-PENDING** — same harness; SEED-1 rating-config (`tools/seed`) is the in-repo test vehicle |
| REQ-151 asset-mode no-tariff → no sell (cold-start half) | `rate-config.ts` returns `null` when a required kind is missing → `200 UNKNOWN no_tariff`, no event |

## Deferred slices (owned by later WPs — recorded, not dropped)
- **`approvals` table read-model** — the approval is event-sourced now (`approval.requested`/`approval.decided`); the queryable `approvals` row projection is WP-10's approvals-queue view (`v_queue_approvals`).
- **Anomaly → exceptions-queue ALARM** — a pricing anomaly is recorded at pricing on `quote.priced.basis.anomaly` (REQ-040, atomic, redacted from counterparties). Surfacing it as an exceptions-queue alarm/pulse is the **Watchtower** (REQ-036, WP-11), which reads anomalies from the ledger. Intentionally NOT a `/rate` job (a pricing anomaly is not a physical-custody exception and must not depend on a custody `parties` FK).
- **Multi-factor cost surface** — floors ride on the linehaul-freight cost proxy (`price.ts` `costBasis`, the single seam). The real cost surface (stops/touches/cube-miles/dwell/density, doc 01 Rater) and interline **pro-rated per-leg** floors are a tenant-0/engagement input. Consequence: interline floors compare the executing revenue **share** against the **full-move** floor — conservative (over-escalates approvals, never under; the anti-$222K direction).
- **`transit_matrix` rate_config kind** → WP-08 (booking honest windows).
- Minor follow-ups (noted, non-blocking): dims-value guard when density becomes load-bearing; `proposed_sell_cents` anomaly coverage; all-zero-floors config sanity; body-fingerprint on the idempotency key; `effective_ts` unit is treated as ms (the config-pack writer must honor that — documented in `rate-config.ts`).

## Assumption log
- Cost basis = linehaul freight (the `costBasis` seam); documented in `price.ts`.
- Rating math (deficit-weight, half-up via BigInt `mulDivHalfUp`/`roundHalfUp`) — integer cents, no float touches money; monotonicity property-tested.
- Multi-event `/rate` emission uses deterministic event ids (keyed off the Idempotency-Key) so a retry is resumable-idempotent (no duplicate/orphaned `quote.priced`); true single-transaction atomicity across the 3 events is a follow-up (transient infra failure without a client retry can still orphan — mitigated by the required Idempotency-Key).

## New REQ rows proposed
- None. All WP-04 work maps to existing register rows; the deferred slices map to REQ-036 (WP-11), WP-10, WP-08. REQ-027/REQ-165 remain OPEN (`F0-*-SPEC'D`) pending the engagement fixtures.

## WP-exit audit swarm (REQ-119)
Four adversarial auditors ran at exit (money/correctness, law compliance, service/isolation/gates, honesty/DoD/contracts), each attempting to BREAK a defect class and running the code to confirm findings. **No open Criticals at close.** Found + fixed (commit closing them):
- **CRITICAL** — the parity harness false-greened on self-consistent, present-but-unpinned data → now requires the three manifest rows `vendored` **and** on-disk hashes matching the pinned `sha256` before it can green (`tools/rater/parity.ts`; re-proved a self-consistent unpinned set FAILS exit 1).
- **HIGH** — a malformed interline split (`Σ split_bps ≠ 10000`) committed `quote.priced`+`agent.acted` then 500'd (un-retryable orphan) → now rejected at the Zod boundary (`RateBody` refine → 400, zero events).
- **HIGH** — WP-04 absent from `tools/traceability/active-wps.json`, hiding the REQ-041 orphan → WP-04 activated + REQ-041 annotated; `check:traceability` green.
- Confirmed correctness fixes: fractional `weight_lb` (→ UNKNOWN, no crash); freight core float multiply (→ `mulDivHalfUp`, off-by-1¢ closed); `rate_config` negative cents (→ non-negative refine).
- **Strong negatives (attacked hard, held):** tenant isolation unbreakable (tenant strictly from JWT); auth correct; no-price-on-air end to end; anomaly atomic + redacted from counterparties; weight-monotonicity sound; I5 complete; no rubber-stamp tests; SEED-1 identity-clean (REQ-167); REQ-027/REQ-165 not fabricated.
