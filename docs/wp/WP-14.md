# WP-14 — PLG + metering (signup → first quote) · Close-out

**Status:** complete, merged to main locally. **Branch:** `wp-14-plg`. **Posture:** the entire PLG surface ships **DARK / fail-closed** — nothing charges, provisions a real tenant, emits credits, or opens signup until an operator binds its flag/secret/keys at **R4** (GTM-gated, `genesis/12`, never before M-H).
**REQs:** REQ-121 (provisioning/signup) · REQ-122 (Spark tier + AI credit allowance) · REQ-123 (Stripe credits + metering + platform tenant) · REQ-124 (Pro/Scale tiers + usage, no seats) · REQ-125 (abuse controls) · REQ-126 (support: copilot-first) · REQ-127 (Migrator drag-drop) · REQ-151 (cold-start rating) · REQ-162 (PROOF-TO-CASH) · REQ-060 (hazmat) · reused REQ-025/030/083/130. **DO NOT BUILD:** REQ-138 (ToS/Privacy/DPA — CONFIRM-2/counsel; structurally blocks *opening* public signup); the WP-15 171-col continuous overlay.
**DoD (genesis/08):** "Stranger completes signup→first quote unassisted <10 min; metering matches event counts exactly." — **met** (the demo-#2 e2e prices a quote for a freshly signed-up tenant, flags-ON in-test; `sum(usage_credits.metered) == COUNT(agent.acted)` exactly, by recompute).

## What shipped — purely additive, all DARK
- **Platform tenant `_platform`** (REQ-123) — a reserved revenue tenant (server-side-only `resolvePlatformTenantDb`), two-way isolation-locked (SECURITY CLEAR).
- **Pool-based provisioning** (REQ-121) — `POST /pub/signup` (behind `PROVISIONING_ENABLED`, OFF) atomically claims a pre-migrated pool D1 (CAS-flip in one batch), inserts tenant + admin + `usage_credits`; a claimed tenant reads **and** writes only its own pool D1 (the sequencer `#append` resolves claimed slugs). **No 4th surface** — workspace = the existing Portal/Command; signup is a pre-auth `/pub` route.
- **Cold-start rating** (REQ-151) — a brokerage market+margin tariff (honest floors: contribution=break-even ≤ full ≤ target) seeded at provisioning so a stranger prices day one; asset/no-tariff → UNKNOWN (no price on air).
- **Migrator drag-drop** (REQ-127) — reuses the WP-10 intake verbs (gate parity); the gap-row law is airtight (every unmapped/colliding/rate-sheet column → exactly one anomaly, values retained in `refs`/`external_refs`; prototype-safe).
- **Exact metering** (REQ-123) — a cross-DB recompute sweep (`agent_runs ⋈ events`) OVERWRITES `usage_credits.metered` (never `+=`); `sum(metered) == COUNT(agent.acted)` exactly.
- **Stripe credits** (REQ-123, DARK) — `NotConfiguredBilling`; a signature-verified idempotent webhook; `credit_purchase` `invoice.issued` + `payment.received` on `_platform` via a secret-gated `/internal/platform/*` route (the sequencer POD gate is exempted ONLY for `_platform` credit invoices — customer invoices still POD-gated); settles paid on any webhook order.
- **Spark caps** (REQ-122/125) — a per-tenant `SparkMeter` DO at the **agent-convenience seam only**. **THE INVERSION (structurally proven):** the physical-truth path (`stop.*`/`pod.signed`/`delivery.evidenced`/`custody.transferred`) + gates + invoicing ALWAYS run for an over-cap tenant — "credits throttle conveniences, not truth."
- **Entitlements** (REQ-060/162) — `tenants.plan`/`policy` levers: hazmat booking fail-closed (no self-grant), the PROOF-TO-CASH plan-flag authority.

### How the product/GTM REQs are addressed (partial/DARK — honest)
- **REQ-124 (Pro/Scale tiers + usage, no seats):** the *mechanism* ships — `tenants.plan` tiering + the usage meter (`usage_credits`, per tenant+period, never seat-based). Tier *numbers* stay `[HYPOTHESIS]` (REQ-130, re-based in WP-16). Live tier packaging is R4.
- **REQ-125 (abuse controls):** the `SparkMeter` is the per-**workspace** velocity control, and the per-action count bounds LLM cost. The per-**IP** velocity dimension is a Cloudflare **edge rate-limit** (a deploy note, like REQ-193) — provisioned at go-live, not an in-Worker gate.
- **REQ-126 (support: copilot-first; escalation; SLA at Scale):** copilot-first = the existing WP-10 Command copilot. Human escalation + the Scale-tier SLA are product/ops deliverables (tracked, not code) — DARK until the support model activates.

## Additive guarantee — mechanically proven
`git diff main...wp-14-plg`: **0 new D1 table** (21/22 — a Durable Object / KV / worker is not a table), **0 new event kind** (35 frozen — credits ride the dormant-but-valid `money_lines.kind='credit_purchase'`; `hazmat` is an OPTIONAL field on `booking.created`, and **`check:seed` still verifies** — the canonical byte law is intact), **no 4th surface**. The sequencer tenant-resolution change weakened NO ledger law (append-only guards, seq/prev_hash/hash chain, device-sig, one-booking, credit/evidence gates all intact; no suite weakened).

## REQ-119 exit audit — CLEAR-TO-CLOSE
An 8-lens adversarial swarm (Spark-inversion, platform/write-path isolation, money-as-projection + exact metering, dark-posture, gap-row law, provisioning/signup, cold-start rating, additivity/ledger-laws) → independent refutation. **Zero Critical.** Core laws PROBED CLEAN. It surfaced one merge-blocking rule-10 violation (Finding B) + 3 Low — **all fixed additively, RED-first** before close:
1. **B (HIGH, blocker)** — the rate-sheet import silently dropped non-rate columns/rows → now emits exactly one anomaly per non-consumed column + one for unconsumed rows (`ccd53e8`).
2. **A (Low)** — an out-of-order (ACH/unpaid) credit settlement left an invoice stuck `issued` → the unpaid branch now runs the re-runnable settle catch-up (`097ad61`).
3. **C (Low)** — a `__proto__` column header dropped its retained values → prototype-less retention containers (`ccd53e8`).
4. **D (Low)** — a signup slug/email collision returned 500 → a clean 409 (`3c371e3`).

## R4 go-live gates + deferred items (see docs/ops/GO-LIVE-CHECKLIST.md)
Every live-flip is DARK today; before R4:
- **REQ-138 legal (ToS/Privacy/DPA)** — the door-opener; public signup legally cannot open without counsel sign-off (do-not-build).
- **Stripe keys + `STRIPE_WEBHOOK_SECRET`** (NotConfiguredBilling); **`PROVISIONING_ENABLED`** (signup/provisioning); **`PLATFORM_INTERNAL_SECRET`** (the credit-append route) — all operator-injected, never in toml.
- **The tenant-D1 pool** — ops pre-creates + migrates the pool D1s the provisioner claims (a pool-refill runbook).
- **Spark $5 tier plan-flag** on `tenants.plan`; tier numbers stay `[HYPOTHESIS]` (REQ-130/WP-16).
- **Async/ACH checkout** must NOT be enabled until the (now-landed) unpaid-branch settlement is confirmed in staging (the covered payment reconciles).
- **Internal-route hardening** (Task-10 review, Low): bind `credit-settle`'s payment→invoice (`payload.invoice_id === invoiceId`); the `#resolveDb` memo platform re-check — both internal-only/DARK, harden before the secret is bound.
- **REQ-125 per-IP edge rate-limit** (Cloudflare rule) before public signup GA.
- **Platform-chart GL** (`GL_PLATFORM_CREDITS_AR`) needs its own parity coverage if a platform journal export is ever built.

## Verification
`pnpm verify` green: typecheck · lint · test (all packages incl. the new billing worker + PLG suites) · check:invariants (21/22 tables, 35 kinds) · rater-purity · traceability · identity · fixtures · parities · **seed (SEED-1 hash verified)** · audit:design.
