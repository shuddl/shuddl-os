# WP-02 — Ledger core — checklist

## DoD (genesis/08 §03) — real output pasted

- [x] **35 event kinds round-trip** — `packages/ledger/test/roundtrip.test.ts` + the frozen canonical snapshot (`__snapshots__/roundtrip.test.ts.snap`). The snapshot is the byte law; SEED-1's hash changed this WP but the ledger byte-format snapshot did NOT (`git diff --exit-code` on it is clean).
  ```
  $ pnpm --filter @shuddl/ledger test roundtrip
   ✓ test/roundtrip.test.ts (36 tests) 161ms
        Tests  36 passed (36)
  ```
- [x] **Chain verifies after 10K events** — `packages/ledger/test/chain.test.ts` (10,000 events verify < 5s; one tampered byte breaks at the right seq).
  ```
  $ pnpm --filter @shuddl/ledger test chain
   ✓ test/chain.test.ts (3 tests) 395ms
     ✓ REQ-002 DoD: chain verifies after 10K events > 10,000 events verify < 5s; one tampered byte breaks at the right seq  381ms
        Tests  3 passed (3)
  ```
- [x] **Correction semantics net to zero in the GL export fixture** — `packages/ledger/test/gl-netting.fixture.test.ts` (vendored fixture, hash-pinned; reconciles to the penny).
  ```
  $ pnpm --filter @shuddl/ledger test gl-netting
   ✓ test/gl-netting.fixture.test.ts (5 tests) 128ms
        Tests  5 passed (5)
  ```
- [x] **Lens tests prove scoping** — `workers/api/test/lens-adversarial.test.ts`. Cross-lens reads fail (REQ-015 DoD); case 9b's geo sweep carries INDEPENDENT guards, which is what makes the suite able to fail if scoping regresses (see routes review, commit 54499ad).
  ```
  $ pnpm --filter @shuddl/api test lens-adversarial
   ✓ test/lens-adversarial.test.ts (29 tests) 505ms
        Tests  29 passed (29)
  ```

## Close-out gate evidence (the 6 DoD items)

```
1) pnpm --filter @shuddl/ledger test   -> Test Files 18 passed (18) | Tests 270 passed (270)  [incl. seed-load.test.ts, 4 tests]
2) pnpm check:seed                      -> SEED-1 hash verified
3) pnpm check:traceability              -> traceability: no orphans in either direction (active: WP-01, WP-02)
4) pnpm verify                          -> exit 0 (typecheck → lint → tools+contracts+ledger+api tests → invariants → traceability → identity → fixtures → seed → design)
5) git diff --exit-code roundtrip.snap  -> CLEAN (exit 0) — SEED-1 hash changed; the ledger byte-format snapshot did NOT
6) pnpm check:invariants                -> invariants OK — 21/22 tables, events append-only (5 migration files, lock: check)
```

`pnpm --filter @shuddl/api test` → `Tests 81 passed (81)` (sequencer 10K/concurrency + adversarial lens + isolation, folded into `pnpm verify`).

## Delivered (REQ → evidence)

| REQ | Evidence |
|---|---|
| REQ-002 | packages/ledger/src/canonical.ts (JCS byte law) + chain.ts + test/chain.test.ts (10K < 5s); every SEED-1 stream verifyChains green (test/seed-load.test.ts) |
| REQ-011 | packages/contracts/src/events.ts — 35-kind discriminated-union envelope; round-trip owner test/roundtrip.test.ts + frozen snapshot |
| REQ-012 | packages/ledger/src/projection/money.ts — corrections are reversal events (correction_credit/debit); nets to zero in test/gl-netting.fixture.test.ts |
| REQ-014 | packages/ledger/src/merkle.ts (RFC 6962) + anchor.ts + tsa/client.ts (RFC 3161 DER) + workers/agents daily cron; a POD hash verifies against a TSA receipt |
| REQ-015 | packages/ledger/src/lens.ts + visibility.ts + redact.ts; adversarial cross-lens read fails in workers/api/test/lens-adversarial.test.ts |
| REQ-005 | packages/contracts/src/events.ts — AgentActedPayload requires `basis` (min 1) at the boundary; test/events.test.ts |
| REQ-009 | packages/ledger/src/projection/passports.ts — pod/exception/osd/custody accrue counters; test/projections.test.ts (OTD deferred, see below) |
| REQ-057 | division columns on shipments/money_lines/invoices (db/tenant/migrations/0002_domain.sql), filterable in gl/export.ts; test/gl-netting + schema-domain |
| REQ-001 | packages/contracts/src/events.ts — EvidenceRef; every event captures its evidence at the point of change; projections derive, never re-key (WP-02 slice; device capture → WP-05) |
| REQ-030/I2 | packages/ledger/src/gates/invoice-gate.ts — no invoice without a signed POD; enforced server-side in the sequencer; test/invoice-gate.test.ts |
| REQ-025 | isolation suite extended to the ledger routes + DO identity-equality tenant pinning (workers/api) |
| REQ-155 | tools/seed/generate.ts (WP-02 envelope, canonical hash chain) + tools/seed/load.ts (FK-ordered D1 loader) + packages/ledger/test/seed-load.test.ts; hash repinned 70ede33ab8df… → 83bf58982212… |
| REQ-118 | tools/traceability/active-wps.json flipped to [WP-01, WP-02]; check:traceability drops the `--wp` override; all nine WP-02 rows annotated at real sites, no orphans either direction |
| REQ-131 | docs/security/threat-model.md — WP-02 review rows added (chain-fork, seq races, TSA receipt validation, REPLACE-under-recursive_triggers, lens/geo leakage) |
| I1/I3/I8 | append-only guards (BEFORE INSERT/UPDATE/DELETE) on events/positions/money_lines; RAISE-only trigger law + partition-aware table budget (tools/checks/invariants.ts) |

## Deferred slices (not lost — owned by later WPs)

- Board-DO fan-out + live map subscriptions → **WP-03** (the sequencer exposes a `notifyBoard` no-op seam).
- Agent queue triggers → **WP-06+** (same seam).
- On-device key generation + offline batch merge + co-sign ack → **WP-05** (the columns, clientView, and the (device_id, device_seq) unique reserve land now).
- REQ-001 device-side capture leg (driver PWA) → **WP-05** (the evidence-carrying event schema lands now).
- Real TSA endpoint smoke → **blocked on the F1 [CONFIRM]**; `HttpTsaClient` is fake-tested only (against `FakeTsaClient`). This is a WP-02 exit line item.
- CMS cert-chain verification of TSA receipts → **WP-16** audit item (raw `.tsr` bytes retained in R2, verifiable offline forever).
- OTD / on-time passport accrual → **WP-08** (on-time requires an appointment window, owned by the Scheduler; no schema-valid `on_time` arrives today).
- Cash application for `payment.received` → **WP-11**.
- REQ-111 log→ledger unification → **WP-11** Watchtower (would need a 36th event kind = register amendment; logs stay event-shaped for now).
- Full-tenant export (REQ-010) → **WP-11**; nightly D1 snapshot job activation → **F1-A** creds (spec in docs/ops/dr-backups.md).

## Build notes (assumption log)

- **Seed change is deliberate** (WP-01 rule): the generator now emits the real WP-02 envelope, so `tools/seed/seed.hash` was regenerated (`pnpm seed`) 70ede33ab8df… → 83bf58982212…. Stated here and in the commit body; never silent.
- **`generateSeed`/`seedHash` are async** (canonical hash chaining via `crypto.subtle`). The generator is now workerd-safe (no `node:*`) so the pool-workers seed-load test can import it; the Node CLI (`pnpm seed`, `pnpm seed:load`) lives in `generate.cli.ts` / `load.cli.ts` (dynamic node:fs / wrangler resolved from workers/api).
- **The loader honors FK ordering** — parties BEFORE any event. Proven load-bearing: disabling parties-first turns seed-load.test.ts red with `D1_ERROR: FOREIGN KEY constraint failed` (the passports.party_id FK aborts the whole event batch).
- **REQ-001 was only INCIDENTALLY matched** by the register-order assertion in traceability.test.ts (`rows[0]?.req_id`). Verified the detector's teeth: neutralizing that token yields `FAIL spec'd-but-unbuilt: REQ-001`. Added the real annotation at packages/contracts/src/events.ts (EvidenceRef). REQ-001 is WP-02/05 — only the ledger-capture half is ours; the device-side capture leg is WP-05.
- **Traceability flip was a near no-op for the other 8 rows** — Tasks 1–16 had already annotated REQ-002/005/009/011/012/014/015/057 at their real implementation sites, so flipping active-wps to [WP-01, WP-02] stayed green (only REQ-001 needed a real home).
- Governance prose (CLAUDE.md, README, plans, BUILD-PROMPT) stays excluded from the orphan scan; implementation docs (docs/wp, docs/security, docs/ops) count as deliverables.

## New REQ rows / register notes proposed (append-only; owner ratifies — NOT edited here)

1. **events PK is `(stream_id, seq)`** with nullable `shipment_id` + CHECK (doc 10 says `(shipment_id, seq)`) — required for shipment-less kinds (agent.acted, message.*, authority.flipped, pre-booking quote.*).
2. **`positions` is a physical partition of doc-10 entry #9** — the I8 guard counts it inside that entry (PARTITION_TABLES pinned to exactly `{positions: "events"}`); effective count stays 21.
3. **`documents.kind` gains `'tsa_receipt'`** (enum addition, not a table/event-kind budget item).
4. **Ledger timestamps are integer epoch-ms** (canonicalization law); doc 14 §04's ISO-8601 applies at rendering only.
5. **Integer-only canonical numbers** — cents / microdegrees (lat_e6/lon_e6) / basis points; a documented JCS restriction.
6. **`shipments.tenant` dropped** — redundant under physical per-tenant D1 (the database IS the tenant).
7. **REQ-111 log→ledger unification deferred to WP-11** — making logs ledger rows needs a 36th event kind (register amendment).
8. **OTD passport accrual deferred to WP-08** — on-time requires an appointment window (owned by the Scheduler).
9. **`SplitComputedPayload.total_cents` narrowed to non-negative** (a split allocates a non-negative pool).
10. **Real-TSA endpoint smoke is a WP-02 exit line item, blocked on the F1 [CONFIRM]** — `HttpTsaClient` is fake-tested only.

## WP-exit audit swarm (REQ-119)

- [ ] Adversarial audit at WP-02 exit (50-agent pattern); findings → REQ rows/defects; no open Criticals at close.
