# WP-16 — Audit swarm + launch gate (the final WP) · Close-out

**Status:** complete, merged to main locally. **Branch:** `wp-16-launch`.
**Posture:** an ASSURANCE + LAUNCH-GATE work package — it adds no product feature. It closes the last fail-open gate, hardens the crown-jewel crypto, machine-checks 100% register coverage, runs a full adversarial pass over every module, and names the launch-gate calendar objects a merge cannot close. **The build is now at the launch gate.**
**REQs:** REQ-119 (adversarial audit swarm, no open Critical — WP-all) · REQ-136 (pen-test basics) · REQ-130 (pricing re-base — CONFIRM-gated, out-of-repo) · REQ-161 (ICP/first-25 CRM — GTM, out-of-repo) · discovered REQ-206..213 (the audit fixes + deferrals).
**DoD (genesis/08:70):** "Zero Critical; coverage report 100%; the five doc-00 acceptance tests pass on video." — the **in-repo half is met** (zero open Criticals; `check:coverage` 100%; the acceptance spine green); the **filmed-video half is a tenant-0 launch-gate object** (see the two-tier split).

## What shipped — the nine launch-gate deliverables (all additive)
1. **`check:coverage`** (REQ-118/119) — a machine gate classifying all **213** register rows into 8 dispositions, failing on ANY unaccounted row (built-without-annotation / deferred-without-recorded-home / unknown-status), with a stale-tag drift check. Wired into `verify` AND `ci.yml`. Closes the 34 rows the active-WP orphan filter silently exempted.
2. **Identity-leak lint fails CLOSED in CI** (REQ-167) — the single genuine fail-open gate: with `CI`/`REQUIRE_DENYLIST` set and no denylist, it now exits non-zero (was warn-and-pass). A real tenant/person/incumbent name can no longer ship green. `ci.yml` pins `REQUIRE_DENYLIST=1`.
3. **TSA CMS signature + cert-chain verification** (REQ-014) — the deferred-to-WP-16 crypto: `verifyTsaSignature` verifies the SignerInfo signature over the re-tagged signedAttrs (RSA + ECDSA), binds `messageDigest` to the TSTInfo, chains the signer to a configured trust anchor with validity + the timestamping EKU, binds `expectedImprintHex` at the source, all fail-closed. Self-generated test CA (adversarially reviewed: no bypass). Revocation + real anchors are deployment config; EKU critical/sole-purpose is a recorded follow-up.
4. **Server-side geo-privacy verified** (REQ-074) — the WP-03 "generalizePosition is client-consumed" concern proven closed: `redactEvent` coarsens PARTY geo to ~11 km + drops accuracy server-side (structural, until OFD), driver/tenant exact; the public status route + the party lens both generalize server-side; the board is tenant-only. The missing adversarial test added; the stale note flipped.
5. **The 2026-07-15 audit Highs reconciled** (REQ-206/207/208) — idempotency now caches ONLY 2xx (a 4xx retry re-runs — closes the silent evidence-loss), the design-CI scans runtime `*-style.json` + enforces the 5-token count, and the map bearing uses the great-circle formula + an epsilon hold-at-rest. Each fixed RED-first + given a register row.
6. **`pen-test-basics.md`** (REQ-136) — a STRIDE-per-surface review mapping each threat to the proving suite (5 isolation suites, auth HS256/alg:none, money-kind + device-sig + source forgery, EDI HMAC, public MAC-verified surfaces), the 5 WP-16 hardenings CLOSED, and the out-of-repo preconditions + residual debt named. Report clean for the in-repo perimeter.
7. **The five-demo acceptance spine** (REQ-119) — `pnpm test:acceptance` runs exactly the 5 doc-00 causal-chain spine tests as a named set; the manifest (`docs/wp/acceptance-demos.md`) states per demo the in-repo spine vs the FILMED tenant-0 delta the spine refuses to fabricate. The Playwright browser layer + the flags-on dev-server wiring are documented as the concrete remaining in-repo increment.
8. **The full REQ-119 launch swarm** — 8 parallel default-refute lenses over ~20 targets × the 12 skills × the Ten Laws + I1–I8 + the budgets. **ZERO open Criticals.** Report archived: `docs/audits/2026-07-22-wp16-launch-gate-audit.md`. Findings fixed RED-first (the invoice-void HIGH + the redaction LOW, REQ-209/210; the CI-wiring MED, REQ-211) or deferred with a recorded home (REQ-212/213, vNEXT).
9. **The launch-gate ledger finalized** — `docs/ops/GO-LIVE-CHECKLIST.md` now carries the WP-16 audit findings + the definitive out-of-repo launch-gate objects.

## Additive guarantee — mechanically proven
`git diff main...wp-16-launch`: **0 new D1 table** (21/22; migrations byte-identical), **0 new event kind** (35 frozen; `check:seed` verifies — the invoice-void fix is a PROJECTION change, not an event/schema change; the `void` invoice status needed no migration, no CHECK exists), **0 new surface**, **0 new canonical view** (11/12). Every fix is additive; the only behavior changes are the two audit fixes (invoice-void read-model consistency; idempotency 2xx-only), both closing a defect, each RED-first with a reconciliation test.

## REQ-119 launch audit — CLEAR-TO-CLOSE
8 lenses, default-refute. **Zero Critical / open High** at close. The crown jewels probed clean at whole-build scale (canonical hash + chain, append-only, tenant isolation, gate parity, money-as-projection, I6 redaction, the WP-15 source-aware overlay, PLG/driver, the do-not-build guards). The one HIGH (invoice-void AR divergence — latent, no producer today) was FIXED; the 2026-07-15 Highs confirmed closed; two defense-in-depth LOWs deferred (REQ-212 complete append-only guards; REQ-213 structural DO-side source lock). Full report: `docs/audits/2026-07-22-wp16-launch-gate-audit.md`.

## Two-tier DoD — what this merge closes vs the LAUNCH GATE
- **In-repo, this WP closes:** zero open Criticals · `check:coverage` 100% (213/213) · the pen-test-basics report · the acceptance spine green · the five WP-16 hardenings. Provable by `pnpm verify`.
- **Out-of-repo, only the LAUNCH GATE closes (a merge cannot):** the five **filmed** acceptance videos on tenant-0 real freight (the `<5s`/`<10min`/real-driver/real-Claude/visual-dim measurements the code refuses to fabricate) · REQ-130 pricing re-base (CONFIRM + tenant-0 telemetry) · REQ-161 ICP/first-25 CRM (GTM, post-M-H) · CONFIRM-2 legal (ToS/Privacy/DPA/eBOL/trademark) · deliverability warmup (REQ-157, ≥2 wks) · the WP-15 M-AUTHORITY tenant-calendar gates · DR restore drill (REQ-135) · monitors stood up (REQ-114) · prod provisioning + the real TSA endpoint · the deferred REQ-212/213 defense-in-depth. All recorded in `docs/ops/GO-LIVE-CHECKLIST.md`.

## Verification
`pnpm verify` green: typecheck · lint · all suites · **check:invariants (21/22 tables, 35 kinds — now git-aware)** · rater-purity · **check:authority-coverage** · traceability · **check:coverage (100%, 213/213)** · identity (fail-closed) · fixtures · parities · **check:seed (SEED-1 verified)** · **audit:design (clean, blocking)**. `test:acceptance` green (the 5 demo spines).

## The build is at the launch gate
Sixteen work packages closed. The append-only co-signed ledger, the 13 agents, the three surfaces on the live map, money-as-a-projection-of-physics, the zero-cutover overlay, and the launch-gate assurance are all built and adversarially audited. What remains between here and live is the tenant-0 pilot, the filmed acceptance demos, the CONFIRM/legal/GTM gates, and prod provisioning — the calendar objects a merge was never going to close.
