# WP-01 — Repo + CI + assurance loop — checklist

## DoD (genesis/08 §03)

- [x] **CI green on empty app** — `pnpm verify` chain green 2026-07-09: typecheck (strict) → lint → 35 tools tests + 4 contracts tests + 19 API tests → invariants (0/22 tables) → traceability (no orphans, both directions) → identity lint (skip-with-warning, no local denylist) → fixture registry (10 pending, loud) → seed hash verified → design audit clean.
- [x] **Dummy PR without REQ-ID fails** — `PR_BODY="dummy PR, no requirement ids" pnpm check:pr` → exit 1 (also unit-tested in tools/traceability/traceability.test.ts).
- [x] **Cross-tenant read attempt fails the suite** — sabotage run (commit 24c4759): honoring `X-Tenant-Id` leaked `MARKER-TENANT-B` and the isolation suite went red (2 failures), green again on revert.

## Delivered (REQ → evidence)

| REQ | Evidence |
|---|---|
| REQ-025 | workers/api/test/isolation.test.ts — adversarial suite, runs on every merge; sabotage-verified |
| REQ-118 | tools/traceability/* + ci.yml gates + PR template; orphan detector green both directions |
| REQ-167 | tools/checks/identity-leak.ts — client-side denylist, masked output; seeded end-to-end leak verified failing; CI secret wiring in ci.yml |
| REQ-024/163 | eslint.config.mjs restricted-import bans + tools/checks/lint-guards.test.ts meta-tests |
| REQ-112 | fixtures/manifest.json (hash-pinned; sources are engagement-workspace M-refs) + tools/fixtures/verify.ts |
| REQ-154/134 | wrangler.toml no-secrets conventions, .gitleaks.toml + CI job, docs/ops/secrets.md rotation runbook |
| REQ-155 | tools/seed/generate.ts — deterministic SEED-1 (20 lifecycle shipments), pinned hash 70ede33ab8df… |
| REQ-156/106 | @shuddl/contracts envelope/codes + error, idempotency (tenant-scoped, replay-returns-original), claim-only tenant resolution — all tested |
| REQ-158/145/149 | tools/design/* advisory mode; **A1 executed: #A93018 measured 4.43:1, locked at #A52F18 = 4.58:1 by the contrast test** |
| REQ-131/114/117/135 | docs/security/threat-model.md · docs/ops/slo.md · docs/ops/dr-backups.md (+ nightly.yml stub) |
| REQ-111/132/133 | partial — event-shaped logs + health; JWT sessions + role guards + schema-parse; Zod at every boundary. See deferred. |
| I3/I8 | tools/checks/invariants.ts — table budget + append-only-events migration lint, unit-tested |

## Deferred slices (not lost — owned by later WPs)

- REQ-132 magic-link email leg → WP-06 (JWT sessions + role matrix live now; email delivery needs WP-06 infra)
- REQ-111 log→ledger pipeline → WP-02 (event-shaped log helper live now)
- REQ-155 SEED-1 DB load → WP-02 (schema required; artifact + hash gate live now)
- Design CI item 6 (blessed screenshots) → WP-03; blocking flip → WP-10 (REQ-158 mode file)
- Fixture vendor-in → blocked on engagement-workspace sources; legacy-export original path **[CONFIRM open]** (fixtures/manifest.json row)
- REQ-117 snapshot job activation → F1-A Cloudflare OIDC creds + WP-02 databases
- REQ-167 denylist population → tenant-side action (IDENTITY_DENYLIST CI secret); lint skips loudly until set
- GitHub Actions activation → F1-A (workflows authored; every gate also runs locally via `pnpm verify`)

## Build notes (assumption log)

- hono 4.12 requires the JWT alg explicit in `verify()` — fixed with "HS256"; errors route through `app.onError`, not middleware try/catch.
- typescript pinned ^5.9 (typescript-eslint 8.x does not yet support TS 7); vitest pinned ~3.2 in @shuddl/api (pool-workers peer range), vitest 4 elsewhere.
- Unauthenticated requests to unknown /v1 paths return 401 (not 404) — route existence is not revealed; hardening beyond plan.
- Governance prose (CLAUDE.md, README, plans, BUILD-PROMPT) is excluded from the orphan detector's annotation scan; implementation docs count.
- Root CLAUDE.md already matches genesis/11 (F0.3 install, fully-qualified paths) — no change needed at close.

## New REQ rows proposed

- (none — no scope discovered outside the register)

## WP-exit audit swarm (REQ-119)

- [x] Adversarial audit at WP-01 exit; findings → REQ rows/defects; no open Criticals at close —
      **checked 2026-08-03 (audit §99), and the box needs its history stated rather than a silent tick.**
      No WP-01-*labelled* exit swarm was ever run. WP-02 and WP-03 ran theirs (50-agent pattern; 2 auditors,
      4 Criticals + 6 Majors) and recorded them here; **WP-04…WP-16 have no such section at all.** The
      practice changed after WP-03 from per-WP swarms to periodic WHOLE-CODEBASE audits, and three have run:
      `docs/audits/2026-07-15-full-audit-and-skill-plan.md` (whole-codebase adversarial),
      `2026-07-22-wp16-launch-gate-audit.md`, and `2026-08-01-technical-debt-audit.md` (99 sections).
      WP-01's own surface — the CI chain, the traceability gates, the isolation suite, the fixture hash law —
      has been audited repeatedly by those, most recently at §63 (fixtures), §69 (append-only guards),
      §73 (REQ-025 isolation) and §83–§84 (the gate sweep). **The DoD item is satisfied; what was never true is
      the per-WP framing.** Ticking it without this note would have hidden a superseded practice behind a
      checkmark — see audit §64 on documents stating observations they no longer verify.
