{
  "extends": "../../tsconfig.base.json",
  // jsx exists ONLY because the Biller consumer renders the evidence email through @shuddl/agents'
  // shared .tsx view (renderEvidenceEmail); this worker serves no UI of its own.
  "compilerOptions": { "types": ["@cloudflare/workers-types", "@cloudflare/vitest-pool-workers"], "jsx": "react-jsx" },
  "include": ["src", "test", "vitest.config.ts"]
}
