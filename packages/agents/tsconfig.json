{
  "extends": "../../tsconfig.base.json",
  // DOM lib + jsx exist for the evidence-email VIEW only (the one React surface in this
  // package); the biller core (compose/gl-map) stays DOM-free by convention.
  "compilerOptions": { "lib": ["ES2022", "DOM", "DOM.Iterable"], "jsx": "react-jsx" },
  "include": ["src", "test"]
}
