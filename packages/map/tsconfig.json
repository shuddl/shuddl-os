{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "resolveJsonModule": true
  },
  "//": "perf/fleet-1k.ts + its test are typechecked; perf.spec.ts + playwright.config.ts import @playwright/test (an uninstalled, browser-only dev tool) so they are intentionally left out of the typecheck — pnpm verify must not depend on Playwright.",
  "include": ["src", "test", "vitest.config.ts", "perf/fleet-1k.ts", "perf/fleet-1k.test.ts"]
}
